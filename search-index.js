/* Generated by build.py — a plain-text search index built from
   the same content/depth data every page is rendered from. */

const SEARCH_INDEX = {
  "ap-csp/unit-1.html": "This Big Idea is about the process of making software: collaborating, defining a program's purpose, designing and iterating, and hunting down errors. It's the smallest slice of the multiple choice, but its vocabulary is exactly what the Create Task written responses are graded on. Collaboration produces better programs than one person working alone, and effective collaboration is a skill with specific practices. Every program has a purpose, and programs can produce results beyond that purpose — including new knowledge for the person who wrote it. Developers use an iterative, incremental process with planning tools, documentation, and testing, rather than writing everything at once. Errors are a normal part of development; there are distinct types of errors and systematic ways to find and fix them.",
  "ap-csp/1-1.html": "Programs get built by people working together. The CED cares about why collaboration helps and what makes it work. Collaboration brings together people with different perspectives, skills, and experiences , which produces programs that are more innovative, more accurate, and less biased than solo work. It helps at every stage: planning, designing, coding, testing, and reviewing. Collaboration is not only for splitting up work. Pair programming is the CED's named example: two people share one workstation. The driver writes code; the navigator reviews each line and thinks ahead. Roles switch regularly. Effective collaboration requires communication, consensus building, conflict resolution, and negotiation — the exam treats these as real skills, not soft filler. Feedback from users, teachers, and peers is part of collaboration. Incorporating it makes the final product better match what people actually need. Online tools (shared repositories, version control, messaging) let people collaborate without being in the same room. A team of three is building a study-timer app. One student likes minimal interfaces, one has strong design skills, one has used timers that annoyed them with too many alerts. Because they combine perspectives, the final design has a clean interface with a single, adjustable alert — something none of them would have built alone. That \"different perspectives → better product\" logic is what an exam question about collaboration is fishing for. When a question asks why collaboration helped, the correct option almost always mentions diverse perspectives , reduced bias , or catching errors earlier . Options claiming collaboration makes programs run faster or use less memory are distractors — collaboration is about the people process, not program performance. The CED gives a specific list of what collaboration reduces : it reduces bias, reduces errors (more eyes on the code), and makes programs more usable by more people (more perspectives on who the users are). When an exam option lists one of these three, that's the signal it's correct. Collaboration works because of complementary skills : one person may be strong at algorithm design, another at interface design, another at testing. The exam frames this as \"different perspectives and skills\" — the phrase to look for. Communication isn't only talking: shared documents, code comments, commit messages, and issue trackers are all collaboration tools. A question describing a team using a shared repository with change tracking is describing collaboration infrastructure. Consensus building means the team agrees on decisions rather than one person dictating; conflict resolution means disagreements get worked through, not ignored. Both are named in the CED as things effective collaborators do. The Create Task is individual even though collaboration during development is allowed — you must be able to say exactly what you personally wrote. Collaborating on ideas is fine; submitting someone else's algorithm as your own is not. Picking an option about performance. \"Collaboration makes the program run faster / use less memory\" is always wrong. Collaboration affects the development process and the quality of the design, not the program's execution. Confusing pair programming with splitting work. In pair programming both people work on the same code at the same time. If two people each write half the program separately, that's dividing labor, not pair programming. Assuming collaboration means more people = always better. The CED's claim is about diverse perspectives, not headcount. A team of ten identical thinkers doesn't get the benefit. Two students are working on a program together. One student types code while the other watches, reviews each line, and suggests improvements. After twenty minutes they switch roles. Which of the following best describes this practice? Pair programming Iterative development Code documentation Procedural abstraction One person writing (driver) while the other reviews (navigator), with roles swapping, is the definition of pair programming. Which of the following is a benefit of developing a program with a diverse team rather than alone? The program will always run more efficiently. The team is less likely to build in unintentional bias, because members bring different perspectives. The program will require less testing. Documentation becomes unnecessary. The CED explicitly ties diverse collaboration to reduced bias and better design. Efficiency, testing needs, and documentation are unaffected. Pair programming: two programmers share one workstation; one writes (driver) while the other reviews (navigator), switching regularly Collaboration: working together throughout development to combine perspectives and skills",
  "ap-csp/1-2.html": "Every program is written to do something for someone. This topic covers how to describe a program's purpose, function, and its inputs and outputs — vocabulary that comes straight back in the Create Task. A program (or software) is a collection of statements executed by a computer to complete a task. Programs are written to solve problems, express creativity, satisfy personal curiosity, or generate new knowledge. The CED distinguishes the program's purpose (what problem/need it addresses) from its function (what it actually does when it runs). Inputs are data the program receives — from a user (tactile, audio, visual, text), from a file, from a sensor, or from another program. Outputs are what it produces: text, graphics, audio, or data sent elsewhere. An event is an action (mouse click, key press, sensor reading) that triggers a specific program response. Event-driven programs run code in response to events rather than in a fixed sequence. Program behavior is defined by how it responds to inputs; the same program can produce different outputs given different inputs. A code segment is a portion of a larger program. A code statement is one complete instruction. The exam uses both terms. A weather program's purpose is to help someone decide what to wear. Its function is: take a ZIP code as input, request a forecast from a weather service, and display the high, low, and chance of rain. The user typing a ZIP code is an input ; the displayed forecast is an output ; pressing \"refresh\" is an event that triggers a new request. On the Create Task you must describe your own program in exactly these terms. The Create Task written response asks you to state your program's purpose and its function separately. Purpose = the human problem it addresses. Function = the behavior when it runs. Practice writing both in one sentence each for any program you look at. The CED distinguishes input types you should be able to name: tactile (keyboard, touchscreen), audio (microphone), visual (camera), and text . A sensor reading — temperature, GPS — is also input. Output can be tactile too (a phone vibrating). Event-driven programming is a distinct model from sequential: the program sets up handlers and then waits. Nothing happens until a click, key press, timer, or message arrives. Most apps you use are event-driven; most exam pseudocode is sequential. Know both. A program's purpose can be for the developer alone — to learn, to satisfy curiosity, to create art. The exam explicitly says a program \"can be developed for creative expression\" or \"to satisfy personal curiosity,\" and those are as legitimate as solving a business problem. \"Programs can generate new knowledge\" means the output teaches the programmer something they didn't know. Running a simulation of a coin flip 10,000 times and seeing the ratio converge is new knowledge even though you wrote the program. A code statement is a single instruction ( x ← 5 ). A code segment is several statements together. An expression is something that evaluates to a value ( x + 1 ) and isn't a complete statement on its own. The exam uses all three terms precisely. Swapping purpose and function on the Create Task. Purpose = why the program exists (the problem or need). Function = what it does when it runs (inputs → processing → outputs). Writing \"the purpose is to display a menu\" describes function, not purpose. Calling an output an event. An event is something that happens to the program (user clicks). The display that results is output. In a question about a button that shows a quote, the click is the event and the quote is the output — not the reverse. Assuming every program must have user input. A program can take input from a file or sensor with no human involved, or take no input at all and still produce output. A student writes a program that displays a random motivational quote each time the user clicks a button. Which of the following best describes the role of the button click in this program? An output of the program A procedure that must be defined by the user An event that triggers the program to run a segment of code A data abstraction that stores the quotes A user action that causes the program to respond is an event. The quote displayed is the output; the click is the trigger. Which of the following best distinguishes the purpose of a program from its function? Purpose is what the program does when it runs; function is the problem it solves. Purpose is the problem or need the program addresses; function is how the program behaves when it runs. Purpose refers to the program's inputs; function refers to its outputs. Purpose is defined by the computer; function is defined by the user. This is the CED's definition and the exact distinction the Create Task asks you to write about. Program: a collection of statements a computer executes to accomplish a task Input: data a program receives from a user, file, sensor, or another program Output: data or behavior a program produces, such as text, graphics, or audio Event: an action, such as a click or key press, that triggers a program response Code segment: a portion of a program's code",
  "ap-csp/1-3.html": "The CED describes a specific development process: investigate, design, prototype, test, repeat. Knowing the vocabulary for each piece — and what documentation is for — is worth several exam questions. A development process can be ordered and structured or exploratory, but good ones share phases: investigating and reflecting, designing , prototyping , and testing . Iterative development means going through those phases repeatedly, improving with each cycle. Incremental development means building and testing one small piece at a time and adding to what already works. The investigating phase includes understanding the users: surveys, interviews, observation, and gathering requirements about what the program needs to do and for whom. The designing phase includes brainstorming, planning the user interface, and using tools such as flowcharts or pseudocode to lay out an algorithm before writing real code. Program documentation — comments inside the code plus external descriptions — explains what a program does and how, so it can be understood, maintained, and reused later by the author or others. Documentation should be written during development, not only at the end. Comments are the in-code form of documentation; the computer ignores them. Good comments explain why and what , not just restate the line. Abstraction underpins good design: managing complexity by focusing on what something does rather than how. Using a procedure or a list is a form of abstraction. Requirements can change during development — that's normal, and iteration is how developers accommodate it. Building a grade-calculator: (1) Investigate — ask three classmates what they want; they all want weighted categories. (2) Design — sketch the screen and write pseudocode for the weighted average. (3) Prototype — code just the average for one category and confirm it's right. (4) Test — try zero grades, a 100, a missing category. Then loop back: add the second category, test again. Each loop is an iteration ; adding one category at a time is incremental . The comments you write during step 3 are documentation . If a question describes a team that repeatedly revisits earlier phases after testing, the answer is iterative . If it describes adding features one working piece at a time, the answer is incremental . They often happen together, but the exam tests them as separate terms. The CED describes two development processes: iterative and incremental , and also notes that some processes are unstructured or exploratory — you try things and see what works. Exploratory development is legitimate, especially early; the exam won't call it wrong. The four phases — investigating and reflecting , designing , prototyping , testing — aren't strictly linear. Testing reveals design flaws, which sends you back to designing. That loop is what \"iterative\" means. Investigating includes understanding the users , not just the problem: who are they, what do they need, what constraints do they have? A program designed without investigating users tends to serve the developer's assumptions instead. This links to 5.3 (bias). Program specifications — a description of what the program must do — come out of the investigating phase. The exam may call them requirements . They can change during development, and accommodating change is part of the process, not a failure. Documentation serves future developers , collaborators , users , and your future self . The CED's specific claim: documentation is useful \"during development\" as well as after, because it helps you keep track of what you've done and why. Two kinds of abstraction appear in design: procedural (a named procedure stands in for its steps) and data (a list stands in for many values). Good design uses both to keep the program understandable. Treating iterative and incremental as synonyms. The exam tests them separately. Iterative = repeating the phases to refine. Incremental = building in small working pieces. A team that builds a rough full version, tests, and refines it is iterating. A team that builds feature 1, tests, then feature 2 is being incremental. Thinking documentation is written only at the end. Options saying \"documentation is completed after the program is finished\" are wrong per the CED. It's throughout. Believing comments affect the program. Comments are ignored by the computer. Any option claiming comments make code faster, slower, or fix errors is a distractor. Skipping investigation on the Create Task. Students jump to coding, then can't answer the written response about their program's purpose for a specific user. Decide who it's for first. A programmer builds a small working version of a game with one level, tests it, then adds a second level and tests again, continuing until all levels are done. Which development approach does this best illustrate? Incremental development Sequential execution Data abstraction Fault-tolerant design Building and testing one piece at a time on top of working code is incremental development. Which of the following is the primary reason to include comments in program code? Comments make the program execute faster. Comments are required for the program to compile. Comments help programmers understand the purpose and behavior of code when they return to it later. Comments prevent runtime errors. Comments are documentation for humans. They have no effect on execution or errors. Iterative development: repeatedly cycling through design, build, and test phases to refine a program Incremental development: building a program in small pieces, testing each before adding the next Prototype: an early, partial version of a program used to test ideas Documentation: written descriptions and comments that explain what a program does and how it works Comment: a note inside code intended for humans and ignored by the computer Requirements: a description of what a program must do, often gathered from users",
  "ap-csp/1-4.html": "The exam names four kinds of errors and expects you to classify a scenario into the right one, then know how to find and fix it. Syntax error: the code violates the rules of the language (missing parenthesis, misspelled keyword). The program cannot run at all until it's fixed. Logic error: the code runs and finishes, but the result is wrong because the reasoning is flawed — e.g., using < where <= was intended, or an off-by-one loop bound. Run-time error: the code is syntactically valid but hits a problem while executing — dividing by zero, accessing index 0 or index beyond a list's length — and stops. Overflow error: the result of a calculation is too large for the computer to store in the fixed number of bits available for it. Related: round-off error , where real numbers can't be represented exactly. Testing means running the program with chosen inputs and comparing the actual output to the expected output. Good test cases include normal values, boundary values, and invalid or empty inputs. Debugging is the process of finding and fixing errors. Common strategies: hand-tracing (executing line by line on paper with a variable table), adding extra output statements to see intermediate values, and testing with different inputs to isolate the failing case. Some errors can be caught before running (syntax); most logic errors are only found by testing. count ← 0 FOR EACH score IN scores { IF (score > 90) { count ← count + 1 } } DISPLAY(count) Intended purpose: count scores of 90 or above. This runs with no error message, but a score of exactly 90 isn't counted because the condition uses > instead of >= . That's a logic error . You'd only catch it by testing with a score of 90 — which is why boundary values belong in every test set. Classify by symptom. Won't run at all → syntax. Runs, then crashes partway → run-time. Runs to the end with the wrong answer → logic. Result is a nonsense huge/tiny number → overflow. The exam rarely asks you to name the error type without giving you one of these symptoms. The CED gives exactly four error types: syntax , logic , run-time , and overflow . Overflow is its own category — a number too large for the storage available. Some resources fold it into run-time errors; the exam lists it separately. A subtle point: a logic error can produce correct output for some inputs and wrong output for others. That's why it's dangerous — testing with one input can miss it. Boundary values (0, negative, empty, max) are where logic errors hide. Testing has a specific shape in the CED: choose inputs, predict the expected output, run, compare. If they don't match, there's an error. The prediction step is what students skip — without it, you can't tell whether output is right. Debugging by hand-tracing means executing the code mentally or on paper with a specific input, recording variable values at each step. It's slow but reliable, and it's the method the exam's own code-analysis questions require you to use anyway. Extra output statements (temporary DISPLAY calls showing a variable's value mid-program) are a legitimate, CED-named debugging technique. Remove them when done. Programs with syntax errors cannot be run, so their behavior is never \"wrong output\" — it's \"no output.\" A question describing a program that ran and did something has ruled out syntax errors by definition. Calling a crash a logic error. If the program stops with an error message partway, it's run-time. Logic errors run to completion. The word \"terminates unexpectedly\" or \"crashes\" means run-time. Calling a wrong answer a syntax error. Syntax errors prevent running. If the program produced a value, its syntax was fine. Testing only typical inputs. The exam's \"which test case would reveal the error\" questions almost always have the answer at a boundary — the empty list, the value exactly equal to the threshold, zero. Assuming a program that ran without errors is correct. No error message means no syntax or run-time error. It says nothing about logic. A program is intended to calculate the average of a list of numbers. It runs to completion and displays a value, but the value is incorrect because the program divides by the wrong count. Which type of error is this? Syntax error Logic error Run-time error Overflow error The program runs and finishes but produces the wrong result, which is the signature of a logic error. A program attempts to access the element at index 8 of a list that contains only 5 elements. The program stops executing and reports an error. Which type of error occurred? Syntax error Logic error Run-time error Overflow error The code was valid, but a problem during execution (an out-of-range index) caused it to stop — a run-time error. Which of the following is the most effective way to determine whether a program's loop produces the intended result for boundary values? Read the code once quickly. Hand-trace the program with test inputs at the boundaries, recording each variable's value. Remove all comments from the program. Rename the variables. Hand-tracing with specific test inputs is the CED's named debugging technique for exactly this situation. Syntax error: a mistake in the code's structure that prevents it from running Logic error: a mistake in the algorithm that produces incorrect output while the program still runs Run-time error: an error that occurs during execution and stops the program Overflow error: a result too large to be represented in the available bits Round-off error: an error from real numbers that cannot be represented exactly in binary Hand-tracing: stepping through code manually, tracking each variable's value Debugging: finding and fixing errors in a program",
  "ap-csp/unit-2.html": "How computers represent, store, compress, and extract meaning from data. Students underrate this Big Idea because the topics seem simple, then lose points on the precise distinctions — lossy vs. lossless, correlation vs. causation, data vs. information. Everything a computer stores is binary, and the number of bits determines how many distinct values can be represented. Compression trades file size against fidelity; lossless keeps everything, lossy discards some. Data by itself is not information — information comes from processing, filtering, combining, and interpreting data in context. Programs are what make large-scale data analysis possible, and cleaning data is a real, necessary step.",
  "ap-csp/2-1.html": "All data on a computer is stored as sequences of bits. You need to convert between binary and decimal fluently and understand how the number of bits limits what can be represented. A bit is a single binary digit: 0 or 1. A byte is 8 bits. Binary is base 2 : each place value is a power of 2 (from the right: 1, 2, 4, 8, 16, 32, 64, 128 …). Decimal is base 10 , with place values that are powers of 10. Binary → decimal: add the place values where there's a 1. 1101 = 8 + 4 + 0 + 1 = 13. Decimal → binary: repeatedly subtract the largest power of 2 that fits. 22 → 16 (leaves 6) → 4 (leaves 2) → 2 (leaves 0), so 22 = 10110 . n bits can represent 2 n distinct values. 8 bits → 256 values (0–255). Adding one bit doubles the number of possible values. Numbers, text, images, and sound are all stored as binary once encoded. Text uses a scheme that maps each character to a number (ASCII, Unicode); an image maps each pixel to numbers for its color; audio is sampled at regular intervals and each sample stored as a number. A fixed number of bits for a value means a maximum representable size — the source of overflow errors. Some languages use a fixed size; others (and AP pseudocode) treat integers as arbitrary-size, so overflow is a real-world issue rather than a pseudocode one. Real numbers are stored with limited precision, so calculations can produce small round-off errors . Convert 10011 to decimal. Place values from the right: 1, 2, 4, 8, 16. Ones are in the 16, 2, and 1 places → 16 + 2 + 1 = 19 . Convert 45 to binary. 32 fits (13 left) → 8 fits (5 left) → 4 fits (1 left) → 1 fits (0 left). Bits set: 32, 8, 4, 1 → 101101 . Check: 32 + 8 + 4 + 1 = 45. How many colors can 3 bits represent? 2 3 = 8. With 4 bits, 16. Each added bit doubles the count. Write the place values above the bits (…32 16 8 4 2 1) before converting — it removes almost every arithmetic slip. For \"how many values\" questions, the answer is always a power of 2 equal to 2 raised to the number of bits, not the number of bits itself. The CED asks you to understand place value in both bases, not just memorize conversions. In decimal, 352 = 3×100 + 5×10 + 2×1. In binary, 101 = 1×4 + 0×2 + 1×1. Same idea, different base. Analog vs. digital: analog data (sound waves, temperature) is continuous. Digital data is discrete. Converting analog to digital means sampling — measuring at regular intervals — and each sample is stored as a number. More samples per second and more bits per sample = higher fidelity and larger file. Bit width limits matter in real languages. An 8-bit unsigned integer holds 0–255; add 1 to 255 and it wraps to 0 (overflow). A 32-bit signed integer maxes at about 2.1 billion. AP pseudocode ignores this, but the concept is tested. Round-off error comes from real numbers: 1/3 can't be stored exactly in any finite number of bits, in any base. So 0.1 + 0.2 might not exactly equal 0.3 in a computer. This is why financial software often works in cents (integers) rather than dollars (decimals). Converting binary to decimal quickly: write the place values above the digits from the right (1, 2, 4, 8, 16, 32, 64, 128). Converting decimal to binary: subtract the largest power of 2 that fits, repeat. Or divide by 2 repeatedly and read the remainders bottom-up. The number of bits needed to represent N distinct values is the smallest n with 2 n ≥ N. 100 values need 7 bits (2 7 = 128). 1,000 values need 10 bits. Off-by-one on place values. The rightmost bit is worth 1, not 2. Students who start at 2 get every conversion wrong by a factor. Answering with the number of bits instead of the number of values. \"How many values can 5 bits represent?\" is 32, not 5. \"How many bits to represent 32 values?\" is 5. Thinking adding a bit adds a fixed number of values. It doubles them. 3 bits → 8, 4 bits → 16, 5 bits → 32. Confusing sampling rate with bit depth. Rate = how often you measure; depth = how many bits each measurement gets. Both affect quality and size, in different ways. What is the decimal value of the binary number 101110? 42 44 46 48 Place values 32, 16, 8, 4, 2, 1 with bits 1,0,1,1,1,0 → 32 + 8 + 4 + 2 = 46. A game stores each player's level using 4 bits. The developers want to allow more than 16 levels. What is the minimum number of bits needed to represent 20 distinct levels? 4 5 10 20 4 bits gives 2⁴ = 16 values, not enough. 5 bits gives 2⁵ = 32 values, which covers 20. The answer is the smallest n with 2ⁿ ≥ 20. Which of the following is a true statement about how computers represent data? Only numbers can be stored as binary; text and images require a different representation. Each additional bit used to store a value halves the number of possible values. Text, images, and sound are all represented as binary sequences after being encoded as numbers. A byte can represent 8 different values. All data types are ultimately binary. Adding a bit doubles (not halves) values, and a byte represents 256 values, not 8. Bit: a single binary digit, 0 or 1 Byte: 8 bits Binary (base 2): a number system with two digits, where each place is a power of 2 Decimal (base 10): the everyday number system, where each place is a power of 10 Overflow error: a value exceeds what its fixed number of bits can hold Sampling: measuring an analog signal, like sound, at regular intervals to store it digitally",
  "ap-csp/2-2.html": "The single most-tested distinction in this Big Idea. Compression shrinks data; the question is always whether the original can be rebuilt. Data compression reduces the number of bits needed to store or transmit data. Fewer bits means less storage and faster transfer. Lossless compression reduces size while allowing complete reconstruction of the original. Used where every bit matters: text, program code, spreadsheets, and formats like PNG and ZIP. It typically achieves smaller savings. Lossy compression permanently removes some data to achieve much greater size reduction. The original cannot be exactly rebuilt, but the loss may be imperceptible. Used for JPEG images, MP3 audio, and streaming video. The trade-off: lossy gives smaller files at the cost of fidelity. The right choice depends on whether exact reconstruction is required and how much quality loss is acceptable. The amount of compression possible depends on the data: highly repetitive data compresses well losslessly (e.g., long runs of the same value); already-random data barely compresses. Decisions about compression are made based on the purpose: archiving a legal document → lossless; sending a photo to a friend → lossy is fine. The text AAAAAAABBBBBCCC can be stored losslessly as 7A5B3C — the original is exactly recoverable from the counts. That's the idea behind run-length encoding , a simple lossless technique. A photo, by contrast, might have thousands of nearly-identical blue-sky pixels; a lossy scheme averages them into fewer values, saving far more space, but the exact original pixels are gone. If the stem says the file must be \"exactly\" or \"perfectly\" restored, or involves text/code/financial records, answer lossless. If it emphasizes minimizing size or transmission time and the data is an image, video, or audio where small quality loss is acceptable, answer lossy. Also remember: compression is about size and speed — not security. The CED says the choice between lossy and lossless depends on the purpose : is the goal to minimize size or to preserve every detail ? Questions give you a purpose and expect the matching choice. Lossless compression exploits redundancy — patterns, repetition, predictable structure. Text has lots (common letters, repeated words), so it compresses well. Random data has none and barely compresses at all. Lossy compression exploits perception : humans can't hear very high frequencies or see tiny color differences between adjacent pixels, so that data can be dropped without a noticeable change. The \"loss\" is real but designed to be invisible. Lossy compression is irreversible . Decompressing a JPEG gives you an approximation of the original image, not the original. Compressing it again loses more. This is why editing workflows keep a lossless master. Compression ratio and quality trade off continuously — a lossy algorithm typically has a quality setting. Higher compression = smaller file = more loss. The exam may describe this as a slider or a percentage. Compression is about size (storage) and transmission time (bandwidth). It has nothing to do with security. Compressed data is not encrypted. Choosing lossy for anything that must be exact. Text documents, program source code, financial records, medical scans for diagnosis, legal archives: lossless. If the stem says \"exactly,\" \"perfectly,\" \"identical,\" or \"no data lost,\" it's lossless. Assuming lossless always gives the smallest file. It's the opposite — lossy compresses far more. Lossless is the safe choice, not the small choice. Confusing compression with encryption. An option saying \"compress the file so it can't be read by others\" is wrong. Compression doesn't hide data. Thinking a decompressed lossy file is restored. It's not. The detail is gone permanently. A hospital needs to store patient X-ray images so that every detail can be recovered exactly for later diagnosis, even if the files take more storage. Which compression approach is most appropriate? Lossy compression, because it reduces file size the most Lossless compression, because the original image can be fully reconstructed No compression is possible for image data Encryption, because it makes the file smaller Exact reconstruction is required, so lossless. Encryption is about security, not size. Which of the following best describes a trade-off of using lossy compression for a video that will be streamed over a slow connection? The video will be larger but higher quality. The video will be smaller and faster to transmit, but some detail is permanently lost. The video can be perfectly reconstructed, but only after transmission completes. The video cannot be played until decompressed by the sender. Lossy = big size reduction (good for slow connections) at the cost of irrecoverable detail. Data compression: reducing the number of bits needed to represent data Lossless compression: compression from which the original data can be completely reconstructed Lossy compression: compression that permanently discards some data for greater size reduction",
  "ap-csp/2-3.html": "Raw data isn't knowledge. This topic is about the steps between a pile of records and an actual conclusion — and the traps along the way. Information is the collection of facts and patterns extracted from data. Data becomes information when it's processed, organized, and interpreted. Programs make it possible to find patterns in datasets far too large for a person to read — this is why computing transformed fields from medicine to sports. Metadata is data about data: a photo's timestamp and location, a file's author, an email's routing headers. Metadata can be used to organize, search, and filter data, and changing or deleting it does not change the underlying data. Common operations for extracting information: filtering (keeping records that meet a condition), sorting , searching , aggregating (sum, average, count), and visualizing (charts, graphs) to make patterns visible. Combining data from multiple sources can reveal relationships not visible in any single source. Correlation is not causation. Two things trending together doesn't mean one causes the other. The exam tests this directly. Data can be incomplete, invalid, or biased — from how it was collected, who was included, or how it was cleaned. Conclusions inherit those flaws. The scale of data matters: large datasets may need distributed or parallel processing, and challenges include storage size, transfer time, and cleaning. Privacy concerns arise because metadata and combined datasets can identify or track people even when no single record seems sensitive. A school's attendance system stores 40,000 rows: student ID, date, period, present/absent. That's data . A program filters to first period, groups by day of week, and computes the absence rate — revealing Mondays have 30% more absences. That's information . If someone then claims \"Monday causes absences,\" that's the correlation/causation trap; a sports schedule or bus route could be the real driver. The row's timestamp and the ID of the device that recorded it are metadata . When a question presents a dataset and a conclusion, ask: does the data actually support that, or could something else explain it? Options that claim causation from a correlation are wrong. Options that say more data or a controlled comparison is needed are usually right. The CED separates data (raw facts) from information (what you learn from analyzing data) and knowledge (what you can do with that information). A list of temperatures is data. \"Average temperature rose 2° over ten years\" is information. \"We should plan for more cooling demand\" is knowledge. Metadata has a precise role in the CED: it helps find, organize, and manage data. A photo library sorted by date uses metadata; searching email by sender uses metadata. Changing metadata (renaming a file) doesn't change the data (the file's contents). Metadata is also a privacy vector . A photo's GPS metadata reveals where you were. An email's header metadata reveals who you talk to and when, even if the message is encrypted. This is why \"harmless\" metadata can matter. Scalability of data processing: as datasets grow to millions or billions of records, single computers can't hold or process them in reasonable time. This is where parallel and distributed computing (Big Idea 4) enters — the two Big Ideas connect here. Incomplete or invalid data can come from collection errors, sensor failures, people skipping survey questions, or data corruption. The exam's point: conclusions drawn from flawed data are flawed, no matter how good the analysis. Data can be biased at collection (who was surveyed), at cleaning (which records were removed), and at visualization (a chart with a truncated axis exaggerates differences). Each stage is a place bias can enter. Combining datasets can reveal patterns, but it can also de-anonymize people: individually harmless datasets, when joined, can identify individuals. This is a real privacy concern the CED expects you to recognize. Accepting causation from correlation. The most common wrong answer in this topic. If two things trend together, the correct options say \"correlated\" or \"a third factor may explain it.\" Options saying \"X causes Y\" are wrong unless the question describes a controlled experiment. Thinking metadata is part of the data. The date a document was created isn't in the document's text. The exam treats them as separate. Assuming more data fixes bias. If the collection method is biased, more data from the same method is more biased data. The fix is a better method, not more volume. Reading a chart without checking the axes. Truncated or uneven axes are a named form of misleading visualization. If a question shows a chart, look at the scale before drawing conclusions. A study finds that cities with more ice cream shops also have higher rates of sunburn. Which of the following is the most reasonable conclusion? Ice cream shops cause sunburn. Sunburn causes people to open ice cream shops. There is a correlation, likely explained by a third factor such as warm, sunny weather. The data must be incorrect. This is the classic correlation-not-causation setup. A shared cause (weather) explains both. A photograph file contains information about the date it was taken, the camera model, and the GPS location. Which of the following best describes this information? Lossy compression Metadata An algorithm A run-time error Data describing the photo, rather than the photo's pixels themselves, is metadata. Which of the following best explains how a program can help extract information from a dataset of millions of customer purchases? The program can filter, sort, and aggregate the records to reveal patterns too large for a person to find by hand. The program guarantees the data contains no bias. The program converts the data into a smaller file so it can be read manually. The program removes the need for data cleaning. Filtering, sorting, and aggregating at scale is exactly what programs add. They do not remove bias or the need to clean data. Information: facts and patterns extracted from data through processing and interpretation Metadata: data that describes other data, such as a file's creation date or location Correlation: two variables tending to change together, which does not by itself prove one causes the other Filtering: selecting only the records that meet a condition Cleaning data: fixing or removing incomplete, invalid, or inconsistent records",
  "ap-csp/2-4.html": "This topic closes the loop: programs are the tool for turning large datasets into answers, and that means handling messy data and asking well-formed questions. Programs can process data to discover information and generate new knowledge — automating what would be impossible manually at scale. Data cleaning is a necessary, real step: making data uniform (same date formats, consistent capitalization), removing duplicates, and handling missing or invalid values, all without changing the meaning of the data. Programs can transform data — reformat, combine, or derive new values — to make analysis possible (e.g., converting temperatures to one unit, computing an average per row). Visualization (charts, graphs, maps) communicates information in a dataset in a way that's much easier to interpret than the raw table. Extracting information requires asking a question first, then choosing which data, transformations, and tools answer it. Not every question can be answered by the data available. Tools include spreadsheets, purpose-built programs, and code; the process is the same regardless of tool. The knowledge gained from data can raise new questions , driving another round of collection and analysis (iteration again). You export a class survey to a spreadsheet. Some students typed \"yes\", others \"Yes\", others \"Y\". A program that normalizes all of these to yes is cleaning . Adding a column that converts \"hours per week\" from text like \"3h\" to the number 3 is transforming . Plotting hours-studied against grade is visualizing . Only after all three can you ask whether studying correlates with grades — and remember 2.3's warning about what a correlation does and doesn't show. A question about \"making data uniform so a program can process it\" wants the answer cleaning . Cleaning never changes what the data means — if an option describes altering values to get a desired result, that's data manipulation, not cleaning, and it's wrong. This topic exists because the CED wants you to understand that programs, not people, do the work on large data. A human can't compute the average of ten million values; a five-line loop can. The value of computing is that it makes analysis of large datasets possible at all. Cleaning in the CED means making data uniform without changing its meaning : consistent capitalization, consistent date formats, consistent units, removing exact duplicates, deciding how to handle blanks. The \"without changing meaning\" clause is what separates cleaning from manipulation. Transformation derives new values from existing ones: converting Celsius to Fahrenheit, computing a per-student average from a list of scores, combining first and last name into one field. The original data is preserved; new columns are added. Filtering selects a subset by condition (only seniors, only scores above 80). Sorting orders by a field. Aggregating combines many records into a summary (count, sum, average, max). Most analyses chain these. Visualization choices matter: a bar chart for categories, a line chart for change over time, a scatter plot for relationships between two variables. The wrong chart type hides the pattern. The CED says data analysis can raise new questions , driving new collection. That iterative loop is the same idea as iterative development — investigate, analyze, refine, repeat. Calling manipulation \"cleaning.\" If an option describes changing values to produce a desired conclusion (\"adjusting outliers upward to raise the average\"), that's not cleaning — it's falsification. Cleaning never changes meaning. Skipping cleaning and analyzing anyway. Questions that describe inconsistent data (\"NY\" and \"New York\") and ask what to do first want cleaning, not analysis. Choosing a visualization that hides the pattern. A pie chart for change over time, a line chart for unrelated categories. Match chart to question. A dataset of survey responses contains the values \"NY\", \"New York\", and \"new york\" for the same state. A programmer writes code to change all three to \"New York\" before analysis. Which of the following best describes this step? Lossy compression Data cleaning Encryption Creating metadata Making inconsistent values uniform without changing their meaning is data cleaning. Which of the following is most likely to help a researcher communicate a pattern found in a large dataset to a general audience? Displaying the full raw table of every record Creating a visualization such as a chart or graph of the pattern Compressing the dataset with a lossless algorithm Removing all metadata from the dataset Visualization exists to make information in data interpretable. The other options don't communicate a pattern. Data cleaning: making data uniform and valid without changing its meaning Data transformation: reformatting or deriving values from data to enable analysis Visualization: representing data graphically to make patterns easier to interpret",
  "ap-csp/unit-3.html": "The largest Big Idea by far, and the one that's mostly code. Variables, expressions, conditionals, loops, lists, procedures, then algorithm efficiency and its limits. Every topic here builds on the one before it — read them in order the first time. Programs store and manipulate values with variables, expressions, and lists; data abstraction manages complexity. Algorithms are built from sequencing, selection, and iteration, and can be expressed in many forms. Procedures with parameters provide procedural abstraction, and libraries let programmers reuse others' procedures. Randomness and simulation let programs model uncertain and real-world processes. Algorithms differ in efficiency; some problems have no reasonable-time solution, and some have no solution at all.",
  "ap-csp/3-1.html": "The foundation of every code-tracing question. A variable holds one value at a time, and assignment replaces it. A variable is a named abstraction for a value stored in memory. It holds one value at a time; assigning a new value replaces the old one. AP pseudocode uses a ← expression for assignment. The right side is evaluated first , then the result is stored in the variable on the left. Because the right side is evaluated first, x ← x + 1 means \"take the current value of x, add 1, store it back in x.\" Data types matter: numbers, Booleans (true/false), strings (text), and lists. Operations behave differently depending on type. Assigning a variable's value to another variable copies the value at that moment. Later changes to one don't affect the other. Good variable names ( totalScore , not t ) are a form of documentation and reduce logic errors. Swapping two values requires a third, temporary variable: temp ← a , a ← b , b ← temp . a ← 5 b ← a a ← a + 3 DISPLAY(a) DISPLAY(b) Trace it: a = 5. b gets a copy of 5. Then a becomes 5 + 3 = 8. b is still 5 — copying happened before a changed. Output: 8 5 . ( DISPLAY outputs its value followed by a space.) On any tracing question, draw a two-column table: variable name, current value. Update one row per line of code. Nearly every wrong answer on these questions comes from doing the update in your head and slipping. A variable's value can change (that's what makes it a variable) but its name and type don't. In AP pseudocode types aren't declared, but the exam still expects you to know what type a value is: 5 is a number, \"5\" is a string, true is a Boolean. Assignment is not symmetric . a ← b changes a and leaves b alone. b ← a is the opposite. Read the arrow: the value flows into the variable at the arrowhead's base. The order of statements is the order of evaluation. x ← 5 then x ← x * 2 gives 10. Reversed, x would be 5 (the first line would use whatever x was before). Sequencing questions test exactly this. Copying a value into a second variable creates an independent copy for simple values. Later changes to either don't affect the other. (Lists behave differently in some languages, but AP pseudocode questions generally treat list assignment as a copy too.) Swapping needs three assignments and a temporary. Two assignments ( a ← b , b ← a ) leave both variables with b's original value — the first line destroyed a's value before it was saved. Variable names should describe what the variable holds. The exam's own questions use descriptive names ( total , count , isFound ), and reading them tells you the algorithm's intent before you trace. Tracing in your head. The single biggest source of wrong answers in Big Idea 3. A table with one row per statement takes 30 seconds and removes the errors. Reading ← as equals. x ← x + 1 isn't a false equation; it's an instruction. Evaluate the right side with the current x, store the result in x. Assuming the copy updates. After b ← a , changing a does not change b. Students expect b to \"follow\" a. It doesn't. Botching the swap. If a question asks which code segment swaps two values, the answer uses a temp variable. Any two-line answer is wrong. Consider the following code segment. x ← 10 y ← x x ← 20 DISPLAY(y) What is displayed? 10 20 30 Nothing; the code has an error y received a copy of x's value (10) at the time of assignment. Changing x afterward does not change y. Which of the following code segments correctly swaps the values of variables a and b ? a ← b b ← a temp ← a a ← b b ← temp a ← b b ← temp b ← a a ← b Without a temporary variable, the first assignment overwrites one of the values before it can be copied. Option B saves a first. Variable: a named storage location that holds one value at a time Assignment: storing the result of an expression in a variable, written with ← in AP pseudocode Data type: the kind of value a variable holds: number, Boolean, string, or list",
  "ap-csp/3-2.html": "A list lets one name refer to many related values. That's data abstraction, and it's one of the two abstraction types the Create Task explicitly grades. Data abstraction provides a separation between the abstract properties of a data type and the concrete details of its representation. Practically: a list lets you treat a whole collection as one thing. A list is an ordered sequence of elements. Each element is referenced by its index . In AP pseudocode, indexing starts at 1 , so the first element is list[1] and the last is list[LENGTH(list)] . Lists manage complexity : instead of score1 , score2 , … score30 , you use one list scores and loop over it. Adding a 31st score requires no code changes. The Create Task requires you to identify a list in your program and explain how it manages complexity — this exact phrase. The answer is about handling many values with one name and code that works regardless of how many there are. A string is also an ordered sequence (of characters), and a list can contain strings, numbers, Booleans, or even other lists. Using a list in place of many separate variables makes the program easier to develop, maintain, and read . Without a list, averaging five test scores means five variables and a formula that must be rewritten if a sixth test is added. With a list: scores ← [88, 92, 79, 95, 84] total ← 0 FOR EACH s IN scores { total ← total + s } DISPLAY(total / LENGTH(scores)) Add a sixth score to the list and nothing else changes. That's the \"manages complexity\" argument the Create Task wants. Index questions are a constant source of lost points. AP pseudocode is 1-indexed. list[0] is an error. If a question shows real-language code (Python-style), it will say so — otherwise assume 1-based. The CED phrase is that data abstraction \"provides a separation between the abstract properties of a data type and the concrete details of its representation.\" In plain terms: you think of scores as \"the list of scores\" and don't think about where each number lives in memory. A list can hold different types in AP pseudocode — numbers, strings, Booleans, even other lists — though most exam lists are one type. A list of lists is how you'd model a grid. The Create Task requires that your list (a) is used to store data and (b) that the program's functionality would be harder or impossible without it . The written response asks you to explain how the list manages complexity. The expected answer: without the list, you'd need a separate variable for each value, and code would have to change every time the number of values changed. Index starts at 1 in AP pseudocode. list[1] is the first element, list[LENGTH(list)] is the last. Every real language you'll use starts at 0 — the exam is the exception, and it's deliberate: they test whether you read the reference sheet. Lists are ordered : the position of each element is meaningful and stable. Two lists with the same elements in different orders are different lists. A string is conceptually an ordered list of characters, but AP pseudocode doesn't let you index into strings the way you index lists. The exam defines any string operations it needs within the question. Index 0. list[0] in AP pseudocode is an error. The first element is list[1] . This single fact is tested on nearly every exam. Explaining the list too vaguely on the Create Task. \"The list stores my data\" doesn't earn the point. You need: what the list holds, and why the program couldn't do its job without it (or would need many separate variables). Confusing data abstraction with procedural abstraction. A list is data abstraction. A procedure is procedural abstraction. Both \"manage complexity,\" but they're different mechanisms and the Create Task asks about them separately. In AP pseudocode, what is displayed by the following code? names ← [\"Ava\", \"Ben\", \"Cy\", \"Dee\"] DISPLAY(names[3]) Ava Ben Cy Dee Indexing starts at 1, so names[3] is the third element, \"Cy\". A student's program stores each of 40 book titles in a separate variable. Which of the following changes would best manage the complexity of the program? Rename each variable with a shorter name. Store the titles in a single list and use a loop to process them. Add a comment above each variable. Convert the titles to uppercase. Replacing many variables with one list is the CED's definition of how data abstraction manages complexity. Data abstraction: using a structure like a list so a collection of values can be treated as one unit List: an ordered sequence of elements, each accessed by index Element: a single value in a list Index: the position of an element in a list; starts at 1 in AP pseudocode",
  "ap-csp/3-3.html": "Arithmetic in pseudocode follows normal order of operations, with one operator — MOD — that shows up constantly. Operators: + , - , * , / , and MOD . Standard precedence applies: parentheses first, then * / MOD left to right, then + - left to right. a MOD b is the remainder when a is divided by b . 17 MOD 5 = 2, 20 MOD 4 = 0, 3 MOD 10 = 3 (if a < b, the result is a). MOD uses: n MOD 2 = 0 tests for even; n MOD 10 gets the last digit; i MOD LENGTH(list) wraps an index around; seconds MOD 60 converts to minutes and seconds. / in AP pseudocode is regular division that can produce decimals ( 7 / 2 = 3.5). There is no integer-division operator on the reference sheet. A sequence of statements executes in order, one after another. The order changes the result: x ← x * 2 then x ← x + 1 is not the same as the reverse. Arithmetic on values of the wrong type (e.g., adding a number to a string) is an error in most languages. a ← 17 b ← a MOD 5 c ← a / 5 d ← (a + 3) MOD 4 DISPLAY(b) DISPLAY(c) DISPLAY(d) b = 17 MOD 5 = 2. c = 17 / 5 = 3.4. d = 20 MOD 4 = 0. Output: 2 3.4 0 . Whenever you see MOD, ask \"what's the remainder?\" and do the long division. For expressions mixing MOD with other operators, put parentheses around the MOD part mentally — MOD has the same precedence as multiplication, so a + b MOD c computes the MOD first. MOD's precedence is the same as multiplication and division — higher than addition and subtraction. 10 + 7 MOD 3 is 10 + 1 = 11 , not 17 MOD 3 = 2 . When operators are the same precedence, evaluate left to right. MOD for wraparound: if you're cycling through positions 1 to n and want to go from n back to 1, the formula is (i MOD n) + 1 . Try it: i = n gives (0) + 1 = 1. i = 1 gives 2. This shows up in circular list and clock problems. MOD for digit extraction: n MOD 10 is the last digit; n MOD 100 is the last two digits. To get other digits you'd need division, and the exam may define an integer-division procedure for that purpose within a question. MOD with a smaller left operand: 3 MOD 7 is 3 — 7 goes in zero times, remainder 3. Students sometimes answer 0 or 4. The remainder of a small number divided by a bigger one is the small number itself. AP pseudocode's / is real division : 7 / 2 is 3.5. Unlike Java or Python 2, there's no integer division operator on the reference sheet. If a question needs integer division, it will define a procedure or use MOD to get the same effect. Expressions can nest: ((a + b) * c) MOD d . Work inside-out, innermost parentheses first. Applying MOD last. a + b MOD c means a + (b MOD c) . Students compute (a + b) MOD c . Same precedence as *, so it goes before +. Returning the quotient instead of the remainder. 17 MOD 5 is 2 (remainder), not 3 (quotient). MOD is always the leftover. Assuming / truncates. It doesn't in AP pseudocode. 9 / 2 is 4.5. Ignoring statement order. x ← x + 1 then x ← x * 2 is not the same as the reverse. Trace in order. What is the value of the expression 25 MOD 7 ? 3 4 18 3.57 7 goes into 25 three times (21), leaving a remainder of 4. Which of the following expressions evaluates to true exactly when the variable n holds an odd integer? n MOD 2 = 0 n MOD 2 = 1 n / 2 = 1 n MOD 1 = 0 Odd numbers leave a remainder of 1 when divided by 2. What is displayed by the following code? x ← 4 x ← x * 3 x ← x + 2 DISPLAY(x MOD 5) 0 2 4 14 x becomes 12, then 14. 14 MOD 5 = 4. MOD: the remainder after integer division; a MOD b Expression: a combination of values, variables, and operators that evaluates to a single value Sequencing: executing statements in order, one after another",
  "ap-csp/3-4.html": "Strings are ordered sequences of characters. The exam tests the concept — concatenation and substrings — rather than specific string functions. A string is an ordered sequence of characters (letters, digits, spaces, symbols), written in quotes: \"Hello\" . Concatenation joins strings end to end. \"AP\" + \"CSP\" → \"APCSP\" . Note there's no automatic space. A substring is any contiguous run of characters within a string. \"Comp\" is a substring of \"Computer\" ; \"Cptr\" is not. Strings can be compared, searched, and have their length measured. The exact operation names vary by language; the CED expects the concepts, not a specific syntax. A string containing digits, like \"42\" , is text, not a number — concatenating \"4\" + \"2\" gives \"42\" , not 6. Strings are immutable in many languages: operations produce a new string rather than changing the original. first ← \"Grace\" last ← \"Hopper\" full ← first + \" \" + last DISPLAY(full) Concatenation with an explicit space in the middle → Grace Hopper . Without the + \" \" + , the output would be GraceHopper . Questions love that missing space. When the exam shows string operations, it defines them in the question (e.g., \"the procedure SUBSTRING(str, start, end) returns…\"). Read that definition carefully — in particular, whether the end index is included — and apply it literally rather than assuming a language you know. The CED's essential knowledge on strings is minimal: they're ordered sequences of characters, and you can concatenate and take substrings . Everything else — length, indexing, searching — the exam defines inside the question if it's needed. Concatenation with + works on strings in AP pseudocode. Combining a string and a number is not defined on the reference sheet; if a question does it, it will explain the behavior. A substring must be contiguous. \"cat\" is a substring of \"concatenate\" (positions 4–6). \"cnt\" is not, even though all three letters appear in order. The empty string \"\" is a valid string with zero characters. Concatenating it changes nothing. It's often the starting value when building a string in a loop. When a question defines a string procedure — say, SUBSTRING(str, start, length) or SUBSTRING(str, start, end) — the parameters matter enormously. One version takes a length, one takes an end index, and the end may or may not be inclusive. Read the definition every time. Building a string character by character in a loop is the standard pattern for reversing or filtering text: start with \"\" , add one character per iteration. Adding a space that isn't there. \"a\" + \"b\" is \"ab\" . If a space is needed it must be concatenated explicitly. Treating a numeric string as a number. \"4\" + \"2\" is \"42\" if both are strings. Arithmetic isn't happening. Assuming a substring definition. Don't apply Java's or Python's rules. Use whatever the question says, including whether indices start at 1 and whether the end is inclusive. The variable word holds the string \"program\" . Which of the following is a substring of word ? \"pgm\" \"gram\" \"margorp\" \"prog ram\" A substring must be a contiguous run of the original characters. \"gram\" is the last four characters in order. What is displayed by the following code? a ← \"12\" b ← \"3\" DISPLAY(a + b) 15 123 36 An error, because strings cannot be added Both values are strings, so + concatenates them: \"12\" followed by \"3\" is \"123\". String: an ordered sequence of characters Concatenation: joining two strings end to end Substring: a contiguous sequence of characters within a string",
  "ap-csp/3-5.html": "Every conditional and loop depends on a Boolean expression. You have to evaluate them exactly, including the ones designed to be misread. A Boolean value is either true or false . A Boolean expression evaluates to one of those two. Relational operators compare values: = , ≠ , > , < , ≥ , ≤ . Note: in AP pseudocode = is comparison, not assignment (assignment is ← ). Logical operators: NOT a is true when a is false. a AND b is true only when both are true. a OR b is true when at least one is true (including both). Precedence: NOT is evaluated before AND , which is before OR . Use parentheses when in doubt — the exam does. De Morgan's laws show up as \"equivalent expression\" questions: NOT (a AND b) = (NOT a) OR (NOT b) , and NOT (a OR b) = (NOT a) AND (NOT b) . Two Boolean expressions are equivalent if they have the same value for every combination of inputs. A truth table proves it. The negation of x > 5 is x ≤ 5 , not x < 5 . Missing the boundary case is a classic distractor. Let age ← 16 and hasLicense ← false . age ≥ 16 AND hasLicense → true AND false → false age ≥ 16 OR hasLicense → true OR false → true NOT (age < 18) → NOT true → false NOT (age ≥ 16 AND hasLicense) → NOT false → true , which by De Morgan equals age < 16 OR NOT hasLicense → false OR true → true. ✓ For \"which expression is equivalent\" questions, don't reason abstractly — plug in all four combinations of true/false (or a few boundary numbers) and compare outputs. It takes 30 seconds and is essentially error-proof. Truth tables are the reliable tool. For two Booleans a and b there are four rows: TT, TF, FT, FF. Evaluate both expressions on each row; if all four match, they're equivalent. For three Booleans, eight rows. It's mechanical and it works. Short-circuit evaluation isn't formally in the CSP CED, but understanding that false AND anything is false and true OR anything is true speeds up evaluation and helps you see equivalences. De Morgan's laws in practice: to negate \"x is between 1 and 10\" — which is x ≥ 1 AND x ≤ 10 — you get x < 1 OR x > 10 . Flip AND to OR, flip each comparison to its opposite (including the boundary). The negation of a comparison flips to the opposite operator, which always includes the boundary the original excluded: NOT(>) is ≤, NOT(≥) is <, NOT(=) is ≠. Students who write NOT(x > 5) as x < 5 miss the case x = 5. Relational operators return a Boolean, so x > 5 by itself is a complete Boolean expression that can be assigned: isBig ← x > 5 . Then IF (isBig) works without writing IF (isBig = true) . Precedence: relational operators evaluate first, then NOT, then AND, then OR. So NOT a AND b is (NOT a) AND b . The exam uses parentheses generously, but knows students misgroup when it doesn't. Dropping the boundary when negating. NOT(score ≥ 90) is score < 90, not score ≤ 90 or score < 89. De Morgan half-done. NOT(a AND b) becomes (NOT a) OR (NOT b). Students negate the parts but forget to flip AND to OR (giving NOT a AND NOT b, which is wrong). Treating OR as exclusive. a OR b is true when both are true, too. It's inclusive or. Grouping NOT loosely. NOT a OR b is (NOT a) OR b . NOT applies only to the thing right after it unless parentheses say otherwise. Which of the following expressions is equivalent to NOT (x > 10 AND y = 3) ? x ≤ 10 AND y ≠ 3 x ≤ 10 OR y ≠ 3 x < 10 OR y ≠ 3 x > 10 OR y = 3 De Morgan: NOT (A AND B) = (NOT A) OR (NOT B). NOT (x > 10) is x ≤ 10 (include the boundary), NOT (y = 3) is y ≠ 3. The variables a and b are Boolean. For which values is the expression (a OR b) AND (NOT a) true? a = true, b = true a = true, b = false a = false, b = true a = false, b = false NOT a requires a = false. Then (a OR b) requires b = true. Only option C satisfies both. Boolean: a value that is either true or false Relational operator: an operator that compares two values, such as = or < Logical operator: NOT, AND, or OR — combines or negates Boolean values Equivalent expressions: expressions that evaluate to the same value for every possible input",
  "ap-csp/3-6.html": "Selection means the program chooses which code to run based on a condition. IF and IF/ELSE are how it does that. Selection is one of the three building blocks of algorithms (with sequencing and iteration). It lets a program take different paths. IF (condition) { block } runs the block only when the condition is true; otherwise it skips it and continues. IF (condition) { block1 } ELSE { block2 } runs exactly one of the two blocks — block1 if true, block2 if false. Never both, never neither. After the conditional finishes, execution continues with the next statement in sequence regardless of which branch ran. Two separate IF statements are independent — both conditions are checked and both blocks can run. An IF/ELSE is mutually exclusive. This difference is a frequent exam trap. The condition must be a Boolean expression. Everything from 3.5 applies. temp ← 72 IF (temp > 80) { DISPLAY(\"hot\") } IF (temp > 60) { DISPLAY(\"warm\") } ELSE { DISPLAY(\"cold\") } First IF: 72 > 80 is false → nothing. Second IF/ELSE: 72 > 60 is true → warm , and the ELSE is skipped. Output: warm . Had the first IF been true, both \"hot\" and \"warm\" would display, because they're independent statements. Count the IFs. Independent IFs can each fire; an IF/ELSE chain fires exactly one branch. When a question asks \"how many lines are displayed,\" this distinction is usually the whole question. An IF with no ELSE means \"maybe do this.\" An IF/ELSE means \"do exactly one of these two.\" The difference is whether there's a case where nothing in the structure runs. The condition is evaluated once , when the IF is reached. If the block changes variables that were in the condition, that doesn't re-trigger anything — the decision was already made. Two IF statements in sequence are independent . Both conditions are checked; zero, one, or both blocks may run. This is the most common structure the exam uses to test whether you understand independence vs. mutual exclusion. IF/ELSE can be rewritten as two IF statements with opposite conditions — IF (x > 5) and IF (x ≤ 5) — and the behavior is identical as long as the first block doesn't change x . If it does, the second IF might see a different x. Equivalence questions exploit this. The exam's block-based pseudocode draws IF/ELSE as connected shapes. Read it the same way: one condition, two branches, exactly one runs. Selection combined with iteration — an IF inside a loop — is how counting, filtering, and finding patterns work. The IF picks which iterations \"count.\" Reading two IFs as an IF/ELSE. If both conditions can be true for the same input, both blocks run. Count outputs accordingly. Reading an IF/ELSE as two IFs. Exactly one branch runs. If a question asks \"which values cause both DISPLAY statements to run\" and they're in an IF/ELSE, the answer is \"none.\" Forgetting execution continues after the conditional. Whatever's after the closing brace runs regardless of which branch was taken. What is displayed by the following code? score ← 85 IF (score ≥ 90) { DISPLAY(\"A\") } ELSE { IF (score ≥ 80) { DISPLAY(\"B\") } ELSE { DISPLAY(\"C\") } } A B C B C 85 ≥ 90 is false, so the ELSE runs. Inside, 85 ≥ 80 is true, so \"B\" is displayed and the inner ELSE is skipped. Consider the following code, where n is a positive integer. IF (n MOD 2 = 0) { DISPLAY(\"even\") } IF (n > 10) { DISPLAY(\"big\") } For which value of n are two words displayed? 7 8 11 12 Both independent IFs must be true: even AND greater than 10. Only 12 qualifies. Selection: choosing which code to execute based on a Boolean condition Conditional statement: an IF or IF/ELSE statement",
  "ap-csp/3-7.html": "A conditional can live inside another conditional's block. The inner one is only reached if the outer path leads there — tracing them carefully is the entire skill. Nested conditionals are conditional statements placed inside the block of another conditional. The inner condition is only evaluated when the outer branch containing it executes. If the outer condition is false, the inner one never runs at all. Nesting lets you express \"if A, then check B\" logic — multiple criteria that depend on one another. Nested IF/ELSE inside ELSE branches builds a multi-way choice (like grade ranges A/B/C/D/F). Only one leaf branch ever executes. Many nested conditionals can be rewritten using AND / OR , and vice versa. The exam asks which rewrite is equivalent . Check with test values. Deep nesting is hard to read; matching the braces to the correct IF is where tracing errors happen. Indentation in the exam's code is reliable — use it. IF (isMember) { IF (total > 50) { discount ← 20 } ELSE { discount ← 10 } } ELSE { discount ← 0 } Members with a total over 50 get 20; other members get 10; non-members get 0. An equivalent flat version: IF (isMember AND total > 50) → 20, ELSE IF (isMember) → 10, ELSE → 0. Same outputs for every input, so they're equivalent. Trace nested conditionals from the outside in. Decide the outer condition, cross out the branch that doesn't run, and only then look at what's inside the surviving branch. Never evaluate an inner condition before you've confirmed its outer branch runs. The rule for nested conditionals: an inner condition is only evaluated if execution reaches it . If the outer condition is false and the inner IF is inside the outer's true-block, the inner condition is never checked at all — not \"checked and found false,\" simply never checked. Nested IF/ELSE in ELSE branches builds a chain: check A; if not A, check B; if not B, check C; else D. Exactly one leaf runs. This is how grade ranges, tax brackets, and any multi-way categorization is expressed. Order in a chain matters when conditions overlap. Checking score ≥ 70 before score ≥ 90 sends a 95 down the 70 branch. Put the most restrictive condition first, or make them mutually exclusive with ranges. A nested IF inside another IF's true-block, with no ELSEs, is equivalent to a single IF with AND: IF (a) { IF (b) { X } } ≡ IF (a AND b) { X } . Adding ELSEs breaks this equivalence — then you need to think about all the paths. For \"which rewrite is equivalent\" questions, the technique is to enumerate paths: what inputs reach each DISPLAY or assignment in the original? Then check the candidate produces the same result for each. Deep nesting (three or more levels) is a readability problem the exam sometimes asks about — the fix is usually a compound condition or a chain. Evaluating an inner condition that was never reached. If the outer branch didn't run, nothing inside it did. Cross it out entirely before looking at what's inside. Mis-pairing an ELSE. In block pseudocode the braces make it unambiguous, but students still attach an ELSE to the wrong IF when tracing quickly. Match each ELSE to the IF whose closing brace immediately precedes it. Assuming equivalence after a superficial check. Testing one input isn't proof. For equivalence questions, test an input for each branch of the original. What is displayed by the following code? x ← 5 y ← 12 IF (x > 3) { IF (y < 10) { DISPLAY(\"one\") } ELSE { DISPLAY(\"two\") } } ELSE { DISPLAY(\"three\") } one two three two three x > 3 is true, so the outer IF branch runs. Inside, y IF (a > 0) { IF (b > 0) { DISPLAY(\"yes\") } } IF (a > 0 OR b > 0) { DISPLAY(\"yes\") } IF (a > 0 AND b > 0) { DISPLAY(\"yes\") } IF (NOT (a > 0)) { DISPLAY(\"yes\") } IF (a > 0) { DISPLAY(\"yes\") } \"yes\" appears only when both a > 0 and b > 0 — that's AND. Nested conditional: a conditional statement placed inside the block of another conditional",
  "ap-csp/3-8.html": "Iteration repeats code. The exam's favorite questions: how many times does the loop run, and what's the final value when it stops? Iteration (looping) is the third building block of algorithms. The code inside the loop is the loop body . REPEAT n TIMES { body } runs the body exactly n times. REPEAT UNTIL (condition) { body } checks the condition before each pass . If the condition is already true at the start, the body runs zero times . Otherwise it runs, then re-checks, until the condition becomes true. FOR EACH item IN list { body } runs the body once per element, in order, with item holding the current element (covered more in 3.10). An infinite loop occurs when a REPEAT UNTIL condition can never become true. The exam asks you to spot the variable that never changes. The typical pattern: initialize a counter or accumulator before the loop, update it inside, and use it after. Tracing = table with one row per iteration. Loop variables retain their final value after the loop ends. If a REPEAT UNTIL stops when i = 5 , i is 5 afterward. i ← 1 sum ← 0 REPEAT UNTIL (i > 4) { sum ← sum + i i ← i + 1 } DISPLAY(sum) DISPLAY(i) check i > 4? sum i after 1 > 4 false 1 2 2 > 4 false 3 3 3 > 4 false 6 4 4 > 4 false 10 5 5 > 4 true → stop Output: 10 5 . Four iterations; i ends at 5, not 4. REPEAT UNTIL runs while the condition is false . Students constantly flip this. Read it as \"keep going until this becomes true.\" And always check the initial condition — a loop that's already satisfied runs zero times. REPEAT UNTIL checks its condition before each iteration, including the first. If the condition is already true, the body runs zero times. (Some real languages have do-while loops that run the body at least once; AP pseudocode's REPEAT UNTIL does not.) The condition is a stopping condition: the loop continues while it's false and stops when it becomes true. Students used to \"while\" loops must mentally negate. REPEAT UNTIL (i > 4) is the same as \"while i ≤ 4.\" REPEAT n TIMES runs exactly n times; there's no loop variable you can read. If you need a counter, declare and increment one yourself. Infinite loop detection: find the variables in the stopping condition, then check whether the body ever changes them in the direction that would make the condition true. If the condition is i > 10 and the body does i ← i - 1 , it never stops (assuming i starts ≤ 10). The final value of a loop variable after the loop is the value that made the condition true — one step past the last iteration's value. If the body ran with i = 1, 2, 3, 4, then i is 5 afterward. The exam's block-based pseudocode draws REPEAT UNTIL as a shape with the condition on top. Same semantics. Combining a loop with an accumulator ( sum ← sum + i ) is how sums, counts, and products are computed. The accumulator must be initialized before the loop. Running the body while the condition is true. REPEAT UNTIL is the opposite — body runs while false. This one flip is responsible for a large share of wrong loop answers. Forgetting the zero-iteration case. If the stopping condition is true before the loop starts, nothing inside runs. Check the initial state first. Reporting the last iteration's value instead of the final value. The variable's final value is the one that stopped the loop, which is one step beyond. Not initializing the accumulator. A sum that's never set to 0 before the loop is undefined. On the exam it's a logic error to spot. How many times is \"hi\" displayed by the following code? count ← 10 REPEAT UNTIL (count < 4) { DISPLAY(\"hi\") count ← count - 2 } 3 4 5 The loop never ends count goes 10 → 8 → 6 → 4 → 2. The body runs while count ≥ 4: for values 10, 8, 6, 4 — four times. When count is 2, the condition is true and the loop stops. Which of the following loops will never terminate? x ← 0 REPEAT UNTIL (x ≥ 5) { x ← x + 1 } x ← 10 REPEAT UNTIL (x = 0) { x ← x - 2 } x ← 3 REPEAT UNTIL (x > 100) { x ← x * 2 } x ← 1 REPEAT UNTIL (x = 0) { x ← x + 1 } In D, x starts at 1 and only increases, so x = 0 can never become true. Option B does terminate: 10, 8, 6, 4, 2, 0. What is the value of total after the following code runs? total ← 0 REPEAT 3 TIMES { total ← total + 5 total ← total * 2 } 30 50 70 150 Pass 1: 0+5=5, ×2=10. Pass 2: 15, ×2=30. Pass 3: 35, ×2=70. Iteration: repeating a block of code Loop body: the statements inside a loop that are repeated REPEAT UNTIL: a loop that runs while its condition is false and stops once it becomes true Infinite loop: a loop whose exit condition is never met",
  "ap-csp/3-9.html": "An algorithm is a finite set of instructions that solves a problem. This topic is about how the three building blocks combine and the standard patterns you're expected to recognize on sight. An algorithm is a finite sequence of precise instructions that accomplishes a task. It must end. Every algorithm is built from sequencing , selection , and iteration . The exam asks you to identify which one a code fragment illustrates. Algorithms can be expressed in natural language, flowcharts, pseudocode, or code — the same algorithm in different forms is still the same algorithm. Different algorithms can solve the same problem; they may differ in efficiency or clarity but produce the same result. Standard patterns to recognize: finding the max/min (track the best so far), computing a sum or average (accumulator), counting items that meet a condition, checking whether a value is present in a list, and building a new list from filtered elements. Existing algorithms can be combined or modified to solve new problems — you don't start from scratch every time. Reading an algorithm: identify what's initialized, what changes each iteration, and what's true when it stops. That reveals its purpose. Find the largest value in a list — the canonical pattern: biggest ← nums[1] FOR EACH n IN nums { IF (n > biggest) { biggest ← n } } DISPLAY(biggest) Initialize to the first element (never to 0 — that fails for all-negative lists), compare each element, keep the winner. Swap > for < and it finds the minimum. Replace the IF with total ← total + n and it sums. Recognizing these variants is faster than re-deriving them. When asked \"what does this algorithm do,\" don't trace with random values — trace with a tiny list of 3 items and watch what the tracked variable equals at the end. The initialization line usually tells you the pattern before you've read anything else. The CED's definition of an algorithm has three properties: it's a finite set of instructions (it ends), the instructions are precise (no ambiguity), and following them accomplishes a task . \"Add salt to taste\" fails precision; \"count forever\" fails finiteness. Every algorithm can be built from the three control structures: sequencing , selection , and iteration . This is a theoretical result, and the exam treats it as a fact you should know. There's no fourth structure you need. The same algorithm can be written in many forms — natural language, flowchart, pseudocode, any programming language — and it's still the same algorithm. Questions may show a flowchart and ask which pseudocode matches, or vice versa. Different algorithms can solve the same problem. To find the maximum, you could scan once tracking the largest, or sort and take the last element. Both work; they differ in efficiency (3.17). Recognizing a pattern from its initialization: x ← list[1] before a loop with a comparison → finding min or max. x ← 0 before a loop with x ← x + … → sum or count. found ← false before a loop → searching. Read the setup line first. Modifying an existing algorithm — changing > to < to find min instead of max, or changing a count into a sum — is a named skill. The exam gives working code and asks what single change achieves a new goal. The CED expects you to know these specific algorithms: finding max/min, computing sum/average, counting elements meeting a condition, determining whether a value is in a list, and combining or reordering lists. These appear as both \"what does this do\" and \"which code does this\" questions. Initializing max to 0. Fails for lists of all negative numbers. Correct: initialize to the first element. The exam asks \"for which list does this algorithm fail\" — the answer is the all-negative list. Tracing with a realistic list. Use three elements. It's enough to see the pattern and short enough not to make arithmetic mistakes. Confusing count and sum. count ← count + 1 counts. sum ← sum + item sums. Look at what's being added. Assuming a flowchart is a different algorithm. Form doesn't matter; logic does. What does the following code display, where vals ← [4, 9, 2, 7] ? count ← 0 FOR EACH v IN vals { IF (v > 5) { count ← count + 1 } } DISPLAY(count) 2 4 16 22 This is the counting pattern. Values greater than 5 are 9 and 7 → count = 2. A programmer wants to modify the algorithm below so it finds the smallest value instead of the largest. best ← list[1] FOR EACH x IN list { IF (x > best) { best ← x } } Which single change accomplishes this? Change best ← list[1] to best ← 0 Change x > best to x Change best ← x to x ← best Remove the IF statement Flipping the comparison makes it track the smallest value seen. Initializing to 0 would break the algorithm for lists of positive numbers. Algorithm: a finite set of precise instructions that accomplishes a task Sequencing: statements executed in order Selection: choosing a path with a conditional Iteration: repeating with a loop Accumulator: a variable that builds up a result, such as a running total, across iterations",
  "ap-csp/3-10.html": "The reference sheet gives you a small set of list operations. Combined with FOR EACH, they're behind a big share of the code questions. AP pseudocode list operations (all 1-indexed): list[i] accesses element i; list[i] ← value assigns it; LENGTH(list) gives the number of elements. APPEND(list, value) adds to the end and increases length by 1. INSERT(list, i, value) puts value at index i, shifting later elements right. REMOVE(list, i) deletes the element at i, shifting later elements left. After INSERT or REMOVE, indices of later elements change . A question that removes index 2 and then reads index 3 is reading what used to be index 4. Traversal means visiting every element. FOR EACH item IN list is the complete traversal; a REPEAT UNTIL with an index can do partial traversals. Common list algorithms: linear search (check each element until found), filtering into a new list, counting, sum/average, min/max, reversing, checking for duplicates. A list can be empty: [] , with LENGTH 0. Accessing list[1] on an empty list is a run-time error. Lists can contain lists, allowing grids or tables — but the CED keeps most questions to one dimension. a ← [10, 20, 30, 40] REMOVE(a, 2) APPEND(a, 50) INSERT(a, 1, 5) DISPLAY(a[3]) DISPLAY(LENGTH(a)) Step through: start [10,20,30,40] → REMOVE index 2 → [10,30,40] → APPEND 50 → [10,30,40,50] → INSERT 5 at index 1 → [5,10,30,40,50] . Then a[3] = 30, LENGTH = 5. Output: 30 5 . Rewrite the list after every INSERT/REMOVE/APPEND — literally write it out with brackets. Tracking index shifts in your head is where these go wrong. And in a FOR EACH loop, changing the loop variable does not change the list. The complete list of operations on the AP reference sheet: list[i] (access), list[i] ← value (assign), LENGTH(list) , APPEND(list, value) , INSERT(list, i, value) , REMOVE(list, i) , and FOR EACH . That's all. Anything else is defined in the question. INSERT shifts right: the element at index i and everything after it moves to i + 1, i + 2, … and the new value goes at i. Length grows by 1. REMOVE shifts left: the element at i is gone, and everything after moves down. Length shrinks by 1. APPEND is INSERT at LENGTH + 1. After a REMOVE, the index you just removed now holds what used to be the next element. This causes the classic \"remove every matching element in a forward loop\" bug: matching elements adjacent to each other get skipped. (The Java version of this bug is in CSA 4.9.) FOR EACH item IN list visits every element in order. The variable item holds a copy of the current element. Changing item inside the loop does not change the list. To change list elements you need an index loop with list[i] ← … . Linear search — checking each element until you find the target — is the only search you can do on an unsorted list. It's O(n) in the worst case: n elements means up to n comparisons. Building a new list by filtering: start with an empty list, FOR EACH over the original, APPEND matches. The original is unchanged. This is how \"return a list of all elements that…\" is done. An empty list has LENGTH 0, and accessing any index is an error. Algorithms that read list[1] before checking length will fail on empty input — a testable edge case. Not rewriting the list after each operation. Index shifts are invisible unless you write the list out. Do it every time. Reading the wrong index after INSERT/REMOVE. After REMOVE(a, 2) , what was a[3] is now a[2] . Questions are built around this. Modifying the FOR EACH variable and expecting the list to change. It won't. Using index 0. Still 1-based. Still tested. What is displayed after the following code runs? nums ← [3, 8, 1, 6] INSERT(nums, 2, 9) REMOVE(nums, 4) DISPLAY(nums) [3, 9, 8, 6] [3, 9, 8, 1] [9, 3, 8, 6] [3, 8, 9, 6] INSERT 9 at index 2 → [3, 9, 8, 1, 6]. REMOVE index 4 (the 1) → [3, 9, 8, 6]. The following procedure is intended to return true if target appears in list . PROCEDURE contains(list, target) { FOR EACH item IN list { IF (item = target) { RETURN true } } RETURN false } Which best describes the algorithm used? Binary search Linear search Sorting Data compression Checking each element in order until a match is found is a linear (sequential) search. The list data has 7 elements. After REMOVE(data, 1) and then APPEND(data, 100) , what is LENGTH(data) ? 6 7 8 9 Remove one (6), append one (7). Traversal: visiting each element of a list, usually with FOR EACH APPEND: adds a value to the end of a list INSERT: places a value at a given index, shifting later elements right REMOVE: deletes the element at a given index, shifting later elements left Linear search: checking elements one by one until the target is found or the list ends",
  "ap-csp/3-11.html": "Binary search finds a value in a sorted list by repeatedly cutting the search space in half. It's dramatically faster than linear search — but only on sorted data. Binary search requires the list to be sorted . On unsorted data it doesn't work at all. This is the single most-tested fact. Procedure: look at the middle element. If it's the target, done. If the target is smaller, discard the upper half; if larger, discard the lower half. Repeat on the remaining half. Each step halves the number of elements left to check. A list of 1,000 elements takes at most about 10 checks (2 10 = 1,024); a million takes about 20. Linear search checks elements one at a time and may need to check every element — up to n checks for n items. The exam asks: given a sorted list of N items, what's the maximum number of checks binary search needs? Answer: the smallest k where 2 k > N. For 15 items that's 4 (2 4 = 16 > 15); for 64 items it's 7 (2 7 = 128 > 64). Roughly log₂ N, rounded down, plus one for the final check. Binary search is a selection + iteration algorithm: repeated comparison and narrowing. Trade-off: sorting has a cost. If you only search once, sorting then binary searching may not beat a single linear search. If you search many times, sort first. Sorted list: [2, 5, 9, 14, 21, 30, 44, 51, 63] , target 44. Middle (index 5) is 21. 44 > 21 → keep the right half: [30, 44, 51, 63] . Middle is 44 (or 51 depending on rounding; either way one more step). Found at step 2 or 3. Linear search would have needed 7 checks to reach 44. With 9 elements, binary search never needs more than 4 (2 4 = 16 ≥ 9). If a question about binary search doesn't say the list is sorted, that's the answer: binary search can't be used. If it asks for the maximum number of checks, find the smallest power of 2 that is greater than the list size and use that exponent: 8 items → 4, 15 → 4, 16 → 5, 64 → 7, 100 → 7, 1000 → 10. Why sorted is required: binary search decides which half to discard by comparing the middle element to the target. That decision is only valid if smaller values are all on one side and larger on the other — i.e., if the list is sorted. On unsorted data, the target could be in the discarded half. The maximum number of comparisons for n elements is the smallest k such that 2 k > n. This equals ⌊log₂ n⌋ + 1. Table: n = 7 → 3; n = 8 → 4; n = 15 → 4; n = 16 → 5; n = 100 → 7; n = 1000 → 10; n = 1,000,000 → 20. A question may give the actual list and target and ask which elements are examined . Trace it: middle first, then the middle of the remaining half, until found. Different rounding conventions for the middle (floor vs. ceiling) can change the exact elements; the exam usually specifies or uses a list where it doesn't matter. Linear vs. binary trade-off: linear needs no preparation but takes up to n steps. Binary needs a sorted list (sorting costs time) but takes log n steps. For one search on an unsorted list, linear wins. For many searches, sort once and use binary. Binary search is a selection + iteration algorithm: each iteration selects a half. Its efficiency is logarithmic , which the CED classifies as reasonable time — and it's the exam's main example of an algorithm that scales dramatically better than linear. Binary search generalizes: guessing a number between 1 and 1000 by always guessing the middle takes at most 10 guesses. Same algorithm, same log₂ bound. Applying binary search to an unsorted list. If the stem doesn't say sorted, binary search isn't valid. That's frequently the entire question. Off-by-one on the step count. For n = 16, students say 4 (log₂ 16). The maximum is 5, because after four halvings you have one element left and still have to check it. Use \"smallest 2 k greater than n.\" Assuming binary search is always better. If the list is unsorted and you search once, linear search is faster than sorting-then-binary. A list of 64 numbers is sorted in ascending order. What is the maximum number of elements that must be examined to find a target value using binary search? 6 7 32 64 Each check halves what remains: 64 → 32 → 16 → 8 → 4 → 2 → 1 takes six checks to get down to a single element, and a seventh check examines that element. The smallest power of 2 greater than 64 is 2⁷ = 128, so the maximum is 7. Which of the following is a requirement for binary search to be used on a list? The list must contain only unique values. The list must be sorted. The list must have an even number of elements. The list must contain only integers. Binary search relies on ordering to decide which half to discard. Which of the following best describes the advantage of binary search over linear search? Binary search works on unsorted lists. Binary search always finds the value in one step. Binary search eliminates half of the remaining elements with each comparison, so it needs far fewer steps on large lists. Binary search uses less memory. The halving is the whole reason binary search scales so well. Binary search: a search on a sorted list that repeatedly halves the range being examined Sorted list: elements arranged in order, required for binary search",
  "ap-csp/3-12.html": "Calling a procedure hands control to it, possibly with values, and gets control (and maybe a result) back. The vocabulary here is precise and tested. A procedure is a named group of instructions that can be executed by calling its name. Other languages call these functions or methods. Parameters are the variables listed in the procedure's definition. Arguments are the actual values passed in when it's called. The first argument goes into the first parameter, and so on. When a procedure is called, the program jumps into the procedure, runs it, and then returns to the line after the call. That interrupts normal sequential flow. RETURN (expression) ends the procedure immediately and sends the value back to the caller. Code after a RETURN in the same path never runs. A procedure with a RETURN can be used inside an expression: x ← square(4) + 1 . A procedure without a RETURN (one that just DISPLAYs or changes things) is called as a statement. The same procedure can be called many times with different arguments — that's the point. Modularity: breaking a program into procedures makes each piece easier to write, test, and reuse. PROCEDURE double(n) { RETURN (n * 2) } a ← 3 b ← double(a) + double(5) DISPLAY(b) double(a) passes the argument 3 into parameter n , returns 6. double(5) returns 10. b = 16. Output: 16 . Note that a is still 3 — the procedure got a copy. Track \"where am I\" during a trace. When you hit a procedure call, write down the line you'll return to, go execute the procedure with the arguments substituted for the parameters, and come back. RETURN means you stop reading the procedure instantly — even if there are lines below it. A procedure call interrupts sequential execution : the program remembers where it was, jumps to the procedure, runs it, then returns to the line after the call. If that line was in the middle of an expression, the returned value is substituted in and evaluation continues. Parameters are local to the procedure. They're created when it's called, initialized to the argument values, and gone when it returns. Changing a parameter inside the procedure doesn't change the caller's variable (for simple values). A procedure can have zero parameters (called with empty parentheses) or several. Arguments are matched to parameters by position , not name: the first argument fills the first parameter. RETURN does two things: it provides the value the call evaluates to, and it ends the procedure immediately . Any code after a RETURN that executes is skipped. Multiple RETURNs in a procedure (one per branch) is normal; exactly one runs per call. A procedure without a RETURN produces no value — it's called for its side effect (displaying something, modifying a list). Using such a call in an expression ( x ← show(5) + 1 ) is meaningless. Procedures can call other procedures, and can call themselves (recursion isn't on the CSP exam, but nested calls are). Trace each call to completion before continuing the caller. The AP reference sheet defines the syntax: PROCEDURE name(parameter1, parameter2) { instructions } and RETURN (expression) . Block-based pseudocode draws the same thing as shapes. Continuing past a RETURN. Once RETURN executes, the procedure is done. Code below it on the same path never runs, even if it looks like it should. Losing your place. Write down the line number you'll return to before jumping into the procedure. Nested calls need a stack of these. Expecting the caller's variable to change. double(a) doesn't change a. Only the returned value matters — and only if it's stored. Matching arguments by name. Position is all that matters. If the procedure is f(x, y) and you call f(y, x) , the values are swapped. Consider the following procedure. PROCEDURE mystery(a, b) { IF (a > b) { RETURN (a - b) } RETURN (b - a) } What is displayed by DISPLAY(mystery(4, 9)) ? -5 5 13 Nothing is displayed 4 > 9 is false, so the first RETURN is skipped. The second RETURN gives 9 - 4 = 5. In the call calculate(7, \"kg\") to a procedure defined as PROCEDURE calculate(amount, unit) , which of the following is true? amount and unit are arguments; 7 and \"kg\" are parameters. amount and unit are parameters; 7 and \"kg\" are arguments. 7 is a parameter and \"kg\" is an argument. The terms parameter and argument are interchangeable on the exam. Parameters are the names in the definition; arguments are the values in the call. Procedure: a named block of code that can be called; also called a function or method Parameter: a variable in a procedure's definition that receives a value when called Argument: the actual value passed to a procedure when it is called Return value: the value a procedure sends back to the code that called it Modularity: dividing a program into separate procedures that each handle one task",
  "ap-csp/3-13.html": "Writing your own procedures is where procedural abstraction happens — and the Create Task requires you to have one with a parameter, selection, and iteration. Procedural abstraction lets a programmer use a procedure by knowing what it does without knowing how . The name and parameters are the interface; the body is hidden detail. Benefits: reduces duplicated code , makes programs easier to read and maintain, and lets you fix a bug in one place instead of many. Parameters generalize a procedure. A procedure that only works on one hardcoded value is far less useful than one that takes that value as a parameter. Good procedure names describe the task ( calculateTax , isValidEmail ). Like variable names, they're documentation. Create Task requirement: a student-developed procedure with at least one parameter that affects its behavior, and whose body includes an algorithm with sequencing, selection, and iteration. You must show the procedure being called , too. Procedures can call other procedures, building larger behavior from smaller verified pieces. A procedure should do one clearly defined thing. If you're describing it with \"and,\" it may be two procedures. Without a procedure, computing a letter grade three times means the IF/ELSE chain appears three times. With one: PROCEDURE letterGrade(score) { IF (score ≥ 90) { RETURN (\"A\") } IF (score ≥ 80) { RETURN (\"B\") } IF (score ≥ 70) { RETURN (\"C\") } RETURN (\"F\") } DISPLAY(letterGrade(91)) DISPLAY(letterGrade(74)) Output: A C . If the school changes the B cutoff, you edit one line. That's the maintenance benefit the CED describes. When a question asks why a programmer should turn repeated code into a procedure, the right answer is about reducing duplication and making changes easier . Answers about making the program run faster or use less memory are distractors — procedures don't do that. Procedural abstraction in the CED: a procedure lets you use a computation by name without knowing (or re-reading) how it works. Once isPrime(n) is written and tested, you call it and trust it. That trust is the abstraction. The Create Task's procedure requirement is specific: a student-developed procedure with a name, a return type (or no return), and at least one parameter that has an effect on the procedure's functionality . The body must contain an algorithm with sequencing, selection, and iteration. And you must show a call to it. \"Parameter that has an effect\" means the parameter is actually used inside and changes what happens. A parameter that's received and ignored doesn't count. Generalization is the reason for parameters: greetRam() can only do one thing; greet(name) can do infinitely many. When code is duplicated with small variations, the variations become parameters and the code becomes one procedure. Procedures make programs easier to modify (change the body once), easier to test (test the procedure alone with known inputs), and easier to read (a good name explains the intent). The exam's \"why use a procedure\" questions want one of these. A procedure should have a single, clear purpose reflected in its name. calculateAverage should calculate an average and nothing else. Mixing responsibilities makes procedures harder to reuse. Procedures reduce duplicated code . Duplication is bad because a bug in duplicated code must be fixed everywhere it appears — and one copy inevitably gets missed. A Create Task procedure with a parameter that isn't used. Doesn't meet the requirement. The parameter must affect behavior. Choosing an efficiency reason for using a procedure. Procedures don't make programs faster. They make them clearer and easier to maintain. Options claiming speed or memory benefits are distractors. Not showing the call. The Create Task needs the procedure and a call to it in your code. A defined-but-never-called procedure doesn't count. A program contains the same 8-line block of code in five different places, differing only in the number used on the first line. Which of the following is the best improvement? Add a comment above each copy explaining that it is repeated. Write a procedure with a parameter for the differing number and call it in each of the five places. Combine the five copies into one very long block. Rename the variables in each copy so they are distinct. Duplicated logic with one varying value is exactly what a parameterized procedure is for. Which of the following best describes procedural abstraction? Storing many values in a list so they can be processed together Using a procedure by knowing its name and what it does, without needing to know how it is implemented Writing a program without any procedures Compressing code so it takes less space Procedural abstraction hides implementation behind an interface. Option A describes data abstraction. Procedural abstraction: using a procedure without needing to know how its internal code works Generalization: using parameters so a procedure works for many inputs, not one fixed case",
  "ap-csp/3-14.html": "Libraries are collections of procedures written by someone else. Using them well means reading documentation and understanding what an API promises. A software library is a collection of procedures that can be used in other programs — often written and tested by other developers. Benefits: save development time, reuse code that's already been debugged, and gain capabilities (graphics, networking, math) you'd otherwise have to build yourself. An API (application programming interface) specifies how a library's procedures are called: their names, parameters, and what they return. It's the contract between your code and the library. Documentation is essential for using a library correctly — it explains what each procedure does, what it needs, and how it behaves in edge cases. You can't use a library well without it. Using a library is procedural abstraction in practice: you call sort(list) without knowing the sorting algorithm inside. Libraries are typically imported into a program before their procedures can be called. Suppose a math library's documentation says: ROUND(x, places) returns x rounded to the given number of decimal places. You never see the rounding code; you just call ROUND(3.14159, 2) and get 3.14. If the documentation had said the second parameter is the number of significant figures , the same call would return 3.1. Reading the API, not guessing, is the skill. Questions here are usually scenario-based: a developer wants to add a map to an app. The correct reasoning is \"use a library to avoid rewriting tested code,\" and the correct next step is \"read its documentation/API.\" Any option suggesting you must understand the library's internal implementation is wrong. A library is a collection of procedures (and sometimes data) that other programs can use. The procedures were written, tested, and documented by someone else — that's the value: you don't redo their work. An API specifies how to use a library: the procedure names, what to pass in, what comes back. You program against the API. The implementation behind it can change without your code changing, as long as the API stays the same. Documentation is the CED's emphasis: you can't use a library correctly without reading how each procedure behaves — what it expects, what it returns, what happens in edge cases. A question describing a developer who \"read the documentation to understand the parameters\" is describing correct library use. Libraries make development faster (don't rewrite tested code) and programs more reliable (widely-used libraries have had their bugs found). They also give access to capabilities you couldn't build yourself — graphics, networking, machine learning. Using a library is procedural abstraction at scale : you call sortList(list) without knowing whether it uses merge sort or quicksort. The abstraction is the whole point. Libraries are also a collaboration mechanism (1.1) — the people who wrote the library are, in effect, on your team. Assuming you need the source code. You use a library through its API. Options saying \"the developer must understand the library's implementation\" are wrong. Claiming libraries guarantee correctness. They're usually well-tested, but calling them with the wrong arguments still produces wrong results — hence documentation. Confusing a library with a language feature. Built-in operators (+, MOD) aren't library calls. A library adds procedures beyond what the language provides. A programmer wants to display a bar chart in their program. Rather than writing the drawing code from scratch, they use a charting library. Which of the following is the most important reason this approach is effective? The library's procedures have already been developed and tested, saving time and reducing errors. Programs that use libraries are always shorter. Libraries eliminate the need for documentation. Using a library guarantees the program has no bugs. Reusing tested code is the core benefit. Libraries don't guarantee bug-free programs or remove the need for documentation. Which of the following best describes an API? A type of lossy compression The set of procedures, their parameters, and return values that a library makes available for other programs to call The internal source code of a library A list used to store data An API is the interface — what you can call and how — not the implementation. Software library: a collection of pre-written procedures that other programs can use API: the specification of how to use a library: its procedures, parameters, and return values Documentation: written explanation of how a library's procedures behave",
  "ap-csp/3-15.html": "One procedure, one rule: RANDOM(a, b) returns a random integer from a to b, inclusive. Nearly every question is about that inclusivity or about probability. RANDOM(a, b) returns a random integer between a and b, inclusive . RANDOM(1, 6) simulates a die: 1, 2, 3, 4, 5, or 6. Each call is independent. Calling it twice can give the same value or different values — the program can't predict which. Number of possible outcomes = b − a + 1. RANDOM(1, 10) has 10 outcomes; RANDOM(0, 10) has 11. Each outcome is equally likely. Probability of a specific value = 1 / (b − a + 1). Probability that RANDOM(1, 10) ≤ 3 is 3/10. Random values are used in games, simulations (3.16), sampling, and testing. To scale or shift randomness, combine with arithmetic: RANDOM(1, 6) + RANDOM(1, 6) simulates two dice (2–12, not uniformly distributed). A program that uses random values will generally produce different output each time it runs — a question may ask which statements are possible rather than certain. x ← RANDOM(1, 4) IF (x ≤ 1) { DISPLAY(\"rare\") } ELSE { DISPLAY(\"common\") } Possible values: 1, 2, 3, 4 (four outcomes). \"rare\" displays only when x = 1 → probability 1/4 = 25%. \"common\" displays 75% of the time. Count outcomes as b − a + 1, then count how many satisfy the condition. Students forget that both endpoints are included and get 1/9 instead of 1/10. If a question describes wanting a value \"from 1 to 100,\" the call is RANDOM(1, 100) , not RANDOM(0, 100) . RANDOM(a, b) returns an integer from a to b inclusive , each with equal probability. The count of possible values is b − a + 1 . RANDOM(1, 100) has 100 outcomes; RANDOM(0, 100) has 101. Probability of a condition: count outcomes satisfying it, divide by total. RANDOM(1, 20) < 6 → outcomes 1–5, so 5/20 = 25%. Combining random calls: two dice is RANDOM(1, 6) + RANDOM(1, 6) , giving 2–12 but not uniformly — 7 is most likely. The exam may ask whether an expression produces each value equally often; sums of random values don't. Scaling and shifting: if you only have RANDOM(1, 10) and need even numbers 2–20, use RANDOM(1, 10) * 2 . If you need multiples of 5 from 0 to 45, (RANDOM(1, 10) - 1) * 5 . Random values make programs non-deterministic : the same program can produce different output each run. Questions about such programs ask what's possible or what must be true, not what \"the\" output is. Random values are essential to simulations (3.16) — they model the variation and uncertainty in real processes — and to games, sampling, and testing with varied inputs. Excluding an endpoint. Both a and b are possible results. RANDOM(1, 6) can return 6. Counting outcomes as b − a. It's b − a + 1. Ten outcomes from 1 to 10, not nine. Assuming a sum of random values is uniform. It isn't. Middle values are more likely. Asking \"what is the output\" of a random program. There isn't one. The question will ask what's possible or what the probability is. What is the probability that the following code displays \"win\"? n ← RANDOM(1, 5) IF (n > 3) { DISPLAY(\"win\") } 1/5 2/5 3/5 1/2 Outcomes 1–5 (five total). n > 3 is true for 4 and 5 → 2/5. Which of the following code segments simulates flipping a fair coin, displaying \"heads\" or \"tails\" with equal probability? IF (RANDOM(1, 2) = 1) { DISPLAY(\"heads\") } ELSE { DISPLAY(\"tails\") } IF (RANDOM(0, 2) = 1) { DISPLAY(\"heads\") } ELSE { DISPLAY(\"tails\") } IF (RANDOM(1, 3) = 1) { DISPLAY(\"heads\") } ELSE { DISPLAY(\"tails\") } IF (RANDOM(1, 2) > 2) { DISPLAY(\"heads\") } ELSE { DISPLAY(\"tails\") } RANDOM(1, 2) has exactly two equally likely outcomes. B and C have three outcomes (not 50/50); D can never be true. RANDOM(a, b): returns a random integer from a to b, inclusive, each equally likely",
  "ap-csp/3-16.html": "A simulation is a program that models a real or imagined situation. The exam tests why we simulate and what's lost when we do. A simulation is an abstraction of a more complex object or phenomenon, used to investigate it in a controlled way. Why simulate: the real thing may be too dangerous (crash tests), too expensive, too slow (climate over centuries), too fast, or simply impossible to observe (a bridge that isn't built yet). Simulations can be run many times and with varied parameters. Simulations remove details and make simplifying assumptions . That's what makes them tractable — and what limits their accuracy. Every simulation question wants you to notice this trade-off. Because of simplifications, a simulation's results may not match reality; conclusions must account for what was left out. Simulations often use random values (3.15) to model uncertainty or variation, and are run repeatedly to see the range of outcomes. Investigating a simulation: change one parameter at a time and observe the effect. That's how simulations generate hypotheses. A simulation is a model, not an experiment on the real system — but it can guide which real experiments are worth running. A city wants to know whether adding a traffic light reduces accidents. Building the light and waiting a year is slow and risks real crashes. A simulation models cars arriving at random intervals, the light cycling, and drivers' reaction times. It ignores weather, driver distraction, and road conditions — simplifications. Running it 10,000 times shows accidents drop 40% in the model . That's useful for deciding to try the light, but the 40% number shouldn't be quoted as a real-world prediction. Two answer patterns dominate. \"Why use a simulation?\" → safety, cost, time, or impossibility of the real thing. \"What's a limitation?\" → simplifying assumptions mean results may not reflect reality. If an option says a simulation gives \"exact\" or \"guaranteed\" real-world results, it's wrong. A simulation is an abstraction of a real-world process: it keeps the parts that matter for the question being asked and drops the rest. The CED's phrase is that simulations \"remove details\" and \"make assumptions\" to simplify. The reasons to simulate, per the CED: the real experiment is impossible (a future bridge), dangerous (a nuclear reaction), expensive (a rocket launch), too slow (centuries of climate), or too fast (a chemical reaction). Simulations are also repeatable with changed parameters. The core limitation: because details were removed, the simulation may not match reality . Results are only as good as the assumptions. A simulation that assumes every person has the same number of daily contacts will mispredict a disease that spreads through super-spreaders. Random values in a simulation model real-world variability. Running the simulation many times and looking at the distribution of results is how you get useful predictions from a random model — a single run tells you one possibility. Investigating with simulation: change one input, hold the others constant, observe the change in output. That's how simulations generate hypotheses about the real system. A simulation can be biased in the same ways data can: if the assumptions reflect one group's experience, the simulation predicts well for that group and poorly for others. Simulations can also be used as a testing tool for programs: simulate user input or network conditions to test a program without real users. Claiming a simulation gives exact real-world results. It gives results under its assumptions. Any \"guarantees\" or \"exactly predicts\" option is wrong. Saying a simulation has no assumptions. Every simulation simplifies. That's the definition. Confusing a simulation with the real thing. A simulated crash test doesn't damage a car. That's the point — and the reason results need validation against real data eventually. An engineer uses a computer simulation to test how a bridge design responds to earthquakes of different strengths. Which of the following is the most likely reason for using a simulation instead of a physical test? Simulations always produce perfectly accurate results. Testing a real bridge under real earthquakes would be dangerous, expensive, and impractical, while a simulation can be run many times safely. Simulations do not require any assumptions about the bridge. A physical test would require less time. Safety, cost, and repeatability are the core reasons to simulate. Simulations still rely on assumptions and are not perfectly accurate. A simulation of disease spread assumes every person has the same number of daily contacts. Which of the following best describes the effect of this assumption? It makes the simulation impossible to run. It is a simplification that may cause the simulation's results to differ from real-world spread. It guarantees the simulation matches real data. It eliminates the need for random values. Simplifying assumptions make simulations feasible but limit accuracy. The exam wants you to name that trade-off. Simulation: a program that models a real-world or hypothetical process as an abstraction Simplifying assumption: a detail intentionally left out or held constant to make a model tractable",
  "ap-csp/3-17.html": "How does the work an algorithm does grow as the input grows? The CED draws one line — polynomial vs. exponential — and everything here follows from it. Efficiency is measured by how the number of steps (or memory) an algorithm needs grows as the input size grows — not by a stopwatch on one computer. An algorithm runs in reasonable time if its steps grow polynomially with input size: constant, linear (n), quadratic (n²), cubic, etc. An algorithm runs in unreasonable time if its steps grow exponentially (2 n ) or factorially (n!). Doubling the input can square or worse the time — these become impossible for even modest inputs. Comparing algorithms: count the operations as a function of n. Linear search is about n steps; binary search about log₂ n; comparing every pair of items is about n². Some problems have no known reasonable-time algorithm. For these, a heuristic — an approach that finds a good-enough, not necessarily optimal, solution quickly — is used instead. Heuristic example: the traveling salesperson problem (shortest route visiting every city). Checking all routes is factorial time; \"always go to the nearest unvisited city\" is a fast heuristic that gives a decent route. Efficiency often involves a trade-off with simplicity or memory. The most efficient algorithm isn't always the best choice for a small, one-time task. Two algorithms check whether a list of n names contains any duplicate. Algorithm A: compare every name to every other name. For n = 10 that's about 45 comparisons; for n = 1,000, about 500,000. Steps grow roughly as n² — polynomial, so reasonable. Algorithm B: try every possible ordering of the list looking for a pattern. For n = 10 that's 3.6 million orderings; for n = 20, over 2 quintillion. Factorial — unreasonable. A is slower than a smarter approach but fine; B is unusable past tiny inputs no matter how fast the computer is. If a question gives a table of input size vs. steps, check the growth: steps doubling when n increases by 1 = exponential = unreasonable. Steps multiplying by 4 when n doubles = n² = reasonable. When the stem says \"no efficient algorithm exists\" or \"the solution doesn't have to be perfect,\" the answer involves a heuristic. The CED defines efficiency by how an algorithm's resource use (steps or memory) grows with input size , and draws one line: polynomial growth (constant, linear, quadratic, cubic…) is reasonable ; exponential (2 n ) or factorial (n!) growth is unreasonable . Why the line is there: doubling the input for a quadratic algorithm quadruples the work — manageable. Doubling the input for an exponential algorithm squares the work. At n = 50, 2 n is a quadrillion. No hardware improvement fixes that. Recognizing growth from a table: steps double when n increases by 1 → exponential. Steps multiply by 4 when n doubles → quadratic. Steps double when n doubles → linear. Steps increase by 1 when n doubles → logarithmic. Reading growth from code: one loop over n items → linear. Nested loops each over n → quadratic. A loop that halves the range each time → logarithmic. Trying every subset or every ordering → exponential or factorial. Heuristics are for problems where the exact solution is unreasonable. A heuristic gives a good-enough answer fast, without guaranteeing the best. The traveling salesperson problem (shortest route through all cities) is the canonical example: exact = factorial time; nearest-neighbor heuristic = fast and usually decent. A problem can have both reasonable and unreasonable algorithms. Sorting by trying every ordering is factorial; sorting with merge sort is n log n. The problem isn't hard; the first algorithm is just bad. The CED explicitly says efficiency is not measured by running time on a specific computer — faster hardware changes the constant, not the growth. An unreasonable algorithm on a supercomputer is still unreasonable. Calling quadratic \"unreasonable.\" It's polynomial, so it's reasonable per the CED. Slow for large n, but reasonable. Thinking a faster computer fixes exponential growth. It shifts the wall by a few units of n. It doesn't remove it. Expecting a heuristic to find the optimal answer. Heuristics trade optimality for speed. If an option says a heuristic \"always finds the best solution,\" it's wrong. Reading a steps table without computing ratios. Look at how steps change as n changes. That ratio is the growth type. The table shows the number of steps an algorithm takes for different input sizes. Input size Steps 10 1,024 11 2,048 12 4,096 Which of the following best describes the algorithm? It runs in reasonable time because the steps increase for each input size. It runs in unreasonable time because the steps double with each additional input, indicating exponential growth. It runs in constant time. It runs in linear time. Steps double each time n increases by 1 — that's 2ⁿ, exponential, unreasonable. A delivery company must plan a route through 50 cities. Checking every possible route would take longer than the age of the universe. Which approach is most appropriate? Use a heuristic that finds a good route quickly, even if it is not guaranteed to be the shortest. Buy a faster computer so that all routes can be checked. Use binary search to find the shortest route. Reduce the problem to one city. When the exact algorithm is unreasonable, a heuristic gives a usable answer in reasonable time. A faster computer doesn't overcome factorial growth. Efficiency: how an algorithm's required steps or memory grow with input size Reasonable time: polynomial growth in steps (n, n², n³ …) as input grows Unreasonable time: exponential or factorial growth in steps as input grows Heuristic: a technique that finds a good-enough solution quickly when an optimal one is impractical",
  "ap-csp/3-18.html": "Some problems can't be solved by any algorithm — not slow, not hard, impossible. The exam expects you to know the distinction and the famous example. A decidable problem is one for which an algorithm exists that gives a correct yes/no answer for every possible input. \"Is this number even?\" is decidable. An undecidable problem is one for which no such algorithm can exist. It's not that we haven't found one — it has been proven that none can exist. Undecidable ≠ unreasonable time. An unreasonable-time problem has an algorithm, just a slow one. An undecidable problem has no algorithm that works for all inputs. The classic example is the halting problem : no general algorithm can determine, for every possible program and input, whether that program will eventually stop or run forever. An algorithm may solve some instances of an undecidable problem (you can tell that DISPLAY(\"hi\") halts), but no single algorithm solves all instances. This is a theoretical limit of computing — a boundary that faster hardware or cleverer programming cannot move. Someone claims to have written willHalt(program, input) that returns true if the program stops and false if it loops forever. Sure, it works on simple cases. But it can be shown that any such procedure must fail on at least one input — a program constructed to do the opposite of whatever willHalt predicts about it. That contradiction is why the halting problem is undecidable. The takeaway for the exam is just the conclusion: no general solution exists. Three categories, don't mix them: (1) reasonable time — has a fast algorithm; (2) unreasonable time — has an algorithm, but too slow, so use a heuristic; (3) undecidable — no algorithm can exist for all cases. If the stem says \"no algorithm can ever\" or mentions determining whether an arbitrary program halts, it's undecidable. A decidable problem is one where an algorithm can be written that gives a correct yes/no answer for every possible input . An undecidable problem is one where no such algorithm can exist — this has been mathematically proven, not just not-yet-found. The halting problem : given any program and any input, will the program eventually stop? Alan Turing proved in 1936 that no algorithm can answer this for all programs. The proof constructs a program that does the opposite of whatever the supposed halting-detector predicts about it, creating a contradiction. Undecidability is not about difficulty or time . An unreasonable-time problem has an algorithm that's too slow. An undecidable problem has no algorithm at all, regardless of time. They're separate categories on the exam. An undecidable problem can still be solved for specific instances . You can tell that DISPLAY(\"hi\") halts and REPEAT UNTIL (false) doesn't. What's impossible is one algorithm that works for every program. Real consequences: no tool can perfectly detect all infinite loops, all bugs, or all malware — because each of those reduces to the halting problem. Tools use heuristics and catch most cases; the impossible part is catching all. This is one of the few places the CED touches theoretical computer science. The exam expects you to know: some problems are provably unsolvable by computers, the halting problem is the example, and this is a fundamental limit rather than a current-technology limit. Saying an undecidable problem \"hasn't been solved yet.\" It's been proven unsolvable. \"Yet\" is wrong. Suggesting a faster computer or heuristic. Those address unreasonable time, not undecidability. No algorithm exists to speed up. Thinking undecidable means no instance can be solved. Specific cases often can. The claim is about a general algorithm for all cases. Which of the following best describes an undecidable problem? A problem that can be solved, but only in unreasonable time A problem for which no algorithm can be constructed that always gives a correct yes/no answer for every input A problem that requires a heuristic A problem that has not yet been solved but eventually will be Undecidable means provably no general algorithm exists — not slow, not unsolved-so-far. A programmer says, \"I can write a program that, given any other program and its input, will always correctly determine whether that program eventually stops.\" Which of the following is true? This is possible with a sufficiently fast computer. This is possible using binary search. This is impossible; determining whether an arbitrary program halts is an undecidable problem. This is possible but would take unreasonable time. This is the halting problem. No algorithm solves it for all programs, regardless of speed. Decidable problem: a problem for which an algorithm exists that correctly answers every input Undecidable problem: a problem for which no algorithm can correctly answer every input Halting problem: the undecidable problem of determining whether an arbitrary program will stop or run forever",
  "ap-csp/unit-4.html": "How the internet moves data, why it keeps working when parts of it fail, and how splitting work across processors speeds things up. Three topics, dense with vocabulary. The internet is a network of networks that moves data in packets using shared protocols, and it's designed so no single failure takes it down. Fault tolerance comes from redundancy — multiple paths and backups. Parallel and distributed computing split work across processors or machines, with a speedup limited by the parts that can't be split.",
  "ap-csp/4-1.html": "A network of networks, connected by agreed-upon rules. The exam wants precise definitions and an understanding of how data actually travels. A computer network is a group of interconnected computing devices that can send and receive data. The internet is a network of networks connected by shared standards — no single organization owns or controls it. Routing is finding a path from sender to receiver. Data can take many different paths; routers pass packets toward their destination. Data is sent in packets : small chunks that each contain the data plus metadata (source and destination addresses, sequence number). Packets from one message may travel different routes and arrive out of order; they're reassembled at the destination using the sequence numbers. Every device has an IP address — a unique numeric identifier used for routing. IPv4 uses 32 bits (about 4 billion addresses, now exhausted); IPv6 uses 128 bits. Protocols are agreed rules that specify how data is formatted, addressed, transmitted, and received. Because they're open standards , any device following them can communicate with any other. Key protocols: IP (addressing and routing packets), TCP (reliable delivery — checks all packets arrive, requests re-sends, reorders), UDP (faster, no delivery guarantee — used for streaming), HTTP/HTTPS (requesting and sending web pages; HTTPS adds encryption). DNS (Domain Name System) translates human-readable names like example.com into IP addresses. Bandwidth is the maximum data-transfer rate of a connection, measured in bits per second. Higher bandwidth = more data per second. The World Wide Web is a system of linked pages and resources that runs on the internet. They aren't the same thing: the internet is the network; the web is one service using it. The internet's scalability — its ability to keep working as it grows — comes from its layered, standardized, decentralized design. You type example.com and press Enter. Your browser asks a DNS server for that name's IP address . It then sends an HTTPS request. That request is split into packets , each stamped with your IP, the server's IP, and a sequence number. Routers forward each packet along whatever path is available — packet 3 might go through Denver while packet 4 goes through Dallas. The server's TCP layer collects them, puts them in order, and asks for any that are missing. Then it sends the page back the same way. Two distinctions the exam loves: internet vs. World Wide Web (network vs. a service on it), and TCP vs. UDP (reliable/ordered vs. fast/unguaranteed). And if an option says packets must travel the same route or arrive in order, it's wrong — they don't, and reassembly handles it. The internet is decentralized : no single authority controls it. It's a network of independently-operated networks that agree to talk using common protocols. That's why it can't easily be \"turned off\" and why it scales — anyone can add a network. Packets carry a chunk of data plus metadata : source address, destination address, and a sequence number. The sequence number is what lets the receiver reassemble packets that arrive out of order. If a packet is missing, TCP asks for it again. Routing happens hop by hop. Each router looks at a packet's destination and forwards it toward the next router closer to that destination. No router knows the whole path; each knows the next step. Different packets of the same message can take different paths. IP handles addressing and routing — getting packets to the right machine. TCP sits on top and handles reliability — making sure they all arrive, in order, and asking for re-sends. UDP is TCP's faster, unreliable sibling used when speed matters more than perfection (live video, games). HTTP is the protocol web browsers and servers use to request and send pages. HTTPS is HTTP with encryption (via TLS), so intercepted traffic can't be read. The padlock icon means HTTPS. DNS is the internet's phone book: it maps names like collegeboard.org to IP addresses. Without DNS you'd type numbers. DNS itself is a distributed, hierarchical system — another example of scalability. IPv4 addresses are 32 bits (four numbers 0–255, like 192.168.1.1) — about 4.3 billion, which ran out. IPv6 uses 128 bits — enough for every device imaginable. The transition is ongoing. Bandwidth is capacity (bits per second); latency is delay (how long one packet takes to arrive). A satellite link can have high bandwidth and high latency. The CED focuses on bandwidth. The World Wide Web is one application built on the internet, alongside email, file transfer, streaming, and games. The web = HTTP + HTML + browsers. The internet = the underlying network. Different things. Open standards (the protocols are public, not owned) are why the internet works across every manufacturer and country. Anyone can implement TCP/IP; nobody needs permission. Using internet and web interchangeably. The exam tests the distinction. Web is a service; internet is the network. Claiming packets take the same path or arrive in order. Neither is guaranteed. Sequence numbers and reassembly handle it. Assigning the wrong job to a protocol. IP = addressing/routing. TCP = reliable delivery. HTTP = web pages. DNS = names to addresses. Learn the four. Thinking a router knows the whole route. It knows the next hop. That's what makes rerouting around failures automatic. Which of the following best explains how a large file is transmitted over the internet? The file is sent as one continuous stream along a single fixed path. The file is divided into packets that may travel different routes and are reassembled in order at the destination. The file is converted to a single IP address and sent to the router. The file is compressed into one packet to guarantee delivery. Packet switching: split into packets, independently routed, reassembled using sequence data. Which of the following best describes the relationship between the internet and the World Wide Web? They are two names for the same thing. The internet is a network of networks; the World Wide Web is a system of linked resources that uses the internet. The World Wide Web is the physical hardware; the internet is the software. The internet is a subset of the World Wide Web. The web is one of many services (with email, streaming, etc.) that run over the internet. Why are open protocols important to the functioning of the internet? They allow devices made by different manufacturers to communicate using shared rules. They encrypt all data automatically. They guarantee that no packets are ever lost. They assign every user the same IP address. Interoperability through shared, public standards is the point of open protocols. Computer network: interconnected devices that can send and receive data Internet: a global network of networks using open protocols Packet: a small unit of data with metadata for routing and reassembly Router: a device that forwards packets toward their destination IP address: a unique numeric address for a device on a network Protocol: an agreed set of rules for formatting and transmitting data TCP: a protocol that ensures reliable, ordered delivery of packets DNS: the system that translates domain names into IP addresses Bandwidth: the maximum rate of data transfer, in bits per second World Wide Web: a system of linked resources accessed over the internet via HTTP",
  "ap-csp/4-2.html": "The internet keeps working when parts of it break. The reason is redundancy, and the exam wants you to explain it in those terms. Fault tolerance is a system's ability to continue functioning correctly when some components fail. The internet is fault tolerant because of redundancy : there are many possible paths between any two points. If a router or cable fails, packets are re-routed through another path. Redundant routing means no single point of failure — the internet doesn't depend on any one connection. Redundancy has a cost: extra hardware and connections. The trade-off is reliability versus expense. The scalability of the internet is tied to this design: adding new networks adds more paths, increasing rather than decreasing resilience. Fault tolerance applies beyond networks — backup servers, mirrored data, and duplicated power supplies are the same idea. A network is more fault tolerant when the removal of any one node still leaves every other node connected. Exam questions often show a diagram and ask which connection's failure would disconnect part of the network. Picture five routers A–E where A connects to B and C, B connects to D, C connects to D, and D connects to E. If the A–B link fails, traffic from A to E still flows A→C→D→E. The network is fault tolerant for that failure. But D–E is the only link to E: if it fails, E is cut off entirely. D–E is a single point of failure; adding a C–E link would fix it. That's exactly the kind of diagram question the exam uses. For network-diagram questions, count paths. Ask: for each connection, if I delete it, can every device still reach every other? A connection whose removal isolates a device is the vulnerability. The answer to \"how do we improve fault tolerance\" is always \"add another path.\" Fault tolerance means a system keeps working correctly — not just stays on — when parts fail. The internet achieves it through redundancy : many paths between any two points, so losing one path doesn't cut the connection. The formal idea: a network is fault tolerant with respect to a component if removing that component still leaves every remaining node reachable from every other. The exam's diagram questions ask exactly this: which single link's removal would disconnect the network? Single point of failure : a component whose failure alone breaks the system. In a network diagram, a node connected by only one link is a single point of failure for itself. Adding a second link fixes it. The design goal is no single points of failure. Redundancy costs money — extra cables, routers, servers. The trade-off is reliability vs. expense. Critical systems (hospitals, financial networks) pay for more redundancy than a home network. Redundancy applies to data too: backups, mirrored drives, and data replicated across data centers are fault tolerance for storage. If one copy is lost, another exists. The internet's fault tolerance is a consequence of its design history: it was built to survive partial destruction. Packets automatically route around dead nodes because each router only needs to know the next hop, not the whole path. Scalability and fault tolerance reinforce each other: adding networks adds paths, which adds redundancy. A bigger internet is a more resilient internet. Confusing redundancy with sending duplicate packets. Redundancy is extra paths (and backup components), not repeated transmissions. TCP re-sends packets that are lost, but that's error recovery, not redundancy. Not checking every link in a diagram. The question is usually \"which link's failure disconnects the network?\" Test each one by mentally removing it. Assuming a bigger network is more fragile. The opposite — more nodes means more alternative paths. Which of the following best explains why the internet is considered fault tolerant? Every packet is sent multiple times to guarantee delivery. There are multiple paths between devices, so data can be rerouted if a connection fails. All data is stored in a single central location. Routers never fail. Redundant paths allow rerouting around failures. That's the definition of the internet's fault tolerance. A small network has four devices. Device P connects only to device Q. Devices Q, R, and S are each connected to the other two. Which of the following is true? The network has no single point of failure. If the connection between Q and R fails, S can no longer reach P. If the connection between P and Q fails, P is disconnected from the rest of the network. The network is fully fault tolerant. P has exactly one connection. Losing it isolates P. Q, R, and S have redundant links to each other. Fault tolerance: the ability of a system to keep working when components fail Redundancy: having multiple copies or paths so a single failure isn't fatal Single point of failure: a component whose failure would bring down the system",
  "ap-csp/4-3.html": "Splitting a task across processors or machines. The exam's signature question here is computing the speedup — and understanding why it's never as much as you'd hope. Sequential computing executes one operation at a time, in order. Total time = sum of all operations. Parallel computing breaks a task into parts that run simultaneously on multiple processors (cores) in one computer. Distributed computing uses multiple separate computers, connected by a network, working together on one problem — often for tasks too large for a single machine. Parallel solutions are generally faster than sequential ones, but only if the task can be divided . Parts that depend on each other's results must still run in order. Speedup = sequential time ÷ parallel time. The exam gives you operation times and asks for the minimum time on a given number of processors. Speedup is limited by the sequential portion : if 20% of a task can't be parallelized, no number of processors gets you more than a 5× speedup. There's also overhead — the cost of coordinating between processors — so real speedup is always less than the ideal. Distributed computing's advantages: scales beyond one machine's limits, and can be more fault tolerant (one machine failing doesn't stop the whole job). A program has three independent tasks taking 40, 30, and 20 seconds, plus a final step of 10 seconds that needs all three results. Sequential: 40 + 30 + 20 + 10 = 100 seconds . Two processors: put the 40 on one processor, the 30 and 20 (= 50) on the other. The parallel phase takes 50 seconds (the longer of the two). Then the 10-second step. Total: 60 seconds . Three processors: 40, 30, 20 each on its own → 40 seconds (the max), plus 10 → 50 seconds . Speedup with three processors = 100 ÷ 50 = 2×, not 3×, because of the 10-second sequential step and the uneven task sizes. For the timing calculation: (1) list tasks that must be sequential — they add up; (2) for parallel tasks, distribute them to balance the processors and take the longest processor's total; (3) add the two. The answer is never the average of parallel tasks — it's the maximum. Sequential : one processor, one operation at a time. Time = sum of all operations. Parallel : multiple processors in one machine, operations that don't depend on each other run at the same time. Distributed : multiple machines over a network, each handling part of the problem. The speedup calculation the exam uses: find the operations that must run sequentially (they add up), find the operations that can run in parallel, distribute those to processors as evenly as possible, and the parallel phase takes as long as the busiest processor. Total time = sequential + max(parallel). Speedup = sequential time ÷ parallel time. Why speedup is limited: if 10% of a task must be sequential, then even with infinite processors, that 10% takes its full time. Maximum speedup is 10×. This is Amdahl's law in spirit; the CED just says the sequential portion limits speedup. Overhead : splitting work, coordinating, and combining results all take time. Two processors don't quite halve the time even for a perfectly divisible task. A task is parallelizable when its parts are independent — summing a list (each half can be summed separately then combined) is; a chain where each step needs the previous step's result (computing a running total step by step) is not. Distributed computing adds scalability (add more machines) and fault tolerance (one machine dying doesn't stop the job), at the cost of network communication overhead. It's how large-scale data processing (2.3) is actually done. Parallel programs must also handle synchronization — making sure results are combined correctly. The CED doesn't go deep here, but it's part of why parallel programming is harder than sequential. Averaging parallel tasks instead of taking the max. If two processors run tasks of 40s and 20s, the parallel phase takes 40s, not 30s. The slower processor sets the pace. Forgetting the sequential part. Any step that needs all the parallel results must wait for them and then adds its own time. Claiming n processors give n× speedup. Only for a perfectly parallel task with no overhead — which doesn't exist. Real speedup is always less. Mixing up parallel and distributed. Multiple cores = parallel. Multiple computers = distributed. A program has four independent tasks that take 8, 6, 4, and 2 seconds. Using two processors that can each run one task at a time, what is the minimum time to complete all tasks? 8 seconds 10 seconds 12 seconds 20 seconds Sequential would be 20. Balance: processor 1 runs 8 + 2 = 10, processor 2 runs 6 + 4 = 10. Minimum time is 10 seconds. Which of the following best describes distributed computing? Running a single task on one processor as fast as possible Using multiple processors within one computer to run parts of a task simultaneously Using multiple computers connected by a network to work together on a problem Compressing a program so it uses less memory Multiple separate machines over a network = distributed. Multiple cores in one machine = parallel. A task consists of a 30-second step that must run first, followed by parts that can run in parallel. Even with an unlimited number of processors, the task cannot complete in less than 30 seconds. Which of the following best explains why? Parallel computing always takes longer than sequential computing. The sequential portion of a task limits the total speedup that parallelization can achieve. Processors cannot run at the same time. The task has a run-time error. The non-parallelizable part sets a floor on total time. This is the core limit of parallel speedup. Sequential computing: operations executed one after another on a single processor Parallel computing: splitting a task across multiple processors running at the same time Distributed computing: multiple networked computers cooperating on one problem Speedup: sequential time divided by parallel time",
  "ap-csp/unit-5.html": "The second-largest Big Idea, and mostly reading comprehension plus vocabulary. Every innovation has effects — good, bad, intended, unintended — and the exam rewards answers that see more than one side. Computing innovations have beneficial and harmful effects, often at the same time, and effects at scale weren't always intended. Access to computing is unequal, and that inequality (the digital divide) matters more as more of life moves online. Bias enters computing systems through data and design, and it can be identified and reduced. Crowdsourcing and citizen science let large groups solve problems and gather data. Computing raises legal and ethical questions about intellectual property, privacy, and access to information. Personal data can be collected, combined, and exploited; security practices protect it.",
  "ap-csp/5-1.html": "The framing question for the whole Big Idea: who does an innovation help, who does it hurt, and did anyone plan for that? A computing innovation includes a program as an integral part of its function — a physical device, a system, or an app. Its effect is the impact it has on society, economy, or culture. Effects can be beneficial or harmful , and the same innovation is often both — for different people or in different contexts. Intended effects are what the innovation was designed to do. Unintended effects are consequences nobody planned — and these can be positive or negative. Innovations designed for one purpose get used in ways their creators never imagined, and sometimes those uses become the main thing (the web was built for sharing physics papers). Effects at scale differ from effects on one person: a feature that's harmless for one user can reshape behavior when a billion people have it. Responsible developers consider potential harmful effects during development, not after. It's impossible to predict everything, which is why monitoring effects after release matters. Advances in computing have generated new careers and ended others, changed how people communicate and access information, and altered privacy expectations. Ride-sharing apps. Intended: easier, cheaper rides. Beneficial: more transportation options, flexible income for drivers. Harmful: reduced income for taxi drivers, congestion, precarious work with no benefits. Unintended: changes in city parking demand and public transit ridership. A strong exam answer names at least one effect on each side and notes that the harms fall on different groups than the benefits. Options that are absolute — \"only beneficial,\" \"has no negative effects,\" \"all users are affected the same way\" — are almost always wrong. The correct option acknowledges that effects vary by group and that unintended consequences exist. A computing innovation , per the CED, \"includes a program as an integral part of its function.\" That covers apps, self-driving cars, medical devices, recommendation systems. A hammer isn't one; a smart thermostat is. Effects are the innovation's impact on society, economy, or culture. The exam wants you to categorize effects along two axes: beneficial vs. harmful and intended vs. unintended . All four combinations exist. An unintended benefit is just as real as an unintended harm. The classic examples: the internet was designed for research communication; its effect on commerce, entertainment, and politics was unintended. Social media was designed to connect friends; its effect on attention, mental health, and misinformation was unintended and, for many, harmful. Scale changes effects. A feature used by 100 people has small effects. The same feature used by a billion people changes how society works. Developers often can't foresee scale effects because they build for the small case. The CED says people create innovations \"for a purpose\" but they can be used \"in ways that are unintended\" — and it's not only bad actors. Ordinary users find uses developers didn't imagine. Sometimes those become the main use (SMS was an afterthought; it became the killer feature). Responsible development means thinking about potential harmful effects during design, not only after harm occurs. It's impossible to predict everything, so responsible developers also monitor effects after release and respond to them. Computing has changed work : new jobs (app developer, data scientist) exist that didn't; some jobs (travel agent, switchboard operator) have largely disappeared. The exam treats this as a mixed effect — beneficial for some, harmful for others, both true. Computing has changed access to information (nearly unlimited, but also unfiltered), communication (instant, global, but also surveilled), and privacy expectations (what was private is now routinely collected). Every one of these is a two-sided effect. The Explore Task (a former exam component whose vocabulary persists in the CED) asked students to analyze a computing innovation's purpose, function, effects, and data use. Questions on this topic often mirror that structure. Picking the one-sided option. \"This innovation is only beneficial\" or \"has no harmful effects\" is essentially never correct. The CED's whole point is that effects are mixed. Confusing unintended with harmful. Unintended effects can be good. The two axes are independent. Assuming the developer is responsible for every use. The CED presents this as a tension, not a settled rule. Options that assign all responsibility to one party are usually distractors. Ignoring who is affected. A strong answer says which group benefits and which group is harmed. \"Society benefits\" is too vague when the harm falls on a specific group. A social media platform introduces a feature that shows users posts their friends have liked. Which of the following is most likely an unintended harmful effect? Users see content from friends. Users spend more time on the platform than they intended, affecting sleep and productivity. The platform's servers store more data. Users can like posts. The feature was intended to increase engagement; harming sleep and productivity is an unintended consequence at scale. Which of the following statements about the effects of a computing innovation is most accurate? An innovation is either beneficial or harmful, never both. The same innovation can be beneficial to some groups and harmful to others. Developers can predict all effects of an innovation before release. Unintended effects are always harmful. This is the CED's core framing. Effects are mixed and depend on who is affected. Computing innovation: a device, system, or app that uses a program as an essential part of how it works Intended effect: an impact the innovation was designed to produce Unintended effect: a consequence not planned by the creators, which can be positive or negative",
  "ap-csp/5-2.html": "Not everyone has the same access to computing. The exam wants the causes, the consequences, and the recognition that it happens both between and within countries. The digital divide is the gap between people who have reliable access to computing devices and the internet and people who don't. Contributing factors: socioeconomic status (cost of devices and service), geographic location (rural areas without infrastructure), demographics (age, disability, education), and government policy (censorship, investment). The divide exists between countries and within a single country — including within a single city. Consequences compound: as school, jobs, healthcare, banking, and government services move online, lacking access means being excluded from essential parts of society. Access isn't only devices and bandwidth — it includes digital literacy (knowing how to use technology) and the availability of content in one's language. Actions that reduce the divide: public infrastructure investment, low-cost devices, community access points (libraries), and designing software to work on older devices and slow connections. Developers contribute to the divide when they assume all users have fast connections and new hardware. During remote schooling, two students in the same district get the same assignments. One has home broadband and a laptop; the other shares a phone with two siblings and uses a fast-food parking lot's Wi-Fi. Same curriculum, wildly different outcomes — that's the digital divide operating within one town. A school that lends laptops and pays for hotspots is acting to narrow it. The digital divide is about access , not attitude. Options describing people who \"choose\" not to use technology are distractors. And it's not only an international issue — a question set entirely within the US can still be about the digital divide. The digital divide is unequal access to computing and the internet. The CED names causes: socioeconomic status , geographic location , demographic characteristics (age, disability, education), and government or institutional policy (censorship, lack of investment). It exists at every scale: between countries (broadband penetration varies enormously), within countries (rural vs. urban), within cities (neighborhood by neighborhood), and within households (who gets the one device). The divide is about more than hardware: digital literacy (knowing how to use technology), affordability of ongoing service (not just a device), reliability of connection, language (most content is in a few languages), and accessibility for people with disabilities are all dimensions. Why it matters more over time: as essential services move online — school, job applications, healthcare portals, government benefits, banking — lacking access stops being an inconvenience and becomes exclusion from society's basic functions. The divide compounds other inequalities. Developers contribute to the divide when they assume their users are like them : fast connections, new devices, large screens, fluent English. A site that requires 50 MB of JavaScript excludes people on slow connections. This connects to 5.3 (bias) and to 1.3 (investigating users during design). Actions that reduce the divide: public infrastructure investment, subsidized service, community access points (libraries, schools), low-cost devices, and — for developers — building things that work on old devices and slow connections, in multiple languages, with accessibility features. The digital divide can also refer to generational differences: older people may have less familiarity even with access. And it can be policy-driven : some governments restrict internet access deliberately. Treating it as only an international issue. Questions set entirely in one country — even one city — can be about the digital divide. Treating it as a matter of choice. Someone who prefers paper isn't part of the digital divide. Someone who can't afford broadband is. Assuming a device solves it. A laptop with no internet, or internet with no skills to use it, doesn't close the gap. It's multi-dimensional. Picking options that widen the gap. \"Require the latest phone,\" \"raise prices,\" \"use high-resolution video only\" — these are wrong answers to \"how would you reduce the divide.\" Which of the following is an example of the digital divide? Two students use different brands of laptop. Students in a rural area lack broadband access and cannot complete online homework, while students in a nearby city can. A student prefers paper books to e-books. A website is available in multiple languages. Unequal access based on geography that affects opportunity is the definition of the digital divide. Which of the following actions would most directly help reduce the digital divide? Requiring all websites to use high-resolution video Providing free public internet access and affordable devices in underserved communities Increasing the price of internet service Designing apps that only run on the newest phones Reducing cost and infrastructure barriers directly expands access. The other options widen the gap. Digital divide: the gap between those with reliable access to computing and the internet and those without Digital literacy: the skills needed to use computing technology effectively",
  "ap-csp/5-3.html": "Programs reflect the data they're built on and the assumptions of the people who built them. Bias is often unintentional, which is exactly why it needs to be looked for. Computing bias occurs when a computing innovation systematically produces unfair or skewed outcomes for certain groups. Bias can enter through the data : if training data underrepresents a group, the system performs worse for that group (e.g., facial recognition trained mostly on one demographic). Bias can enter through design choices : which features are included, what the defaults are, what's considered \"normal\" — reflecting the assumptions of the developers. Bias can be intentional or unintentional . Most exam scenarios are unintentional — a well-meaning team that didn't test with diverse users. Bias may exist at every level of development, from problem selection to data collection to testing. Programmers should take action to reduce bias : audit data for representativeness, test with diverse users, involve diverse teams (this is why 1.1 emphasizes diverse collaboration), and examine outcomes by group. Bias in a system used at scale — hiring, lending, policing — can amplify existing inequality dramatically. A company builds a résumé-screening tool by training it on a decade of past hiring decisions. Those past decisions favored one gender for engineering roles. The tool learns the pattern and down-ranks résumés with signals associated with the other gender. Nobody at the company intended that — the bias was in the historical data. Detecting it required checking the tool's outcomes by group, and fixing it required different data, not just different code. Look for the source. If the scenario mentions training data, the bias came from the data. If it mentions who was on the team or what they assumed, it's design. Options claiming bias can be eliminated by \"using more advanced algorithms\" alone are wrong — you have to address the data and the assumptions. Computing bias is when a system produces systematically unfair results for certain groups. The CED's key claim: bias in a computing innovation reflects the biases in its data , its design , and its developers — and it can be unintentional. Bias from data: systems trained on historical data learn historical patterns, including unfair ones. A hiring tool trained on past hires learns past discrimination. A facial recognition system trained mostly on one demographic works worse on others. The data doesn't have to be labeled \"biased\" — it just has to be unrepresentative. Bias from design: defaults, categories, and assumptions encode the designers' worldview. A form with only two gender options, a voice assistant tested only with one accent, a health app that assumes a particular body type — each excludes people the designers didn't picture. Bias from developers: teams that lack diversity have blind spots. This is why 1.1 emphasizes diverse collaboration — it's a direct countermeasure to bias. Bias can exist at every level : which problem to solve (whose needs matter?), what data to collect, how to clean it, what to test, who to test with, and how to interpret results. It's not one step you can check off. Amplification at scale: a biased human decision affects one person. A biased algorithm used for millions of decisions affects millions — consistently and invisibly. That's why algorithmic bias is treated as more dangerous than individual bias. Reducing bias is an active responsibility: audit data for representation, test outcomes across groups, involve affected communities, build diverse teams, and be willing to not ship something that can't be made fair. The CED says programmers \"should take action to reduce bias.\" Bias is often invisible from inside : developers who share the blind spot don't see the problem. External testing and diverse perspectives are how it gets found. Thinking bias requires intent. Most computing bias is unintentional. \"The developers didn't mean to\" doesn't make a system unbiased. Believing a better algorithm alone fixes biased data. If the data is unrepresentative, the algorithm learns from unrepresentative data. The data must be fixed. Assuming computers are objective. \"An algorithm can't be biased because it's math\" is a common belief and a wrong answer. Algorithms encode human choices. Missing the connection to collaboration. \"Why would a diverse team reduce bias?\" — because they catch each other's blind spots. This links 5.3 back to 1.1. A voice assistant was tested only with speakers of one regional accent and performs poorly for users with other accents. Which of the following best describes this situation? A syntax error in the program Computing bias resulting from unrepresentative data and testing An example of the digital divide Lossy compression of audio Underrepresentation in data/testing leading to worse performance for a group is computing bias. Which of the following is the most effective way for a development team to reduce bias in a program that recommends loan approvals? Use a larger number of processors. Examine the training data for underrepresented groups and test outcomes across demographic groups before deployment. Remove all comments from the code. Compress the data before analysis. Auditing data and testing outcomes by group directly targets the sources of bias. Computing bias: systematic unfairness in a computing system's outcomes toward certain groups Training data: the data used to build a system's behavior; unrepresentative data produces biased results",
  "ap-csp/5-4.html": "Using the internet to gather help, data, or money from a large, distributed group. Short topic, reliable points. Crowdsourcing obtains input, ideas, services, or funding from a large group of people, typically online. Citizen science is crowdsourced scientific research: members of the public collect or classify data (bird counts, galaxy classification) that researchers couldn't gather alone. The internet makes crowdsourcing possible by connecting large numbers of people cheaply and letting them contribute in small pieces. Benefits: scale (thousands of contributors), speed , diversity of input, and lower cost. Some problems are only solvable this way. Examples: collaborative encyclopedias, funding platforms, mapping projects, distributed volunteer computing, product reviews, and bug-finding contests. Limitations: contributions can be inconsistent in quality, contributors may share biases, and results need verification. Crowdsourcing also enables new business and creative models that didn't exist before widespread connectivity. Astronomers have millions of telescope images of galaxies and need each classified by shape. A program can't do it reliably, and one lab would take decades. They post the images online with a simple tutorial; hundreds of thousands of volunteers classify them, each image getting several independent classifications that are compared for agreement. That's citizen science: the crowd provides the labor, the design provides the quality control. If a scenario involves many members of the public contributing data or effort over the internet toward a shared goal, the answer is crowdsourcing (or citizen science if it's research). Don't confuse it with distributed computing — that's machines sharing work, not people. Crowdsourcing uses the internet to gather contributions — data, labor, ideas, funding — from a large, distributed group. The CED's point is that the internet makes this possible at a scale and speed that didn't exist before. Citizen science is crowdsourcing for research: the public collects or classifies data. Examples: counting birds, classifying galaxy images, recording rainfall, identifying protein structures in a game. The volume of data and geographic coverage would be impossible for professional researchers alone. Crowdsourcing enables new business models : crowdfunding (many small contributions fund a project), gig platforms (work distributed to many independent people), and collaborative products (an encyclopedia written by volunteers). Quality control is the challenge: contributors vary in skill and care. Solutions include having multiple people do the same task and comparing, reputation systems, expert review of samples, and designing tasks to be simple enough to do reliably. Crowdsourcing inherits contributors' biases : if the crowd is mostly one demographic, the data reflects that demographic. This connects to 5.3. Crowdsourcing is distinct from distributed computing (4.3): crowdsourcing is many people contributing; distributed computing is many machines . Volunteer computing projects that run on people's home computers blur the line but are technically distributed computing with crowdsourced hardware. The CED links crowdsourcing to the broader theme of the internet enabling collaboration among people who never meet — the same idea as 1.1, at planetary scale. Confusing crowdsourcing with distributed computing. People vs. machines. If humans are contributing effort or data, it's crowdsourcing. Assuming crowdsourced data is accurate. It needs verification. Options claiming crowdsourcing guarantees accuracy are wrong. Missing citizen science. Any scenario where the public helps with scientific data collection is citizen science, even if the word \"crowdsourcing\" doesn't appear. A research group asks the public to photograph and upload pictures of local insects, which the researchers use to track species populations. This is best described as an example of which of the following? Citizen science Distributed computing The digital divide Data compression Public participation in gathering scientific data is citizen science, a form of crowdsourcing. Which of the following is a benefit of crowdsourcing that would be difficult to achieve otherwise? Guaranteed accuracy of every contribution Collecting data or ideas from a very large and diverse group of people quickly and at low cost Elimination of all bias Faster processor speeds Scale and diversity at low cost is the distinctive benefit. Accuracy and bias still need managing. Crowdsourcing: gathering input, work, or funding from a large group of people, usually online Citizen science: scientific research that relies on public volunteers to collect or analyze data",
  "ap-csp/5-5.html": "Who owns code and content, what you're allowed to do with it, and where computing raises ethical questions that don't have settled answers. Intellectual property (IP) is work or an invention that results from creativity and to which someone has rights — including software, images, music, and text. Copyright protects creative works; using someone's material without permission is a violation. Software, artwork, and writing found online are copyrighted by default even without a notice. Creative Commons licenses let creators grant specific permissions in advance (e.g., free to use with attribution, or free for non-commercial use), so others can reuse work without asking. Open source software makes its source code freely available to use, study, modify, and distribute, under a license that sets the terms. Open access refers to freely available research and publications. The Create Task requires you to cite any code you didn't write yourself. Using others' code is fine; failing to credit it is not. Ethical concerns raised by computing: privacy and surveillance, algorithmic decision-making in high-stakes areas, the spread of misinformation , and access to technology that can be used for harm as well as good. Legal concerns include plagiarism , piracy , hacking and unauthorized access, and misuse of personal data. Laws vary by country and lag behind technology. Computing innovations can be used in ways their creators find objectionable — the exam asks whether the developer, the user, or society bears responsibility, and expects nuance. You find a JavaScript animation library on the web with no license file. You can look at it, but copying it into your project is a copyright question — no license means all rights reserved. Another library uses an open-source license that permits reuse with attribution. You use that one and credit it in your code comments. On the Create Task, the credited library is fine; uncredited copied code is treated as plagiarism. \"No license\" does not mean \"free to use\" — it means default copyright. Open source and Creative Commons both grant permissions; they don't remove copyright. On ethics questions, the correct answer usually recognizes competing interests rather than declaring one side simply right. Intellectual property covers creative and inventive work: software, music, images, writing, inventions. The creator has legal rights to control its use. On the internet, IP is easy to copy and hard to protect, which creates constant legal and ethical tension. Copyright is automatic — a work is copyrighted the moment it's created, with or without a notice. Using copyrighted material without permission is infringement. \"I found it online\" is not permission. \"It didn't have a copyright symbol\" is not permission. Creative Commons licenses let creators pre-grant specific permissions: attribution required, non-commercial only, share-alike (derivatives must use the same license), no derivatives. Each license is a different bundle. CC doesn't mean \"free for anything\" — read which one. Open source software makes its source code available under a license that permits use, modification, and redistribution — often with conditions (attribution, or that derivatives also be open). It's still copyrighted; the license grants rights that copyright would otherwise reserve. Open access is the research-publication equivalent: papers and data freely available rather than behind paywalls. It's a movement about who gets to read science. The Create Task requires you to cite code you didn't write — in comments, with the source. Using others' code is allowed; presenting it as your own is plagiarism and can invalidate your score. Ethical concerns the CED names: computing innovations can be used in ways their creators find objectionable (a face-recognition tool used for surveillance), and computing raises questions about privacy , ownership of data , misinformation , and who bears responsibility for harm. Legal concerns : laws differ by country and change slowly. Something legal in one jurisdiction may not be in another. And laws lag technology — a new capability may be legal simply because no law addresses it yet, which doesn't make it ethical. The exam distinguishes legal from ethical : something can be legal and unethical (collecting data users don't understand they're giving), or illegal and arguably ethical (civil disobedience). Questions may ask you to identify which kind of concern a scenario raises. Treating \"no license\" as \"free to use.\" It means all rights reserved. Default copyright is the strictest state. Assuming Creative Commons or open source means no rules. Both are licenses with conditions. Attribution is almost always required. Not citing code on the Create Task. Uncited code from any source — a tutorial, a classmate, an AI — is plagiarism under College Board policy. Conflating legal with ethical. The exam treats them as separate questions. Legal ≠ right. A student finds a photograph online with a Creative Commons license that permits reuse with attribution. Which of the following is an appropriate use of the photo? Using the photo without credit, since it is Creative Commons Using the photo in a project and crediting the photographer as the license requires Claiming the photo as the student's own work Selling the photo under the student's name Creative Commons licenses grant specific permissions with conditions; attribution is the condition here. Which of the following best describes open source software? Software that has no copyright Software whose source code is freely available to use, modify, and share under the terms of its license Software that is free to download but whose code is hidden Software created by the government Open source = available source code under a license. It is still copyrighted; the license grants rights. Intellectual property: creative work or inventions to which a person or organization holds rights Copyright: legal protection of a creative work that restricts copying and reuse Creative Commons: licenses that let creators pre-authorize specific kinds of reuse Open source: software whose source code is freely available under a license permitting use and modification Plagiarism: presenting someone else's work as your own",
  "ap-csp/5-6.html": "The longest topic in the Big Idea and a frequent source of questions: what data is at risk, how it gets stolen, and how it's protected. Personally identifiable information (PII) is information that can be used to identify a person: name, address, Social Security number, date of birth, biometric data, medical and financial records, and more. PII is collected through search history, location, cookies, and app activity . Combining seemingly harmless pieces can identify someone. Once online, information is hard to remove. PII enables useful personalization but can be used for stalking, identity theft, and targeted manipulation . It is often collected and sold without users' full awareness. Authentication proves identity. Strong passwords are long and not reused. Multifactor authentication requires two or more of: something you know (password), something you have (phone, key), something you are (fingerprint). Encryption scrambles data so it's unreadable without a key. Symmetric encryption uses one shared key for both encrypting and decrypting. Public key (asymmetric) encryption uses a public key to encrypt and a private key to decrypt — anyone can send you a secret message, only you can read it. Certificate authorities issue digital certificates that verify a website's public key really belongs to that site. This is what makes HTTPS trustworthy. Attacks to know: malware (software designed to damage or gain unauthorized access), virus (malware that attaches to other programs and spreads), phishing (fake messages that trick people into revealing information), keylogging (recording keystrokes), rogue access points (fake Wi-Fi that intercepts traffic). Data traveling over an unsecured network can be intercepted; encryption (like HTTPS) protects it in transit. Software updates patch known security vulnerabilities — running old versions leaves known holes open. Users should read what data an app collects, limit permissions, and recognize that free services are often paid for with personal data. Public key encryption in one paragraph: Alice publishes her public key anywhere. Bob uses it to encrypt a message. Only Alice's private key — which she never shares — can decrypt it. Even if someone intercepts the message and has Alice's public key, they can't read it, because the public key only locks . That asymmetry is what lets strangers communicate securely over an open network without first meeting to share a password. Multifactor in one line: password (know) + code texted to your phone (have) = two factors. Two passwords is still one factor. Vocabulary questions dominate here. Phishing is the person being tricked; keylogging is the keystrokes being recorded; a virus spreads by attaching to programs; a rogue access point is a fake network. For encryption: symmetric = one shared key; public key = a public lock and a private unlock. And multifactor means different categories , not just multiple steps. PII — personally identifiable information — includes name, address, SSN, date of birth, phone, email, biometrics, medical and financial records, and anything that in combination can identify a person. The CED emphasizes combination : ZIP code + birthdate + gender identifies most Americans uniquely. PII is collected through search history , location data , cookies (small files sites store in your browser to recognize you), app permissions , purchase records, and social media activity. Much of it is collected automatically and combined by data brokers. The CED's concern: PII can be used to enhance the user experience (personalization) but also to stalk , steal identities , and manipulate (targeted ads, targeted misinformation). Once information is public, it's essentially impossible to remove. Authentication verifies identity. Three factor types: something you know (password, PIN), something you have (phone, hardware key), something you are (fingerprint, face). Multifactor authentication uses two or more different types. Two passwords = one factor. Strong passwords are long, not reused across sites, and not based on personal information. Length matters more than complexity. Password managers exist because humans can't remember dozens of unique long passwords. Encryption transforms data so it's unreadable without a key. Symmetric : one shared key encrypts and decrypts — fast, but you have to get the key to the other party securely. Public key (asymmetric) : a public key (shared freely) encrypts; only the matching private key decrypts. Solves the key-exchange problem. Public key in practice: your browser gets a website's public key, encrypts a message only the site can read, and they use that to set up a fast symmetric session. That's HTTPS. Certificate authorities vouch that the public key really belongs to that site, so an attacker can't substitute their own. Attacks to distinguish: malware is the umbrella (software intended to harm). A virus is malware that attaches to other programs and spreads when they run. Phishing tricks the user into giving up information via fake messages or sites. Keylogging records keystrokes to capture passwords. A rogue access point is a fake Wi-Fi network that intercepts everything you send. Unencrypted traffic on any network can be read by anyone on that network. Software updates matter because they patch known vulnerabilities. An unpatched system has holes that attackers know about. Delaying updates is a security decision. Free services are typically paid for with your data. Reading what an app collects, limiting permissions, and being skeptical of unexpected requests for information are the user-side defenses the CED expects you to know. Security is a cost-benefit question, not absolute. No system is perfectly secure; the goal is to make attacks expensive enough that they're not worth attempting for the value at stake. Calling two passwords multifactor. Same factor type. Multifactor needs different types: know + have, know + are, etc. Sharing the private key. In public key encryption, the private key is never shared. The public key is the one you give out. Mislabeling attacks. Fake email asking for your password = phishing. Software recording keystrokes = keylogging. Fake Wi-Fi = rogue access point. Each has one definition. Thinking encryption is compression. Encryption hides; compression shrinks. Different goals, different tools. Assuming individual data points are harmless. The exam's point is that combination identifies. ZIP + birthday + gender is PII in combination. A user receives an email that appears to be from their bank, asking them to click a link and enter their password to \"verify their account.\" The link leads to a fake site. This is an example of which of the following? Keylogging Phishing A rogue access point A virus Deceptive messages that trick people into giving up information are phishing. Which of the following is an example of multifactor authentication? Entering a password and then a second, different password Entering a password and then a code sent to the user's phone Entering the same password twice Choosing a very long password Password (something you know) + phone code (something you have) uses two different factor categories. In public key encryption, which of the following is true? The same key is used to encrypt and decrypt a message. The public key is used to encrypt, and only the corresponding private key can decrypt. The private key must be shared with anyone who wants to send a message. Public key encryption cannot be used over the internet. Asymmetric encryption: public key locks, private key unlocks, and the private key is never shared. Which of the following is considered personally identifiable information (PII)? The current temperature in a city A person's date of birth combined with their home address The number of users of a website A public domain image Information that can identify a specific individual — especially in combination — is PII. PII: personally identifiable information — data that can identify an individual Multifactor authentication: verifying identity with two or more categories of evidence: know, have, are Encryption: converting data into a form unreadable without a key Symmetric encryption: one shared key encrypts and decrypts Public key encryption: a public key encrypts; only the matching private key decrypts Certificate authority: a trusted organization that verifies a site's public key via a digital certificate Malware: software designed to damage a system or gain unauthorized access Virus: malware that attaches to programs and spreads when they run Phishing: tricking people into revealing information through fake messages or sites Keylogging: secretly recording a user's keystrokes Rogue access point: a fake wireless network set up to intercept traffic",
  "ap-csa/unit-1.html": "Everything else in the course assumes Unit 1. Variables, types, expressions, calling methods on objects, and the String class. Weakness here shows up as lost points in every other unit. Java programs are sequences of statements that a compiler translates; errors can happen at compile time or run time. Variables have types; primitive types hold values directly while reference types hold addresses of objects. Methods are called on classes (static) or objects (instance), take arguments, and may return values. String and Math are the two classes you must know in detail, using exactly the methods on the Java Quick Reference.",
  "ap-csa/1-1.html": "The vocabulary you need before the first line of Java: what a compiler does, what a syntax error is, and why some errors only appear when the program runs. An algorithm is a finite set of instructions that accomplishes a task. A program expresses an algorithm in a programming language so a computer can run it. Java source code is written in .java files and compiled into bytecode. The compiler translates and checks the code before it can run. A syntax error (compile-time error) is a violation of Java's grammar — the compiler refuses to produce a program. A run-time error (exception) happens while the program executes, such as dividing an int by zero. A logic error compiles and runs but produces the wrong result. Sequence , selection , and iteration are the three control structures every algorithm is built from. A Java program's entry point is public static void main(String[] args) . Statements end with semicolons; blocks are enclosed in braces { } . Comments ( // single line, /* */ block) are ignored by the compiler and exist for people. public class Hello { public static void main(String[] args) { System.out.println(\"Hello, AP CSA\"); int x = 10 / 0; // compiles fine, then ArithmeticException at run time } } The missing-semicolon version of line 5 would be a compile-time error — nothing runs. As written, it compiles, prints the greeting, then throws a run-time exception on line 6. The exam wants you to classify each. If a question shows code and asks \"what happens,\" check syntax first (missing semicolon, mismatched braces, undeclared variable = won't compile), then look for run-time traps (division by zero, null, bad index), then trace for logic. Java is compiled to bytecode , then run by the Java Virtual Machine. That two-stage design is why the same .class file runs on any operating system — and why the exam separates compile-time from run-time . The compiler checks syntax (grammar) and types (is that an int where an int is expected?). It cannot check logic. A program that compiles cleanly can still be completely wrong. Exceptions are Java's run-time errors. The ones the exam names: ArithmeticException (int division by zero), ArrayIndexOutOfBoundsException , IndexOutOfBoundsException (ArrayList), NullPointerException , StringIndexOutOfBoundsException , ConcurrentModificationException . You won't write exception handling; you'll identify which one a code segment throws. The main method header is exact: public static void main(String[] args) . You don't need to explain each word yet, but you should recognize it as the entry point. Statements end with ; . Blocks use { } . Indentation is for humans only — Java ignores it. Case matters: Count and count are different variables. An algorithm exists independent of language. The same algorithm in AP CSP pseudocode and in Java is the same algorithm. The exam sometimes describes an algorithm in English and asks which Java implements it. Saying a program with a syntax error \"produces wrong output.\" It produces no output — it never runs. Wrong output means it compiled and ran. Saying a crash is a logic error. A crash (exception) is a run-time error. Logic errors finish silently with the wrong answer. Expecting the compiler to catch division by zero. It doesn't; the values aren't known until run time. Which of the following is a compile-time error in Java? Dividing an integer by zero Accessing an array index that is out of bounds Omitting the semicolon at the end of a statement Calling a method on a variable whose value is null A missing semicolon violates syntax, so the compiler rejects it. The other three compile and fail at run time. A program compiles and runs to completion but prints an incorrect total because the loop stops one iteration early. This is best described as which type of error? Compile-time error Run-time error Logic error Syntax error Runs fine, wrong answer — that's a logic error. Compiler: translates Java source code into a form the computer can run, reporting syntax errors Compile-time error: an error the compiler detects; the program cannot run Run-time error: an error that occurs during execution, such as an exception Logic error: code that runs but produces incorrect results",
  "ap-csa/1-2.html": "Java is statically typed: every variable is declared with a type that never changes. The exam's primitive types are int, double, and boolean; everything else is a reference. A variable is a named memory location with a declared type . Declaration: int count; . Declaration with initialization: double price = 9.99; Primitive types on the exam: int (whole numbers), double (decimals), boolean ( true / false ). A primitive variable stores the value itself. Reference types (like String and every other class) store the address of an object, not the object. Two reference variables can point to the same object. int has a fixed range: Integer.MIN_VALUE (−2,147,483,648) to Integer.MAX_VALUE (2,147,483,647). Exceeding it causes overflow — it wraps around silently, no error. The final keyword makes a variable a constant: final int MAX = 100; . Assigning to it again is a compile-time error. Naming: variables start with a lowercase letter ( totalScore ); constants are all caps ( MAX_SIZE ); classes start uppercase ( String ). Names must be declared before use and are case-sensitive. int age = 17; double gpa = 3.85; boolean isSenior = true; String name = \"Ram\"; final double TAX = 0.07; age = 18; // fine TAX = 0.08; // compile-time error: cannot assign to final Four types, four kinds of value. name holds a reference to a String object; the other three hold their values directly. That distinction matters in 1.13 and 3.6. Questions on ranges are usually about overflow: Integer.MAX_VALUE + 1 equals Integer.MIN_VALUE , not an error. And a variable declared but never assigned can't be used — that's a compile error, not a default of 0. The exam's three primitives are int , double , and boolean . Java has others ( char , long , float …) but the CED excludes them. If you see char on the exam, it's in a String context and the question will explain. int is a 32-bit signed integer: exactly −2,147,483,648 to 2,147,483,647. Integer.MAX_VALUE and Integer.MIN_VALUE are on the reference sheet. Overflow wraps silently: MAX + 1 = MIN. No exception. double holds decimals with about 15–16 significant digits of precision. Not every decimal is exact — 0.1 + 0.2 is 0.30000000000000004 . This is why comparing doubles with == is unreliable. A variable must be declared before use and initialized before its value is read . A declared-but-unassigned local variable is a compile error when read. (Instance variables are different — they get defaults. That's 3.3.) Naming rules: start with a letter, _ , or $ ; no spaces; case-sensitive; can't be a keyword. Conventions: camelCase for variables and methods, PascalCase for classes, ALL_CAPS for constants. Conventions aren't enforced by the compiler but the exam follows them and expects you to. final variables must be initialized exactly once. Attempting a second assignment is a compile error. Constants make code readable ( MAX_SPEED instead of 120 ) and safe from accidental change. Assigning a double to an int. int x = 3.7; won't compile. Needs a cast. The reverse ( double d = 3; ) is fine. Expecting overflow to throw. It wraps. Integer.MAX_VALUE + 1 is a large negative number. Assuming a local variable defaults to 0. It has no default. Reading it before assignment is a compile error. Using a keyword as a name. int class = 5; won't compile. Which of the following declarations will cause a compile-time error? int count = 5; double avg = 3; int total = 3.5; boolean done = false; A double literal can't be assigned to an int without a cast. Assigning an int to a double (option B) is fine — it widens automatically. What is the result of evaluating Integer.MAX_VALUE + 1 ? A compile-time error A run-time exception Integer.MIN_VALUE Integer.MAX_VALUE Integer overflow wraps around silently to the minimum value. Java doesn't throw an error. Primitive type: int, double, boolean — stores the value directly Reference type: a variable that stores the address of an object final: keyword that makes a variable's value unchangeable after initialization Overflow: an int calculation exceeding the representable range wraps around",
  "ap-csa/1-3.html": "Arithmetic in Java has one rule that catches everyone: dividing two ints gives an int. Everything else follows normal math. Operators: + , - , * , / , % . Normal precedence: parentheses, then * / % left to right, then + - left to right. Integer division: when both operands are int , / truncates (drops the decimal). 7 / 2 is 3 , -7 / 2 is -3 . If either operand is a double , the result is a double: 7 / 2.0 is 3.5 . % is the remainder : 7 % 2 is 1 , 10 % 5 is 0 . For negative dividends the result is negative: -7 % 2 is -1 . Dividing an int by zero throws an ArithmeticException . Dividing a double by zero gives Infinity or NaN , no exception. System.out.println(x) prints x and moves to a new line; System.out.print(x) prints without a newline. + with a String is concatenation . Evaluation is left to right: \"Sum: \" + 1 + 2 is \"Sum: 12\" , but \"Sum: \" + (1 + 2) is \"Sum: 3\" . String literals are in double quotes; escape sequences include \\n (newline), \\\" (quote), \\\\ (backslash). System.out.println(17 / 5); // 3 System.out.println(17 % 5); // 2 System.out.println(17 / 5.0); // 3.4 System.out.println(17.0 / 5); // 3.4 System.out.println(1 + 2 + \"3\"); // 33 System.out.println(\"1\" + 2 + 3); // 123 System.out.println(2 * 3 % 4); // 2 (6 % 4) Lines 5 and 6 are the concatenation trap: left-to-right evaluation means the ints are added before the String is reached in line 5, but not in line 6. Look at both operands of every / . Both int? Truncate. Either double? Decimal result. For the + trap, scan left to right and note the moment a String appears — from then on everything is concatenation. Integer division truncates toward zero. 7 / 2 is 3; -7 / 2 is −3 (not −4). Truncation, not floor. The exam tests negatives specifically. Remainder sign follows the dividend (left operand): -7 % 3 is −1; 7 % -3 is 1. The identity that always holds: a == (a / b) * b + a % b . Mixed arithmetic promotes to double : if either operand is a double, the whole operation is done in double. 1 / 2 * 2.0 is 0.0 (integer division first: 0, then 0 * 2.0). 1 / 2.0 * 2 is 1.0. Order matters because promotion happens per operation, not per expression. String concatenation is left-to-right. Once a String is on the left of a + , everything after is concatenated. 1 + 2 + \"3\" + 4 + 5 is \"3345\" : 1 + 2 = 3, then \"3\" + \"3\" = \"33\", then + 4 → \"334\", + 5 → \"3345\". System.out.println() with no argument prints a blank line. print vs println matters for exact-output questions: with print , consecutive outputs run together on one line. Printing a double prints the full value: System.out.println(10.0 / 4) shows 2.5 ; 10.0 / 5 shows 2.0 , not 2 . The .0 is part of the answer. Escape sequences: \\n newline, \\t tab, \\\" literal quote, \\\\ literal backslash. The exam uses \\n and \\\" most. Rounding integer division. 7 / 2 is 3, never 3.5 or 4. Two ints in, int out, fraction dropped. Flooring negative division. -7 / 2 is −3. Java truncates toward zero. Adding before concatenating. \"Total: \" + 5 + 10 is \"Total: 510\" . The String came first, so no addition happens. Dropping the .0 on doubles. If the result is a double, the output shows a decimal point. 4.0 , not 4 . What is printed by System.out.println(25 / 4 * 2.0); ? 12.5 12.0 12 13.0 25 / 4 is integer division = 6. Then 6 * 2.0 = 12.0 (double). What is printed by System.out.println(\"Total: \" + 5 + 10); ? Total: 15 Total: 510 A compile-time error 15 Left to right: \"Total: \" + 5 → \"Total: 5\", then + 10 → \"Total: 510\". What is the value of -13 % 5 ? 3 -3 2 -2 Java's remainder keeps the sign of the dividend: -13 = 5 × (-2) + (-3), so the result is -3. Integer division: int / int drops the fractional part Remainder (%): the remainder after division; sign follows the left operand Concatenation: joining Strings with +; any + involving a String produces a String println: prints a value followed by a newline",
  "ap-csa/1-4.html": "Assignment stores a value; the revised course also expects you to know how a program reads input, using the Scanner class methods on the reference sheet. = is assignment : evaluate the right side, store it in the variable on the left. It is not equality (that's == ). The right side is evaluated using the variable's current value, so x = x + 1 increments. The expression must produce a value compatible with the variable's type. Assigning one primitive variable to another copies the value . Assigning one reference variable to another copies the reference — both then point to the same object. Input comes from Scanner . Create one with Scanner input = new Scanner(System.in); then read with nextInt() , nextDouble() , nextLine() , or next() (one word). hasNext() , hasNextInt() , hasNextLine() return whether more input is available — used to loop over input of unknown length. The nextInt/nextLine trap: nextInt() leaves the newline in the buffer, so a following nextLine() returns an empty string. The fix is an extra nextLine() to consume it. Swapping two variables requires a temporary: int temp = a; a = b; b = temp; Scanner in = new Scanner(System.in); System.out.print(\"Age: \"); int age = in.nextInt(); in.nextLine(); // consume leftover newline System.out.print(\"Name: \"); String name = in.nextLine(); System.out.println(name + \" is \" + age); Without line 4, if the user types 17 and Enter, name becomes the empty string because nextLine() reads the leftover newline. This exact trap is the reason Scanner questions exist. Questions about Scanner are usually \"which method reads a whole line vs. one token\" ( nextLine vs next ) or the nextInt-then-nextLine trap. Also remember: = assigns; a question with if (x = 5) is a compile error because an int isn't a boolean. Assignment is right-to-left. Evaluate the entire right side using current values; then store in the left variable. x = x + 1 is not an equation; it's \"compute x + 1, then overwrite x.\" Copying primitives copies values. After int b = a; , a and b are independent. Copying references copies addresses. After Dog b = a; , both point at one Dog. (Full treatment in 1.13.) Scanner reads tokens or lines. next() reads one whitespace-delimited token. nextLine() reads to the end of the line, including spaces. nextInt() / nextDouble() read one token and convert it — and throw InputMismatchException if it isn't a number. The buffer trap in detail: the user types 42⏎ . nextInt() consumes 42 and leaves ⏎ . The next nextLine() reads up to that ⏎ — an empty String — and consumes it. Only the following nextLine() gets the actual next line. Fix: call nextLine() once after nextInt() to discard the leftover. hasNext family: hasNext() , hasNextInt() , hasNextDouble() , hasNextLine() return booleans and don't consume anything. They're how you loop over input of unknown length — and they're the same methods used for files in 4.6. Scanner is in java.util — needs an import. Creating one: new Scanner(System.in) for keyboard. A swap needs a temporary variable. The three-line pattern is worth memorizing because it appears inside sorting algorithms in Unit 4. Using == for assignment or = for comparison. if (x = 5) won't compile (int isn't boolean). x == 5; compiles but does nothing. Forgetting the buffer clear after nextInt. The next nextLine() gets an empty string. This is the single most common Scanner bug and it's tested. Using next() when you need a whole line. next() stops at the first space. \"Ram Singh\" becomes \"Ram\". Two-line swap. a = b; b = a; leaves both equal to b's original value. After the following code runs, what are the values of a and b ? int a = 3; int b = 7; a = b; b = a; a = 3, b = 7 a = 7, b = 3 a = 7, b = 7 a = 3, b = 3 a becomes 7, then b is assigned a's new value, 7. No swap happened — a temp variable was needed. A program reads an integer with nextInt() and then immediately calls nextLine() to read the user's name. The name is stored as an empty string. Which of the following best explains why? nextLine() cannot read Strings. nextInt() does not consume the newline after the number, so nextLine() reads that leftover newline. The Scanner must be recreated between reads. nextInt() reads the entire line including the name. This is the standard Scanner buffer behavior. An extra nextLine() call fixes it. Assignment (=): stores the value of the right-side expression in the left-side variable Scanner: a class for reading input from the keyboard or a file nextLine(): reads an entire line of input as a String nextInt(): reads the next integer token, leaving the newline in the buffer",
  "ap-csa/1-5.html": "Casting converts between types. The exam tests the truncation of (int), automatic widening to double, and the standard rounding idiom. Casting temporarily converts a value to another type: (int) 3.99 is 3 (truncates toward zero, never rounds); (double) 7 is 7.0 . A cast applies to the value immediately after it. (int) 7.8 / 2 is 3 (cast to 7 first, then integer division). (int) (7.8 / 2) is 3 too but for a different reason (3.9 truncated). (double) 7 / 2 is 3.5 . Widening is automatic: assigning an int to a double works ( double d = 5; gives 5.0). Narrowing (double to int) requires an explicit cast or the compiler complains. In mixed arithmetic, ints are promoted to doubles: 5 / 2.0 is 2.5 . Rounding idiom: (int) (x + 0.5) rounds a positive double to the nearest int. For negatives, (int) (x - 0.5) . Values outside int's range overflow silently; casting a large double to int clamps to Integer.MAX_VALUE or MIN_VALUE . Doubles can't represent most decimals exactly: 0.1 + 0.2 is not exactly 0.3 . Comparing doubles with == is unreliable; check whether the difference is below a small tolerance. double x = 7.6; int a = (int) x; // 7 truncation int b = (int) (x + 0.5); // 8 rounding double c = (double) 7 / 2; // 3.5 double d = (double) (7 / 2); // 3.0 division happened first int e = (int) -7.6; // -7 toward zero, not -8 Lines 4 and 5 are the exam's favorite pair. The cast binds to the nearest value; parentheses change what gets cast. Ask two things for every cast: what exactly is being cast (the next value only, unless parentheses say otherwise), and is it truncation or rounding (a bare (int) always truncates). Toward zero: (int) -2.9 is -2. A cast is an operator that applies to the very next value , before any binary operators. (int) x / y casts x, then divides. (int) (x / y) divides, then casts. These give different results whenever x / y has a fractional part — and the exam builds questions on exactly that difference. (int) truncates toward zero : 3.99 → 3, −3.99 → −3, 0.5 → 0. It never rounds. If you need rounding, use the +0.5 idiom (positives) or −0.5 (negatives). (double) on an int adds .0 : 7 → 7.0. Its main use is forcing real division: (double) a / b . Automatic widening happens in assignment ( double d = 5; ), in arithmetic (int + double → double), and in method calls (passing an int to a double parameter). Narrowing never happens automatically — you must cast. Casting a double outside int's range clamps to Integer.MAX_VALUE or MIN_VALUE rather than wrapping. Rarely tested, but different from arithmetic overflow. Precision loss: because doubles can't represent most decimals exactly, a computation like 0.1 * 3 gives 0.30000000000000004 . Comparing doubles for equality is fragile; the exam avoids it or gives you a tolerance approach. Rounding to two decimal places: (int) (x * 100 + 0.5) / 100.0 . Multiply, round, divide by a double so the result stays decimal. Misplacing the cast's scope. (int) 7.8 / 2 is 7 / 2 = 3 . Students compute (int) 3.9 = 3 — same number by coincidence, wrong reasoning, and it fails on other values. Rounding instead of truncating. (int) 2.9 is 2, not 3. Flooring negatives. (int) -2.9 is −2, not −3. Toward zero. Casting the wrong thing to force real division. (double) (7 / 2) is 3.0 — division already truncated. Cast an operand, not the result. What is the value of (int) 9.99 / 2 * 2.0 ? 9.99 9.0 8.0 10.0 (int) 9.99 → 9. 9 / 2 → 4 (integer division). 4 * 2.0 → 8.0. Which expression correctly rounds the positive double val to the nearest integer? (int) val (int) (val + 0.5) (int) val + 0.5 (double) (val + 0.5) Adding 0.5 then truncating rounds correctly for positives. Option C truncates first, then adds 0.5 producing a double. What is printed by System.out.println((int) -4.7); ? -5 -4 4 -4.0 Casting truncates toward zero, so -4.7 becomes -4. Cast: an explicit conversion, e.g. (int) or (double) Truncation: dropping the fractional part when casting to int Widening: automatic conversion from int to double Narrowing: conversion from double to int, requires a cast",
  "ap-csa/1-6.html": "Shorthand that shows up in every loop. The traps are the hidden cast in compound operators and what ++ does inside an expression. x += 5 means x = x + 5 . Same for -= , *= , /= , %= . Compound operators include an implicit cast back to the variable's type: if x is an int, x /= 2.0 compiles and stores an int. x = x / 2.0 would not compile. x++ and ++x both add 1 to x. x-- and --x subtract 1. On the exam these are used as standalone statements, where the pre/post distinction doesn't matter. The AP exam does not test x++ inside larger expressions (like y = x++ ). Keep increments on their own line. x *= 2 + 3 means x = x * (2 + 3) — the whole right side is computed first. These operators work on doubles too: total += 0.5; int x = 10; x += 3; // 13 x -= 5; // 8 x *= 2; // 16 x /= 3; // 5 (integer division) x %= 4; // 1 x++; // 2 x *= 1 + 1; // 4 (x * 2, not x * 1 + 1) Rewrite every compound operator as its long form before tracing: x *= a + b → x = x * (a + b) . The parentheses are the part students drop. x op= expr means x = x op (expr) — the entire right side is parenthesized. x *= 2 + 3 is x = x * 5 , not x * 2 + 3 . The hidden cast: compound operators cast the result back to the variable's type. int x = 10; x /= 4.0; compiles and leaves x = 2 (10 / 4.0 = 2.5, cast to int). The long form x = x / 4.0 would be a compile error (double to int). This asymmetry is occasionally tested. x++ and ++x are identical as standalone statements. The exam only uses them that way — inside larger expressions ( y = x++ ) is explicitly out of scope, so you won't see it. x-- and --x likewise. x += 1 , x++ , x = x + 1 are all the same statement. Compound operators work on doubles ( total += 0.25 ) and, for += only, on Strings ( s += \"x\" concatenates). Loop updates almost always use these: i++ , i += 2 , i-- . Reading them fluently speeds up every loop question. Forgetting the implied parentheses. x -= 2 * 3 subtracts 6, not 2 then times 3. Misreading x -= 5 as x = -5 . It subtracts 5 from x. Thinking x++ inside a loop body increments twice with the loop header. Each increment is one. If both the header and body increment, i goes up by 2 per iteration. What is the value of n after the following code? int n = 7; n += 3; n /= 2; n *= n; 25 50 49 100 7 + 3 = 10. 10 / 2 = 5. 5 * 5 = 25. What is the value of total after int total = 12; total -= 2 * 3; ? 30 6 18 10 The right side (2 * 3 = 6) is computed first, then total = 12 - 6 = 6. Compound assignment: operators like += that combine arithmetic with assignment Increment (++): adds 1 to a variable Decrement (--): subtracts 1 from a variable",
  "ap-csa/1-7.html": "Java ships with thousands of classes. An API tells you what they do and how to call them, without showing the code inside. A library is a collection of classes written and tested by others. Java's standard library includes String , Math , Scanner , ArrayList , and more. An API (Application Program Interface) is the documentation of a library: each class, its methods, their parameters, return types, and what they do. You program to the API without knowing the implementation. The AP exam provides a Java Quick Reference — a mini-API listing exactly the methods you may use from String, Math, Integer, Double, ArrayList, and Object. Classes in the java.lang package (String, Math, Integer, Double) are available automatically. Others ( Scanner , ArrayList , File ) require an import statement at the top of the file. Reading documentation is a skill: the method header tells you the return type, name, and parameter types. That's enough to call it correctly. Using a library is abstraction in practice — you rely on what a method does, not how . import java.util.Scanner; // needed: not in java.lang import java.util.ArrayList; public class Demo { public static void main(String[] args) { String s = \"abc\"; // String: no import double r = Math.sqrt(16); // Math: no import ArrayList<String> list = new ArrayList<String>(); } } If a question asks why a program won't compile and it uses Scanner or ArrayList without an import, that's the answer. If it asks what a method returns, look at the header's return type — the exam sometimes gives you a header for a made-up method and expects you to use it correctly from the header alone. The Java Quick Reference provided on the exam lists exactly: String (length, substring ×2, indexOf, equals, compareTo), Integer (MIN_VALUE, MAX_VALUE, parseInt), Double (parseDouble), Math (abs, pow, sqrt, random), ArrayList (size, add ×2, get, set, remove), Object (equals, toString), File and Scanner (the input methods). Anything not listed you should treat as unavailable on FRQs. Reading a method in an API: public int indexOf(String str) tells you it's public (callable), returns an int, is named indexOf, takes one String. That's everything needed to call it correctly. java.lang is imported automatically: String, Math, Integer, Double, Object, System. Everything else needs import package.Class; at the top of the file, before the class declaration. java.util.Scanner , java.util.ArrayList , java.io.File , java.io.FileNotFoundException are the ones on the exam. Libraries are abstraction: you don't know how Math.sqrt computes a square root and don't need to. The API is the contract. This is the same idea as procedural abstraction in 3.1. On FRQs, the problem often gives you a class with its method headers and says \"you may use these.\" Treat that as an API: use the methods as specified, don't reimplement them, and don't assume methods that aren't listed. Using methods not on the reference sheet. charAt , contains , split , toCharArray , Math.round , Math.floor — none are on it. On an FRQ they may not earn credit even if they'd work in real Java. Forgetting an import. Using Scanner or ArrayList without importing is a compile error and a standard question. Assuming a method's behavior instead of reading it. If an FRQ defines getScore() , read what it returns. Don't guess. Which of the following best describes an API? The source code of a library The documentation describing a library's classes, methods, parameters, and return values A type of run-time error A Java keyword An API is the interface specification — what you can call and how — not the implementation. Which of the following classes requires an import statement before it can be used? String Math Scanner Integer Scanner is in java.util. The other three are in java.lang, which is imported automatically. Library: a collection of pre-written classes available for use API: documentation describing how to use a library's classes and methods import: a statement that makes a class outside java.lang available in a file Java Quick Reference: the list of methods provided on the AP exam",
  "ap-csa/1-8.html": "Comments document code for humans. The exam has specific vocabulary for the promises a method makes. // starts a single-line comment. /* ... */ is a multi-line comment. /** ... */ is a Javadoc comment, used to document classes and methods. A precondition is a condition that must be true before a method is called for it to work correctly. The method is not required to check it — the caller is responsible. A postcondition is a condition that will be true after the method finishes, assuming preconditions were met. It describes what the method guarantees. On FRQs, the problem statement gives preconditions (e.g., \"the array contains at least one element\"). You may rely on them — you don't need to handle cases they rule out. Good comments explain intent and non-obvious decisions. Restating the code ( x++; // add one to x ) adds nothing. Documentation helps future readers, including you, and is part of a professional development process. /** * Returns the average of the values in nums. * Precondition: nums.length > 0 * Postcondition: the returned value is the sum of the * elements divided by nums.length */ public static double average(int[] nums) { // no need to check for empty array: precondition handles it ... } Questions ask \"which statement is a precondition?\" — look for what must be true of the input before calling. Postconditions describe the result . And when an FRQ states a precondition, don't waste time writing code to check it. A precondition is a promise the caller makes: \"I will only call this with valid input.\" The method can assume it's true. On FRQs, when a precondition says \"the array is non-empty,\" you don't check for empty — doing so wastes time and can even introduce bugs. A postcondition is a promise the method makes: \"when I return, this will be true.\" It describes the result, not the process. Different implementations can satisfy the same postcondition. Preconditions and postconditions are the formal version of an API contract. They let you use a method without reading its code: know what it needs, know what it guarantees. Comments explain why , not what . i++; // increment i is noise. i++; // skip the header row is documentation. Javadoc comments ( /** */ ) before a class or method are extracted by tools into API documentation. That's how the official Java docs are generated. The exam may show them; you won't be asked to write them. Reading FRQ problem statements is reading documentation: every method you're given comes with a description, sometimes preconditions, and the expected behavior. Students who skim lose points to details that were spelled out. Checking preconditions inside the method on an FRQ. Not wrong, but wasted effort, and if your check is buggy it costs points. Trust the precondition. Confusing pre and post. Pre = about inputs, before. Post = about outputs/state, after. Restating code in comments. The exam won't penalize it, but it signals not understanding what comments are for. A method getElement(int index) is documented with \"Precondition: 0 <= index < size.\" Which of the following is true? The method must check whether index is valid and throw an exception if not. The caller is responsible for ensuring index is valid before calling the method. The method will return -1 if index is invalid. The precondition is checked by the compiler. Preconditions are the caller's responsibility. The method assumes they hold. Which of the following best describes a postcondition? A requirement on the arguments passed to a method A statement about what is true after a method completes execution A comment that explains the algorithm used A run-time check inside the method Postconditions describe the guaranteed state or result once the method finishes. Precondition: a condition that must be true before a method is called Postcondition: a condition guaranteed to be true after a method finishes Javadoc: a /** */ comment used to document classes and methods",
  "ap-csa/1-9.html": "A method's signature is how Java knows which method you mean. Reading a signature tells you how to call it and what you get back. A method header looks like: public static int max(int a, int b) — access modifier, (optionally) static , return type , name , parameter list . The signature is the method name plus the parameter types, in order: max(int, int) . The return type is not part of the signature. Parameters are the variables in the header. Arguments are the values you pass in a call. They must match in number, order, and (compatible) type. Overloading: a class can have several methods with the same name if their parameter lists differ. Java picks the one whose parameters match the arguments. A method with return type void returns nothing and is called as a statement. A method with a non-void return type produces a value you can use in an expression. Passing an int where a double parameter is expected works (widening). Passing a double where an int is expected is a compile error. public static double area(double radius) // area(double) public static double area(double w, double h) // area(double, double) public static void greet(String name) // greet(String) area(3); // calls first: 3 widens to 3.0 area(4, 5); // calls second greet(\"Ram\"); // fine; returns nothing int x = greet(\"Ram\"); // compile error: void has no value To decide which overloaded method a call uses, count the arguments and match their types. When an FRQ gives you a method header to implement, copy it exactly — changing the return type or a parameter name costs points. A full method header: [access] [static] returnType name(paramType1 name1, paramType2 name2) . Access is public or private . static is optional. Return type is any type or void . The signature is name + parameter types in order. calc(int, double) and calc(double, int) are different signatures — both can coexist. Return type and parameter names are not part of the signature, so int f(int x) and double f(int y) conflict. Overload resolution: Java picks the method whose parameter types match the argument types exactly; if none match exactly, it picks the closest via widening. f(5) with only f(double) defined works (5 widens). f(5.0) with only f(int) defined fails. Arguments are evaluated before the call, left to right. f(x, x++) is out of scope, but f(a + 1, b * 2) computes both, then calls. The number of arguments must equal the number of parameters. No defaults, no optional parameters in the Java the exam covers. On FRQs, you're given the header to implement. Copy it character for character . Changing public to private , dropping static , renaming a parameter — each can cost the rubric point for the header. Passing a double where an int parameter is expected. Compile error. Narrowing doesn't happen automatically. Assuming return type distinguishes overloads. It doesn't. Two methods with the same name and parameter types won't compile regardless of return type. Rewriting the FRQ header. Use exactly what's given. Assigning a void call to a variable. int x = printer.show(); where show is void — compile error. Given the header public static int compute(int x, double y) , which call will compile? compute(2.5, 3) compute(2, 3) compute(2) compute(\"2\", 3.0) The int 3 widens to double for y. Option A tries to pass a double for the int parameter, which doesn't compile. Which of the following is part of a method's signature? Its return type Its name and the types of its parameters The names of its parameters The body of the method Signature = name + parameter types. Return type and parameter names aren't included. Method signature: the method name and its parameter types in order Return type: the type of value a method produces, or void Overloading: multiple methods with the same name but different parameter lists Parameter: a variable in a method header Argument: a value passed to a method when it is called",
  "ap-csa/1-10.html": "Class (static) methods belong to the class, not to an object. You call them with the class name, and the call transfers control until the method returns. A class method (or static method) is declared with static and called using the class name: Math.abs(-3) , Integer.parseInt(\"42\") . Inside the same class, a static method can be called by name alone: helper(5) . A method call transfers control to the method. When it hits a return statement (or the end of a void method), control comes back to the line after the call. If the method returns a value, the call expression is that value: int m = Math.max(3, 9); stores 9. A void method's call is a statement by itself. Using it in an expression is a compile error. Arguments are evaluated before the call; the parameter receives a copy of each argument's value. public class Calc { public static int square(int n) { return n * n; } public static void main(String[] args) { int r = square(4) + Calc.square(2); // 16 + 4 System.out.println(r); // 20 } } Trace method calls by writing down the line you're returning to, evaluating the method with the argument values, then substituting the return value back. Multiple calls on one line are evaluated left to right. A call to a static method: ClassName.methodName(args) . Inside the same class, the ClassName. prefix is optional. Both are common on the exam. Control transfer: the call suspends the current method, runs the called method to its return, then resumes. If the called method itself calls another, that nests. A trace must track a stack of \"where to return to.\" Return substitution: the entire call expression is replaced by the returned value. 3 + max(4, 9) * 2 → 3 + 9 * 2 → 21. Evaluate the call when you reach it in normal left-to-right, precedence-respecting order. Parameters get copies. For primitives, the copy is the value; the caller's variable is untouched by anything the method does. For objects (1.13, 3.6) the copy is the reference. A method with void return type is called as a statement. It can still have effects — printing, modifying an object — but produces no value. Early return: a return inside an if ends the method immediately. Code after it (on that path) doesn't run. A method can have several returns; exactly one executes per call. Continuing to trace after a return. Once return executes, you're back in the caller. Stop reading the method. Losing the return point. Write it down before jumping into the method. Expecting a primitive argument to change. increment(x) doesn't change x. Only the returned value matters. Consider the following method. public static int mystery(int a, int b) { if (a > b) { return a - b; } return b - a; } What is printed by System.out.println(mystery(3, 8) + mystery(10, 4)); ? -1 11 1 17 mystery(3, 8) → 8 - 3 = 5. mystery(10, 4) → 10 - 4 = 6. 5 + 6 = 11. Which of the following correctly calls a static method display in class Printer that takes one String parameter and returns nothing? String s = Printer.display(\"hi\"); Printer.display(\"hi\"); Printer p = new Printer(); display(\"hi\"); display.Printer(\"hi\"); Static methods are called with ClassName.method(). A void method can't be assigned to a variable. Class method / static method: a method that belongs to the class and is called with the class name Return statement: ends the method and sends a value back to the caller",
  "ap-csa/1-11.html": "Five Math methods are on the reference sheet. Math.random is the one with a formula you must memorize. Math.abs(x) — absolute value; returns int for int input, double for double. Math.pow(base, exp) — base raised to exp; always returns a double , even Math.pow(2, 3) is 8.0 . Math.sqrt(x) — square root, returns a double. Math.random() — returns a double ≥ 0.0 and < 1.0 . Never exactly 1.0. Random int in a range [low, high] inclusive: (int) (Math.random() * (high - low + 1)) + low . Number of possible values = high − low + 1. Example: die roll 1–6 is (int) (Math.random() * 6) + 1 . Random 10–20: (int) (Math.random() * 11) + 10 . All Math methods are static: always Math.method(...) , never new Math() . int a = Math.abs(-7); // 7 double b = Math.pow(2, 10); // 1024.0 double c = Math.sqrt(2.25); // 1.5 int roll = (int) (Math.random() * 6) + 1; // 1..6 int r = (int) (Math.random() * 50) + 25; // 25..74 (50 values) int wrong = (int) Math.random() * 6 + 1; // always 1! cast binds first The last line is the classic error: (int) Math.random() is 0 before the multiplication happens. For \"which expression produces a random integer from a to b,\" check two things: the multiplier equals the count (b − a + 1), and the added value is a. For \"what range does this produce,\" the minimum is the added value and the maximum is that plus the multiplier minus one. Math.abs is overloaded: abs(int) returns int, abs(double) returns double. Math.abs(-3) is 3 ; Math.abs(-3.0) is 3.0 . Math.pow takes two doubles (ints widen) and always returns a double. Math.pow(2, 3) is 8.0 . Assigning it to an int requires a cast: int x = (int) Math.pow(2, 3); . Math.sqrt returns a double. Negative input gives NaN , not an exception. Math.random() returns a double in [0.0, 1.0) — 0 possible, 1 impossible. Multiply by n → [0, n). Cast to int → 0 to n−1. Add low → low to low+n−1. The formula, fully: (int) (Math.random() * (high - low + 1)) + low . Parentheses around the multiplication are essential — the cast must apply to the product, not to Math.random() alone (which would always give 0). Reverse-engineering a range: for (int) (Math.random() * 7) + 3 : multiplier 7 → 7 values; added 3 → starts at 3; so 3 through 9. Probability questions: Math.random() < 0.3 is true 30% of the time. (int) (Math.random() * 4) == 0 is true 25% of the time. All Math methods are static: Math.sqrt(x) , never x.sqrt() or new Math() . Casting Math.random() before multiplying. (int) Math.random() * 6 is always 0. The cast binds first. Off-by-one in the multiplier. For 1–6 you need 6 values → multiply by 6. For 5–15 you need 11 values → multiply by 11, not 10. Treating Math.pow's result as an int. It's a double. Math.pow(2, 3) + 1 is 9.0 . Thinking Math.random() can return 1.0. It can't. The upper bound is exclusive. Which expression generates a random integer between 5 and 15, inclusive? (int) (Math.random() * 10) + 5 (int) (Math.random() * 11) + 5 (int) (Math.random() * 15) + 5 (int) (Math.random() * 11) + 4 15 - 5 + 1 = 11 possible values, so multiply by 11 and add the low value 5. Option A only reaches 14. What is printed by System.out.println(Math.pow(3, 2) + Math.abs(-4)); ? 13 13.0 9.04 A compile-time error Math.pow returns a double (9.0). 9.0 + 4 = 13.0. What range of values can (int) (Math.random() * 4) * 2 produce? 0 to 8 0, 2, 4, or 6 1 to 8 0 to 7 (int)(Math.random() * 4) is 0, 1, 2, or 3. Times 2 gives 0, 2, 4, 6. Math.random(): returns a double in [0.0, 1.0) Math.pow(a, b): a raised to b, returned as a double Math.abs(x): absolute value Math.sqrt(x): square root as a double",
  "ap-csa/1-12.html": "A class is a blueprint; an object is one thing built from it. This is the mental model for the whole second half of the course. A class defines a type: what data its objects hold ( attributes , stored in instance variables) and what they can do ( behaviors , defined by methods). An object is an instance of a class — one specific thing with its own values for the attributes. \"hello\" and \"world\" are two String objects. Objects are created with new (except String literals, which Java creates for you). Each new makes a separate object. The class's methods are the same for every object; the instance variable values differ. Two Student objects share the getName() method but have different names. Object-oriented programming models a program as objects interacting by calling each other's methods. A class can be used without seeing its code — you only need its API. That's the point of 1.7. // Class: the blueprint (someone wrote this) public class Dog { private String name; private int age; // ... constructor and methods } // Objects: individual dogs built from the blueprint Dog d1 = new Dog(\"Rex\", 3); Dog d2 = new Dog(\"Bo\", 5); // d1 and d2 are two instances with different attribute values Class vs. object questions are definitional: class = template/type, object = one instance with specific values. \"How many objects are created\" = count the new keywords (plus string literals). A class is a type you define: it says what data its objects hold and what operations they support. Objects are instances — each has its own copy of the instance variables with its own values, but all share the class's methods. Attributes (state) become instance variables. Behaviors become methods. When the exam describes a real-world thing — a bank account with a balance that can be deposited to — you should be able to map nouns to attributes and verbs to behaviors. Objects are created with new . Every new makes a distinct object, even with identical arguments. new Dog(\"Rex\") twice creates two dogs named Rex. String is a class, but String literals are created for you: \"hello\" is a String object without new . Also, identical literals may share one object — which is why == on Strings sometimes \"works\" and why you should never rely on it. You can use a class without seeing its code. FRQ 3 gives you a class (say, Student ) with its method headers and asks you to process a list of them. You never see the class body; you use the API. Object-oriented design is thinking in terms of objects that hold state and send messages (method calls) to each other. The exam's Unit 3 is where you write classes; Unit 1 is where you learn to use them. Counting variables as objects. Dog c = a; creates zero objects. Count the new s. Calling the blueprint an instance. Class = blueprint. Object = the thing built from it. Assuming two objects with equal state are the same object. They aren't. == is false; .equals may be true if the class defines it that way. Which of the following best describes the relationship between a class and an object? A class is an instance of an object. An object is an instance of a class, with its own attribute values. A class and an object are the same thing. An object contains multiple classes. The class is the blueprint; each object is one instance made from it. How many objects are created by the following code? Dog a = new Dog(\"Max\", 2); Dog b = new Dog(\"Max\", 2); Dog c = a; 1 2 3 0 Two new statements create two objects. Line 3 copies a reference — c points to the same object as a. Class: a template that defines the attributes and behaviors of a type of object Object: a specific instance of a class Attribute: data stored in an object, held in instance variables Behavior: what an object can do, defined by its methods",
  "ap-csa/1-13.html": "new calls a constructor and returns a reference. Understanding that variables hold references — not objects — explains aliasing and every null crash. ClassName var = new ClassName(args); — instantiation . The new keyword allocates memory, runs the constructor with the arguments, and returns a reference to the new object. A constructor has the same name as the class and no return type. Constructors can be overloaded; the arguments determine which one runs. A reference variable stores an address , not the object. Dog b = a; makes b refer to the same object as a — this is called aliasing . Changing the object through b is visible through a . null is the value of a reference variable that doesn't refer to any object. Calling a method on a null reference throws a NullPointerException at run time. Instance variables of reference type default to null if not initialized; a declared local variable has no default and must be assigned before use. == on references compares addresses : true only if both refer to the same object. Two separate objects with identical contents are != . Dog a = new Dog(\"Rex\", 3); Dog b = a; // alias: same object Dog c = new Dog(\"Rex\", 3); Dog d = null; b.setAge(4); System.out.println(a.getAge()); // 4 — a and b share the object System.out.println(a == b); // true System.out.println(a == c); // false — different objects d.getAge(); // NullPointerException Draw boxes and arrows. Each new is a box; each variable is an arrow. Assignment between reference variables re-points an arrow — it never copies a box. If an arrow points to nothing (null) and you call a method through it, the program crashes. new does three things: allocates memory for the object, runs the constructor to initialize it, and returns a reference (the address). The variable on the left stores that reference. Constructor overloading works like method overloading: the arguments' count and types select which constructor runs. new Dog() and new Dog(\"Rex\", 3) call different constructors. Aliasing is the single most important reference concept. After Dog b = a; , there is one Dog and two names for it. b.setAge(5) changes the Dog that a also points to. This surprises students every year. Reassignment breaks the alias. b = new Dog(\"Bo\", 1); makes b point to a new object; a still points to the original. Now there are two Dogs. null means \"points to nothing.\" It's a valid value for any reference variable. Calling any method on it — even toString , even equals — throws NullPointerException . Checking if (x == null) is safe (no method call). == on references compares addresses. Two variables are == only if they alias the same object. Contents don't matter. For Strings and most classes, use .equals for content comparison. Drawing it: boxes for objects, labeled arrows for variables. Assignment between reference variables redraws an arrow. It never duplicates a box. Expecting Dog b = a; to make a copy. It doesn't. Same object. Expecting a == b to be true for two separate objects with the same values. Different objects, different addresses, false. Calling a method on null. If a question declares a reference variable and never assigns it (or assigns null), any method call on it crashes. Writing public void ClassName() for a constructor. The void makes it a method, not a constructor. Constructors have no return type. Consider the following code, where Counter has a method increment() that adds 1 to its count and getCount() that returns it. Counter x = new Counter(); Counter y = x; x.increment(); y.increment(); System.out.println(x.getCount()); What is printed? 0 1 2 A compile-time error x and y refer to the same Counter. Both increments apply to it, so the count is 2. Which of the following will cause a NullPointerException at run time? String s = \"\"; int n = s.length(); String s = null; int n = s.length(); String s = \"abc\"; s = null; String s = null; s = \"abc\"; Calling a method on a null reference throws NullPointerException. Assigning null (C, D) is fine; an empty String (A) is a valid object. Instantiation: creating an object with new Constructor: a special method that initializes a new object; same name as the class, no return type Reference: the address of an object, stored in a reference variable Aliasing: two reference variables referring to the same object null: a reference value meaning 'no object' NullPointerException: the run-time error from calling a method on null",
  "ap-csa/1-14.html": "Instance methods operate on a specific object. The call syntax is object-dot-method, and the exam expects you to classify methods by what they do. Instance methods are called on an object: obj.method(args) . The method operates on that object's instance variables. An accessor (getter) returns information about the object's state without changing it: getName() , length() . A mutator (setter) changes the object's state; typically void : setAge(5) , deposit(100) . Method calls can be chained if each returns an object: s.substring(1).toUpperCase() . The dot operator on a null reference throws NullPointerException. The dot operator on a primitive doesn't compile ( int has no methods). Calling a method with the wrong number or types of arguments, or calling a method the class doesn't have, is a compile-time error. Account acct = new Account(\"Ram\", 100.0); acct.deposit(50.0); // mutator: changes balance double b = acct.getBalance(); // accessor: 150.0 String who = acct.getOwner(); // accessor: \"Ram\" acct.getBalance(); // legal but pointless: value discarded When a question gives you a class's method headers and asks what a code segment does, check each call: is it a mutator (state changed for later lines) or an accessor (value used now)? Track object state in a table just like variables. Instance methods are called on an object and have access to that object's instance variables. acct.deposit(50) changes that account's balance. Calling it on a different account changes a different balance. Accessor (getter): returns state, doesn't change it. Convention: getX() , or isX() for booleans. Mutator (setter): changes state, usually void. Convention: setX(value) , or verbs like deposit , increment . A method that both changes state and returns something exists (ArrayList's remove does), but the exam's own classes mostly follow the accessor/mutator split cleanly. Method chaining works when a method returns an object: s.substring(2).toUpperCase().length() . Read left to right: each call operates on what the previous one returned. The dot operator requires a reference to an object . On a primitive ( int x; x.foo() ) it's a compile error. On null it's a run-time error. On a valid reference to a class without that method, compile error. Calling an accessor and ignoring the result is legal and useless: acct.getBalance(); on its own line does nothing observable. Calling a mutator and expecting a return value. double b = acct.deposit(50); where deposit is void — compile error. Calling a method on a primitive. int n = 5; n.toString() — compile error. Primitives have no methods. Not tracking state across calls. Two mutator calls in a row compound. Update the object's state in your table after each. A class Temperature has a method public double getDegrees() and a method public void setDegrees(double d) . Which of the following is true? getDegrees is a mutator and setDegrees is an accessor. getDegrees is an accessor and setDegrees is a mutator. Both are accessors. Both are mutators. get returns state (accessor); set changes it (mutator). Which of the following will not compile? String s = \"hi\"; int n = s.length(); int x = 5; int n = x.length(); String s = \"hi\"; String t = s.toUpperCase(); String s = \"hi\"; boolean b = s.equals(\"hi\"); Primitives have no methods. Calling .length() on an int is a compile-time error. Instance method: a method called on an object that operates on that object's data Accessor: a method that returns information about an object without modifying it Mutator: a method that changes an object's state",
  "ap-csa/1-15.html": "The single most-tested class on the exam. Know every method on the reference sheet and the three traps that account for most lost points. Strings are immutable : methods return a new String; the original never changes. s.toUpperCase() by itself does nothing unless you store the result. Indices start at 0 . The last character is at length() - 1 . length() — number of characters. substring(from, to) — characters from index from up to but not including to . substring(from) — from that index to the end. indexOf(str) — index of the first occurrence, or −1 if absent. charAt is not on the reference sheet; use substring(i, i + 1) to get one character as a String. equals(other) — true if contents match. == compares references and is wrong for comparing String contents. compareTo(other) — returns a negative int if this comes before other alphabetically, 0 if equal, positive if after. It's an int, never a boolean. Uppercase letters come before lowercase. Also available: toUpperCase() , toLowerCase() , and string concatenation with + . Concatenating anything with a String produces a String. A String literal with escape sequences: \"\\\"quoted\\\"\" , \"line1\\nline2\" . String s = \"Computer\"; s.length(); // 8 s.substring(3, 6); // \"put\" (indices 3,4,5) s.substring(5); // \"ter\" s.indexOf(\"put\"); // 3 s.indexOf(\"z\"); // -1 s.substring(0, 1); // \"C\" (one character) \"apple\".compareTo(\"banana\"); // negative \"Zebra\".compareTo(\"apple\"); // negative: 'Z' < 'a' s.equals(\"Computer\"); // true s == \"Computer\"; // unreliable — never use for content s.toUpperCase(); // returns \"COMPUTER\" but s is still \"Computer\" s = s.toUpperCase(); // now s is \"COMPUTER\" Three checks on every String question: (1) substring's second index is exclusive — count characters from from to to - 1 ; (2) any content comparison must use .equals , never == ; (3) compareTo gives an int, so if (a.compareTo(b)) won't compile — you need < 0 , == 0 , or > 0 . Every String method returns a new String or a value; none modify the original. This is immutability. s.toUpperCase() on its own line does nothing visible. s = s.toUpperCase() reassigns s to the new String. substring(from, to) : characters at indices from, from+1, …, to−1. Length of result = to − from. substring(from) : from to the end. substring(i, i) is the empty string. substring(i, i + 1) is one character — this is the exam's replacement for charAt . Valid substring indices: 0 ≤ from ≤ to ≤ length(). Note that to can equal length() (that's \"to the end\"). from > to or to > length() throws StringIndexOutOfBoundsException . indexOf(str) returns the index where str starts , or −1. Only the first occurrence. \"banana\".indexOf(\"an\") is 1. To find later occurrences, search a substring of the remainder. compareTo compares character by character using Unicode values. Uppercase letters (65–90) come before lowercase (97–122), so \"Zoo\".compareTo(\"apple\") is negative. If one string is a prefix of the other, the shorter one is smaller. The magnitude of the result is the difference of the first differing characters (or lengths) — but the exam only cares about the sign. equals is case-sensitive: \"Hi\".equals(\"hi\") is false. To compare ignoring case, lowercase both first. length() counts all characters including spaces and punctuation. \"a b\".length() is 3. Concatenation makes a new String , so building a long String in a loop by += creates many intermediate Strings. Fine at exam scale; worth knowing why StringBuilder exists in real code. Including the end index in substring. \"hello\".substring(1, 3) is \"el\" , not \"ell\" . Using == on Strings. Sometimes true for literals, unreliable in general, and the exam treats it as wrong. Use .equals . Treating compareTo as boolean. if (a.compareTo(b)) won't compile. Compare the int to 0. Forgetting immutability. Calling a String method without storing the result changes nothing. Using charAt. Not on the reference sheet. substring(i, i + 1) instead. What is printed by the following code? String w = \"programming\"; System.out.println(w.substring(3, 7) + w.indexOf(\"g\")); gram3 gramm3 gram10 ram3 substring(3, 7) is indices 3–6: \"gram\". indexOf(\"g\") is the first g, at index 3. Concatenated: \"gram3\". Which expression correctly checks whether the String a comes alphabetically before the String b ? a < b a.compareTo(b) < 0 a.compareTo(b) a.equals(b) < 0 compareTo returns a negative int when a precedes b. Strings can't be compared with String s = \"hello\"; s.toUpperCase(); System.out.println(s); HELLO hello Hello A compile-time error Strings are immutable. toUpperCase() returns a new String that was discarded; s is unchanged. Which of the following returns the last character of a non-empty String s as a String? s.substring(s.length()) s.substring(s.length() - 1) s.substring(s.length() - 1, s.length() - 1) s.charAt(s.length() - 1) substring(length - 1) returns from the last index to the end — one character. Option A returns an empty string; C returns empty (from == to); D returns a char, and charAt isn't on the reference sheet. Immutable: cannot be changed; String methods return new Strings substring(a, b): characters from index a up to but not including b indexOf(str): index of the first occurrence, or -1 if not found equals: compares String contents compareTo: returns a negative, zero, or positive int based on alphabetical order",
  "ap-csa/unit-2.html": "Conditionals and loops — the control flow that makes programs do something interesting. FRQ 1 (Methods and Control Structures) lives entirely here, and a large share of the multiple choice is tracing loops. Boolean expressions drive selection; compound conditions, short-circuit evaluation, and De Morgan's laws determine exactly which branch runs. while and for loops repeat code; the loop condition and update determine how many times. Standard algorithms — sum, count, max, string processing — are built from selection and iteration in predictable shapes. Nested loops multiply iterations, and informal run-time analysis counts statement executions.",
  "ap-csa/2-1.html": "Every algorithm is sequence, selection, and repetition. This topic names them and shows how they combine. Sequence: statements executed in order. Selection: a decision that chooses between paths ( if ). Repetition (iteration): a loop that repeats code ( while , for ). An algorithm can be described in words, in a flowchart, or in code. The same logic can be implemented several equivalent ways. Reading an algorithm: identify the initializations, what decisions are made, what repeats, and what's true when it stops. Tracing is the essential skill — a table with one column per variable and one row per executed statement or loop pass. Two algorithms are equivalent if they produce the same result for all inputs, even if the code differs. int total = 0; // sequence for (int i = 1; i <= 5; i++) // repetition { if (i % 2 == 1) // selection { total += i; } } // total is 1 + 3 + 5 = 9 Before tracing, write what each variable starts as. Then execute statements strictly in order. If a question describes an algorithm in English and asks which code implements it, translate the English into initializations, a loop, and a decision — then match. The three control structures are complete : any algorithm can be expressed with only sequence, selection, and repetition. There's no fourth thing to learn — methods and recursion are ways of organizing these three. Reading an algorithm from code: find what's initialized before the loop (the accumulator or tracker), what the loop iterates over, what the selection decides each time, and what's true when the loop ends. Those four facts identify the algorithm. Writing an algorithm from a description: the description's nouns are variables, its \"for each\" or \"until\" is the loop, its \"if\" is the selection, its \"total/count/largest\" is the accumulator's role. Two code segments are equivalent when every input produces the same result — not when they look similar. The exam's equivalence questions are best answered by testing with specific inputs, including boundaries. Tracing discipline: a table with a column per variable, a row per statement executed (or per loop iteration). Write the values, not the expressions. This is a skill to practice, not a concept to understand. Tracing mentally. Write it down. Every time. Identifying an algorithm by one variable name. A variable named sum might be counting. Read what's added to it. Checking equivalence with one input. One match proves nothing. Try a boundary and a normal case. Which control structure is demonstrated by the code segment if (x > 0) { count++; } ? Sequence Selection Repetition Recursion An if statement chooses whether to execute code — that's selection. An algorithm should add up the even numbers from 2 to 10. Which of the following correctly does so? int s = 0; for (int i = 2; i <= 10; i += 2) { s += i; } int s = 0; for (int i = 2; i < 10; i += 2) { s += i; } int s = 0; for (int i = 1; i <= 10; i++) { s += i; } int s = 2; for (int i = 2; i <= 10; i++) { s += i; } Start at 2, step by 2, include 10. Option B stops at 8; C adds all numbers; D starts wrong and adds odds. Sequence: statements executed one after another Selection: choosing a path with a conditional Repetition: repeating with a loop; also called iteration",
  "ap-csa/2-2.html": "Expressions that evaluate to true or false control everything in this unit. The == vs .equals distinction is where points are lost. Relational operators: == , != , < , > , <= , >= . They produce a boolean. == compares values for primitives and references for objects. To compare object contents (Strings especially) use .equals() . A boolean variable can hold the result: boolean ok = score >= 70; . Then if (ok) — no need for if (ok == true) . Arithmetic is evaluated before relational operators: x + 1 > y * 2 computes both sides first. Comparing doubles with == is unreliable due to round-off; the exam usually avoids it or uses a tolerance. != means \"not equal\"; for Strings, use !a.equals(b) . int a = 5, b = 10; boolean p = a * 2 == b; // true boolean q = a != b; // true String s1 = \"hi\"; String s2 = new String(\"hi\"); boolean r = s1 == s2; // false (different objects) boolean t = s1.equals(s2); // true (same contents) Any time a question compares two Strings with == , be suspicious. If both came from literals it may happen to be true, but the exam's intended answer is that == is not a reliable content comparison — .equals is. For primitives , == and != compare values. 5 == 5 is true. For doubles , exact equality is unreliable due to representation error; compare with a tolerance ( Math.abs(a - b) < 0.0001 ) in real code. The exam avoids the issue. For references , == compares addresses. Two separate objects are never == even with identical contents. Use .equals for content comparison — String defines it by contents; your own classes inherit Object's version (address comparison) unless you write your own. Relational operators have lower precedence than arithmetic, so a + b > c * d does both computations first. They have higher precedence than logical operators ( && , || ), so a > b && c < d compares first, then ands. A relational expression is a boolean value — it can be stored, returned, passed as an argument. return x > 0; is cleaner than if (x > 0) return true; else return false; and the exam accepts both. != on booleans is XOR: a != b is true when exactly one is true. Comparing a boolean to true ( if (done == true) ) is redundant; if (done) means the same. Comparing to false is if (!done) . String comparison with == . The most common wrong answer in Unit 2. Always .equals . Chaining comparisons. 1 < x < 10 doesn't compile (the first comparison yields a boolean, which can't be compared to 10). Write 1 < x && x < 10 . Using = in a condition. Compile error for non-booleans. Which of the following expressions correctly tests whether the String name holds the text \"Ram\"? name == \"Ram\" name.equals(\"Ram\") name = \"Ram\" name.compareTo(\"Ram\") equals compares contents. == compares references; = is assignment; compareTo returns an int, not a boolean. What is the value of x > 3 == y < 2 when x = 5 and y = 1 ? true false 5 A compile-time error x > 3 is true; y , =, ==, != — compares two values and yields a boolean equals(): compares object contents",
  "ap-csa/2-3.html": "The if statement runs a block when a condition is true; adding else gives an alternative. Braces and semicolons cause more errors here than logic does. if (condition) { statements } — runs the block only when the condition is true. This is one-way selection . if (condition) { A } else { B } — runs exactly one of A or B. Two-way selection . The condition must be a boolean. if (x) where x is an int doesn't compile. if (x = 5) doesn't compile either (assignment isn't a boolean). Braces are optional for a single statement, but omitting them is a classic trap: only the first statement is controlled by the if. Always use braces. A semicolon right after the condition — if (x > 0); — ends the if with an empty body, so the next block always runs. After the if/else completes, execution continues with the next statement regardless of which branch ran. int score = 72; if (score >= 70) System.out.println(\"Pass\"); System.out.println(\"Congrats\"); // NOT part of the if — always prints if (score > 90) { System.out.println(\"Honors\"); } else { System.out.println(\"Standard\"); } // Output: Pass Congrats Standard Indentation means nothing to Java. When braces are missing, exactly one statement belongs to the if. The exam writes misleadingly-indented code on purpose; read braces, not spacing. The if body without braces is exactly one statement . A second line, no matter how it's indented, is outside the if. The exam writes deliberately misleading indentation to test this. Always mentally add the braces. if (cond); — a semicolon right after the condition — is an empty if. The block that follows runs unconditionally. It compiles fine, which is why it's dangerous. The else pairs with the nearest unmatched if . With braces, no ambiguity. Without, the dangling-else rule applies (2.4). A condition that's a constant — if (true) — always runs. Rarely written on purpose, but appears in \"which of these is redundant\" questions. The if statement itself has no value. x = if (…) isn't Java. To choose between values, use if/else with two assignments. Two-way selection covers every input: one branch or the other. One-way selection has a \"do nothing\" case. That difference is what \"how many lines are printed\" questions probe. Attaching the second unbraced statement to the if. Only the first belongs. The indentation lies. Missing the stray semicolon. if (x > 5); means the next block always runs. Using a non-boolean condition. if (count) doesn't compile. Java isn't C. What is printed by the following code? int n = 4; if (n > 5) System.out.print(\"A\"); System.out.print(\"B\"); System.out.print(\"C\"); C BC ABC AC Without braces, only print(\"A\") is controlled by the if. \"B\" and \"C\" always print. Which of the following will not compile? if (x > 3) { y = 1; } if (x == 3) { y = 1; } else { y = 2; } if (x) { y = 1; } where x is an int if (x > 3 == true) { y = 1; } The condition must be a boolean; an int isn't. Option D is legal, just redundant. One-way selection: an if statement with no else Two-way selection: an if statement with an else",
  "ap-csa/2-4.html": "Conditionals inside conditionals build multi-way decisions. The skill is knowing which else belongs to which if. A nested if is an if statement inside another's block. The inner one runs only if the outer branch containing it runs. An else-if chain — if / else if / else if / else — tests conditions in order and runs the first true branch only. Later conditions are never checked once one matches. Order matters in an else-if chain. Testing score >= 70 before score >= 90 means 95 gets the 70 branch. A trailing else catches everything not matched above. Without it, it's possible no branch runs. The dangling else : with no braces, an else pairs with the nearest preceding if. Braces remove the ambiguity. Nested ifs can often be rewritten as compound conditions with && (next topic); the exam asks which rewrite is equivalent. int score = 85; if (score >= 90) { grade = \"A\"; } else if (score >= 80) { grade = \"B\"; // runs; the rest is skipped } else if (score >= 70) { grade = \"C\"; } else { grade = \"F\"; } Reverse the order (test 70 first) and 85 becomes a C. First true branch wins. Trace else-if chains top to bottom and stop at the first true condition. For nested ifs with no braces, an else attaches to the closest if above it — draw the braces in yourself before tracing. An else-if chain is nested if/else written flat. if (A) X else if (B) Y else Z is exactly if (A) { X } else { if (B) { Y } else { Z } } . Tracing: check A; if false, check B; if false, do Z. Exactly one of X, Y, Z runs. Order matters when conditions overlap. With ranges, either order them so the most specific comes first, or make them disjoint with compound conditions. An unreachable branch (a condition that can never be true because an earlier one caught it) is a classic \"what's wrong with this code\" answer. Nested if without else is a filter: if (a) { if (b) { X } } runs X only when both hold. Equivalent to if (a && b) { X } . Adding an else to either if breaks the equivalence — then it matters which if the else belongs to. Dangling else: if (a) if (b) X; else Y; — the else belongs to the inner if (b), not the outer. So Y runs when a is true and b is false. When a is false, nothing runs. Braces fix the ambiguity and the exam expects you to know the rule when they're absent. Trace nested conditionals by elimination : decide the outer condition, cross out the branch that doesn't run, and only then look inside the surviving branch. Deep nesting is a readability smell. The fix is usually compound conditions or a chain — and equivalence questions ask which flat version matches a nested one. Evaluating a later branch after an earlier one matched. First true condition wins; the rest are skipped. Attaching a dangling else to the outer if. Nearest unmatched if. Inner. Assuming a flat rewrite is equivalent without checking each path. Test an input for each branch of the original. What is printed by the following code when x = 15 ? if (x > 5) { if (x > 20) { System.out.print(\"big\"); } else { System.out.print(\"medium\"); } } else { System.out.print(\"small\"); } big medium small medium small x > 5 is true (outer branch). Inside, x > 20 is false, so the inner else prints \"medium\". The outer else never runs. Consider the following code. if (n >= 10) System.out.print(\"X\"); else if (n >= 100) System.out.print(\"Y\"); else System.out.print(\"Z\"); For which values of n is \"Y\" printed? n ≥ 100 n ≥ 10 10 ≤ n < 100 No value of n Any n ≥ 100 is also ≥ 10, so the first branch catches it. The \"Y\" branch is unreachable. Nested if: an if statement inside the block of another if else-if chain: a series of conditions tested in order; only the first true one runs Dangling else: an else that pairs with the nearest unbraced if",
  "ap-csa/2-5.html": "Combine conditions with && and ||, negate with !. Short-circuit evaluation is the concept that turns into exam questions. !a is true when a is false. a && b is true only when both are true. a || b is true when at least one is. Precedence: ! first, then && , then || . Relational operators bind tighter than all three. Use parentheses to be safe. Short-circuit evaluation: for a && b , if a is false, b is never evaluated . For a || b , if a is true, b is never evaluated. Short-circuiting prevents errors: if (s != null && s.length() > 0) is safe because the length call is skipped when s is null. Reversed, it would crash. Also prevents division by zero: if (d != 0 && n / d > 2) . Compound conditions can replace nested ifs: if (a) { if (b) { ... } } ≡ if (a && b) { ... } when there are no else branches. !(x > 5) is x <= 5 — the negation includes the boundary. int age = 17; boolean permit = true; boolean canDrive = age >= 16 && permit; // true boolean discount = age < 13 || age >= 65; // false boolean neither = !(age < 13 || age >= 65); // true String s = null; if (s != null && s.length() > 3) // safe: second part skipped { ... } For \"which condition is safe\" questions, the null or zero check must come first in an && . For evaluation questions, compute each relational part to true/false, then apply ! → && → ||. Precedence: ! (highest, unary) → && → || (lowest). And all three are below relational operators. a || b && c is a || (b && c) . Short-circuit is guaranteed, not optional. Java's && and || always skip the right operand when the left decides. This is what makes null-guards and zero-guards safe: x != null && x.method() never calls the method on null. Order of the guard matters. s.length() > 0 && s != null is wrong — the length call happens first and crashes on null. Guard first, use second. || short-circuits on true: x == 0 || 10 / x > 2 is safe because when x is 0, the division never runs. Three or more operands: a && b && c is true only if all three are; evaluated left to right, stopping at the first false. a || b || c stops at the first true. Compound conditions replace nested ifs when there are no elses. if (a && b) ≡ if (a) { if (b) … } . if (a || b) ≡ two separate ifs with the same body — except the body would run twice if both are true, so they're not quite equivalent. !(a && b) is not !a && !b . It's !a || !b (2.6). Guarding after using. arr[i] == 5 && i < arr.length crashes when i is out of bounds. Put the bound check first. Misgrouping mixed && and ||. && binds tighter. Add parentheses mentally. Thinking both sides always evaluate. They don't. The exam asks \"does the method on the right ever get called\" — and with short-circuit, the answer can be no. For which values of x is x > 3 && x < 10 || x == 0 true? Only x from 4 to 9 x from 4 to 9, or x equal to 0 Only x equal to 0 x from 3 to 10 && binds tighter than ||: (x > 3 && x if (arr.length > 0 && arr[0] == 5) . Which best explains why this is safe even when arr has zero elements? Java checks all array indices at compile time. Because of short-circuit evaluation, arr[0] is never evaluated when arr.length > 0 is false. arr[0] returns 0 for empty arrays. The condition always evaluates both parts, so it is not safe. && stops as soon as the left side is false, so the out-of-bounds access never happens. && (and): true only when both operands are true || (or): true when at least one operand is true ! (not): negates a boolean Short-circuit evaluation: the right operand of && or || is skipped when the left operand decides the result",
  "ap-csa/2-6.html": "Two conditions can look different and mean the same thing. The exam asks which is equivalent — De Morgan's laws and truth tables are how you answer. Boolean expressions are equivalent if they evaluate to the same value for every possible input. De Morgan's laws: !(a && b) ≡ !a || !b ; !(a || b) ≡ !a && !b . Negating flips the operator and negates each part. Negating a comparison flips it to include the boundary: !(x < 5) ≡ x >= 5 ; !(x == y) ≡ x != y . A truth table lists every combination of inputs and the result — the reliable way to prove equivalence when reasoning gets confusing. Simplifications: a == true ≡ a ; a == false ≡ !a ; !!a ≡ a . Two if/else structures are equivalent if every input takes the same action, regardless of how the conditions are written. Is !(x > 10 || y <= 0) equivalent to x <= 10 && y > 0 ? De Morgan: !(A || B) = !A && !B . !(x > 10) = x <= 10 . !(y <= 0) = y > 0 . So yes. Check one case: x = 3, y = 4 → original: !(false || false) = true; rewrite: true && true = true. ✓ Apply De Morgan mechanically: flip the operator, negate each side, and flip each comparison to its boundary-inclusive opposite. Then test one or two values. Options that negate the parts but keep the same &&/|| are the standard distractor. De Morgan mechanically: to negate (P && Q) , negate P, negate Q, change && to ||. To negate (P || Q) , negate P, negate Q, change || to &&. Then simplify each negated comparison by flipping it: !(x < 5) → x >= 5 . Comparison negations: < ↔ >= , > ↔ <= , == ↔ != . The negation always flips the boundary's membership. Truth tables prove equivalence with certainty. Two variables: four rows. Three: eight. If every row matches, equivalent. If any row differs, not — and that row is your counterexample. Simplifications the exam expects: x == true → x ; x == false → !x ; !!x → x ; x && true → x ; x || false → x ; x && false → false ; x || true → true . Code equivalence extends beyond boolean expressions: two if/else structures are equivalent if every input takes the same action. Nested ifs vs. compound conditions, chains vs. separate ifs — all can be equivalent or not depending on details. Test. A boolean-returning method can often be simplified: if (c) return true; else return false; is just return c; . And if (c) return false; else return true; is return !c; . Negating parts but not the operator. !(a && b) ≠ !a && !b . The && must become ||. Dropping the boundary. !(x > 5) is x <= 5 , not x < 5 . Trusting intuition on equivalence. Build the table. It's faster than being wrong. Which expression is equivalent to !(a < 3 && b != 7) ? a >= 3 || b == 7 a >= 3 && b == 7 a > 3 || b == 7 a < 3 || b != 7 De Morgan: !(A && B) = !A || !B. !(a = 3; !(b != 7) = b == 7. Which of the following is equivalent to the code segment below? if (x > 0) { if (y > 0) { return true; } } return false; return x > 0 || y > 0; return x > 0 && y > 0; return !(x > 0 && y > 0); return x > 0; true is returned only when both are positive — that's &&. Equivalent expressions: expressions with the same value for all inputs De Morgan's laws: rules for negating && and || expressions Truth table: a table of all input combinations and their resulting values",
  "ap-csa/2-7.html": "A while loop repeats as long as its condition is true. The exam asks how many iterations happen and what the variables hold when it stops. while (condition) { body } — the condition is checked before each iteration. If it's false initially, the body runs zero times. Something in the body must eventually make the condition false, or the loop is infinite . Typical structure: initialize before the loop; test; body; update. Forgetting the update is the classic infinite loop. After the loop, variables keep their final values. If a loop runs while i < 5 incrementing i, then i is 5 afterward. while loops are the choice when the number of iterations isn't known in advance (reading input until a sentinel, searching until found). An if inside a while with a return is a common way to exit a search loop early. int n = 20; int count = 0; while (n > 1) { n = n / 2; count++; } System.out.println(n + \" \" + count); check n > 1 n after count 20: true 10 1 10: true 5 2 5: true 2 3 2: true 1 4 1: false → exit Output: 1 4 . Always trace with a table and include a row for the final failed check — that's where students stop one iteration early or late. And check whether the condition is true at the start; \"zero iterations\" is a real answer choice. A while loop has four parts, even when they aren't adjacent: initialization (before), condition (header), body , update (in the body). Identify all four before tracing. A missing update is an infinite loop. The condition is checked before every iteration , including the first. Zero iterations is a real outcome. The exam includes loops whose condition is false at the start. Iteration count vs. final value: the body runs once for each check that passes. The variable's final value is the one that made the check fail . If the body runs for i = 3, 7, 11, then the loop stops with i = 15 — four values, three iterations. Sentinel loops read input until a special value: while (n != -1) { … n = input.nextInt(); } . The sentinel isn't processed. The first read happens before the loop. Search loops often have compound conditions: while (i < arr.length && arr[i] != target) . The bounds check comes first (short-circuit). After the loop, i < arr.length tells you whether it was found. Modifying the condition's variable in unexpected ways — dividing, multiplying, non-uniform steps — is how the exam varies these. Trace with a table; don't assume a formula. A return inside a while loop (in a method) exits the loop and the method at once. Common in \"find the first…\" methods. Counting the failed check as an iteration. Body runs only when the check passes. Reporting the last-processed value as final. Final is one update past that. Missing the zero-iteration case. Check the condition against the initial values first. Assuming the update is +1. Read the body. How many times is \"x\" printed? int k = 3; while (k < 12) { System.out.print(\"x\"); k += 4; } 2 3 4 0 k = 3 (print), 7 (print), 11 (print), 15 (stop). Three times. What is the value of i after the following loop? int i = 0; while (i * i < 50) { i++; } 7 8 49 50 7 * 7 = 49 int x = 10; while (x > 0) { x -= 3; } int x = 1; while (x != 10) { x += 2; } int x = 0; while (x < 5) { x++; } int x = 8; while (x > 1) { x /= 2; } x takes odd values 1, 3, 5, 7, 9, 11, … and never equals 10. while loop: repeats its body as long as the condition is true; condition checked first Infinite loop: a loop whose condition never becomes false Sentinel: a special input value that signals a loop to stop",
  "ap-csa/2-8.html": "The for loop packages initialization, condition, and update into one header. It's equivalent to a while loop, and the exam expects you to convert between them. for (init; condition; update) { body } . Execution: init once → check condition → body → update → check condition → … until the condition is false. A variable declared in the init ( int i = 0 ) exists only inside the loop . Using it after the loop is a compile error. for (int i = 0; i < n; i++) runs exactly n times, i = 0 through n−1. i <= n runs n + 1 times. Number of iterations for i = a; i < b; i++ is b − a (if b > a). For i <= b , it's b − a + 1. The update can be anything: i += 2 , i-- , i *= 2 . Counting down: for (int i = 10; i > 0; i--) . Any for loop can be rewritten as a while loop by moving the init above and the update to the end of the body. The difference: scope of the loop variable. Modifying the loop variable inside the body is legal but confusing and a source of trick questions. for (int i = 2; i <= 10; i += 3) { System.out.print(i + \" \"); } // prints: 2 5 8 // i would be 11 after, but i doesn't exist outside the loop // equivalent while loop: int i = 2; while (i <= 10) { System.out.print(i + \" \"); i += 3; } System.out.println(i); // 11 — i is in scope here Count iterations by listing the loop variable's values: start, add the step until the condition fails. Don't compute a formula unless the step is 1. And remember: < vs <= is one full extra iteration. Execution order of for (init; cond; update) : init once → cond → body → update → cond → body → update → … → cond false → exit. The update runs after the body, before the next check. Loop variable scope: a variable declared in the init exists only inside the loop. After the loop it's gone — using it is a compile error. To use it after, declare it before the loop and initialize in the init: int i; for (i = 0; …) . Iteration counts: i = a; i < b → b − a iterations. i = a; i <= b → b − a + 1. With step s: ⌈(b − a) / s⌉ for < . When in doubt, list the values. Counting down: for (int i = n; i >= 0; i--) runs n + 1 times (n down to 0). i > 0 runs n times (n down to 1). The exam tests the boundary. Multiplicative steps: for (int i = 1; i < 100; i *= 2) → 1, 2, 4, 8, 16, 32, 64: seven iterations. No formula; list them. for ↔ while conversion: for (A; B; C) { D } ≡ A; while (B) { D; C } . The only difference is the loop variable's scope. Equivalence questions between the two forms hinge on the placement of C (must be at the end of the body) and on scope. Modifying the loop variable inside the body is legal and confusing. If the body does i += 2 and the header does i++ , i increases by 3 per iteration. < vs <= off-by-one. One full extra iteration. Count the endpoints. Using the loop variable after the loop. Out of scope if declared in the header. Converting for to while with the update at the top of the body. It goes at the bottom, or the first iteration sees the wrong value. Applying a formula to a non-unit step. List the values instead. What is printed by the following code? for (int i = 10; i > 0; i -= 4) { System.out.print(i + \" \"); } 10 6 2 10 6 2 -2 10 6 6 2 10 (print), 6 (print), 2 (print), -2 fails i > 0. How many times does the body of for (int j = 3; j <= 15; j++) execute? 12 13 15 14 j takes values 3 through 15 inclusive: 15 - 3 + 1 = 13. Which of the following will cause a compile-time error? for (int i = 0; i < 3; i++) { } System.out.println(i); int i; for (i = 0; i < 3; i++) { } System.out.println(i); for (int i = 3; i > 0; i--) { System.out.println(i); } for (int i = 0; i < 10; i += 3) { } i declared in the for header is out of scope after the loop. Option B declares it outside, so it's fine. for loop: a loop with initialization, condition, and update in its header Loop variable: the counter declared in the for header; scoped to the loop Scope: the region of code where a variable can be used",
  "ap-csa/2-9.html": "The CED lists specific algorithms you should be able to write and recognize. These are the shapes FRQ 1 is built from. Named algorithms the CED expects: compute a sum or average; count values meeting a condition; find the minimum or maximum; check whether a number is even/odd or divisible; compute the digits of an integer; determine whether a number is prime; find the frequency of a condition. Sum/count pattern: initialize to 0 before the loop, update inside. Average = sum ÷ count, cast to double if needed. Max/min pattern: initialize to the first value (or an extreme like Integer.MIN_VALUE ), compare each value, replace if better. Digit extraction: n % 10 is the last digit; n / 10 removes it. Loop while n > 0 . Divisibility: a % b == 0 . Prime: loop from 2 to n−1 (or √n) checking for a divisor; if none, prime. Early exit: a return inside the loop ends the method at the first match — used in \"is there any…\" checks. // sum of the digits of a positive int public static int digitSum(int n) { int sum = 0; while (n > 0) { sum += n % 10; n /= 10; } return sum; } // digitSum(4712): 2, then 1, then 7, then 4 → 14 // is n prime? public static boolean isPrime(int n) { if (n < 2) return false; for (int d = 2; d < n; d++) { if (n % d == 0) return false; } return true; } On FRQ 1, the rubric gives separate points for the loop, the condition, the update, and the return. Even if the logic isn't perfect, a correctly structured loop with the right variable initialized earns credit. Write the skeleton first, then fill in the condition. The CED's named algorithms for this topic: compute a sum, average, minimum, or maximum of a set of numbers; count values with a property; determine even/odd or divisibility; process the digits of an integer; determine whether a number is prime; compute the frequency of a condition. FRQ 1 draws directly from these. Accumulator initialization: sum starts at 0, product at 1, count at 0, max at the first value (or Integer.MIN_VALUE ), min at the first value (or Integer.MAX_VALUE ). Wrong initialization is the most common logic error the exam plants. Average needs a double: (double) sum / count or sum / (double) count . Casting after dividing ( (double) (sum / count) ) is the trap — the truncation already happened. Digit processing: while n > 0 : last digit is n % 10 ; drop it with n /= 10 . This processes digits right to left. Count digits, sum them, reverse the number, check for a digit — all this loop. Prime check: n < 2 is not prime. For d from 2 to n − 1 (or to √n), if n % d == 0 , not prime. If the loop completes, prime. Early return inside the loop is the clean way. Divisibility: a % b == 0 . Even: n % 2 == 0 . Multiple of 5: n % 5 == 0 . FRQ 1 rubric shape: points for the loop header, the correct condition, the correct update/accumulation, and the return. Structure earns points even when a detail is off — so always write the full skeleton. max = 0 for a list that could be all negative. Initialize to the first element. Integer average. sum / count with two ints truncates. Cast one operand. Prime loop that starts at 1. Every number is divisible by 1. Start at 2. Processing digits with a for loop over a String. Works, but the CED's method is % and /. Know both. What does the following method return when called with n = 3407 ? public static int mystery(int n) { int count = 0; while (n > 0) { if (n % 10 % 2 == 1) { count++; } n /= 10; } return count; } 2 3 4 14 It counts odd digits. Digits 7, 0, 4, 3: odd ones are 7 and 3 → 2. The following code is intended to find the largest value among a , b , and c . int max = 0; if (a > max) max = a; if (b > max) max = b; if (c > max) max = c; For which inputs does it give the wrong answer? When all three are positive When all three are negative When two values are equal It is always correct If all values are negative, none exceed 0, so max stays 0 — wrong. Initialize to a first value instead. Accumulator: a variable that collects a running total or count across iterations Digit extraction: using % 10 and / 10 to process an integer's digits",
  "ap-csa/2-10.html": "Combine loops with substring and indexOf to process text. FRQs love \"count how many times,\" \"reverse,\" and \"find all occurrences.\" Iterate over characters with for (int i = 0; i < s.length(); i++) and get each one as a String: s.substring(i, i + 1) . Named algorithms: count occurrences of a substring; find all positions of a substring; reverse a String; check for a palindrome; build a new String from selected characters. Counting substrings: loop i from 0 to s.length() - sub.length() (inclusive) and check s.substring(i, i + sub.length()).equals(sub) . The upper bound prevents an out-of-bounds substring call. Reverse: start with an empty String and prepend each character: rev = s.substring(i, i + 1) + rev; Building a String: start with \"\" and concatenate. Strings are immutable, so you're creating a new one each time — fine at exam scale. indexOf finds the first occurrence only. To find all, loop or use substring to search the remainder. // count occurrences of sub in s (overlapping allowed) public static int countOf(String s, String sub) { int count = 0; for (int i = 0; i <= s.length() - sub.length(); i++) { if (s.substring(i, i + sub.length()).equals(sub)) { count++; } } return count; } // countOf(\"banana\", \"ana\") → 2 (indices 1 and 3) // reverse String rev = \"\"; for (int i = 0; i < s.length(); i++) { rev = s.substring(i, i + 1) + rev; } The loop bound for substring searching is i <= s.length() - sub.length() — get that off by one and you either miss the last match or throw an exception. Always use .equals inside the loop, never == . The CED's named String algorithms: find whether a String contains a substring; count occurrences of a substring; find all positions of a substring; reverse a String; determine if a String is a palindrome; create a new String from selected characters of another. Character-by-character loop: for (int i = 0; i < s.length(); i++) with String c = s.substring(i, i + 1); . Since there's no charAt on the reference sheet, every character is a one-length String. Compare with .equals . Substring-window loop: to check every substring of length k, loop i from 0 to s.length() - k inclusive and examine s.substring(i, i + k) . The bound is the single most-tested detail: too high throws an exception; too low misses the last window. Reverse: prepend each character to an accumulator: rev = s.substring(i, i + 1) + rev; . Or loop from the end appending. Both work. Palindrome: compare s with its reverse using .equals , or compare characters from both ends moving inward with a loop to the middle. Find all occurrences: loop the window and record each i where the window equals the target. Overlapping matches (\"aa\" in \"aaa\" at 0 and 1) are found by this method; indexOf alone only finds the first. Building a filtered String: start with \"\" , loop characters, append those that pass the test. Remove vowels, keep digits, etc. Off-by-one on the window bound. i <= s.length() - k . Not < , not - k + 1 . Using == to compare the window. Always .equals . Appending instead of prepending for reverse. rev += c copies; rev = c + rev reverses. Forgetting Strings are immutable. Every operation needs its result stored. What is printed by the following code? String s = \"abcde\"; String r = \"\"; for (int i = 0; i < s.length(); i += 2) { r = s.substring(i, i + 1) + r; } System.out.println(r); ace eca abcde edcba i = 0, 2, 4 gives a, c, e; each is prepended: \"a\", \"ca\", \"eca\". The method below is intended to return true if s contains the substring \"ab\" . Which loop condition is correct? for (int i = 0; /* condition */ ; i++) { if (s.substring(i, i + 2).equals(\"ab\")) return true; } return false; i < s.length() i <= s.length() i < s.length() - 1 i < s.length() - 2 substring(i, i + 2) needs i + 2 ≤ length, so i ≤ length - 2, i.e., i < length - 1. Option A throws an exception on the last iteration; D misses a match at the end. Palindrome: a String that reads the same forward and backward Substring search: checking each window of a String against a target",
  "ap-csa/2-11.html": "The inner loop runs completely for each pass of the outer loop. Count total iterations, and read the loop bounds carefully when the inner depends on the outer. In nested loops, the inner loop runs to completion every time the outer loop's body executes. Total body executions = (outer iterations) × (inner iterations) when the inner bounds are fixed. When the inner bound depends on the outer variable ( for (int j = 0; j < i; j++) ), the count is a sum: 0 + 1 + 2 + … — triangular numbers. Output questions: track which loop controls rows vs. columns. A println after the inner loop ends the line. Nested loops are how you compare every pair of elements, print grids, or process 2D data (Unit 4). A return exits both loops (and the method). A break is not tested on the exam. for (int r = 1; r <= 3; r++) { for (int c = 1; c <= r; c++) { System.out.print(\"*\"); } System.out.println(); } // * // ** // *** // Total stars: 1 + 2 + 3 = 6 Write the outer variable's value in the margin, then list the inner loop's values for that pass. Don't try to hold both counters in your head. For \"how many times\" with a dependent inner loop, write out the sum explicitly. Execution order: outer init → outer check → [inner init → inner check → inner body → inner update → …→ inner exit] → outer update → outer check → … Each outer iteration runs the entire inner loop from scratch, including its init. Independent bounds (inner doesn't depend on outer): iterations multiply. 4 × 3 = 12. Dependent bounds (inner starts or ends at the outer variable): iterations sum. for j from i to n gives n + (n−1) + … + 1 = n(n+1)/2. for j from 0 to i gives 1 + 2 + … + n. Output patterns: print inside the inner loop and println() after it produces rows. The outer loop counts rows; the inner counts columns. Triangles come from dependent bounds. Return exits everything. A return in the inner loop ends the method — both loops stop. Useful for searching a grid; a trap when tracing. Nested loops are the natural structure for pairs (compare every element with every other — duplicates, closest pair), grids (2D arrays in Unit 4), and substring searches (outer picks the start, inner compares characters). Tracing technique: write the outer variable in the margin, then the full sequence of inner values for that pass. Don't interleave in your head. Run-time: two nested loops over n → n² statement executions. This is what 2.12 counts and what makes some algorithms slow for large inputs. Multiplying when the bound is dependent. Sum the triangular series instead. Forgetting the inner loop re-initializes. j starts over on every outer pass. Putting println in the wrong loop. Inside the inner loop → one item per line. After it → one row per line. Continuing the outer loop after a return. Return ends the method. How many times is \"hi\" printed? for (int i = 0; i < 4; i++) { for (int j = 0; j < 3; j++) { System.out.println(\"hi\"); } } 7 12 3 4 4 outer × 3 inner = 12. How many times is count++ executed? for (int i = 1; i <= 4; i++) { for (int j = i; j <= 4; j++) { count++; } } 16 10 6 4 i = 1: j runs 1–4 (4 times). i = 2: 3 times. i = 3: 2. i = 4: 1. Total 4 + 3 + 2 + 1 = 10. What is printed by the following code? for (int i = 1; i <= 2; i++) { for (int j = 1; j <= 3; j++) { System.out.print(i * j + \" \"); } } 1 2 3 2 4 6 1 2 3 4 5 6 1 2 2 4 3 6 1 1 2 2 3 3 i = 1: 1 2 3. i = 2: 2 4 6. Nested loop: a loop inside the body of another loop Inner loop: the loop that runs to completion on each pass of the outer",
  "ap-csa/2-12.html": "Not Big-O — the exam asks you to count how many times a statement executes, and to compare two code segments by that count. Statement execution count is the number of times a particular statement runs. It's the exam's measure of efficiency. A single loop of n iterations executes its body n times. Nested loops multiply. Sequential loops add. Comparing algorithms: fewer executions for the same input size = more efficient. The exam gives two versions and asks which is better or by how much. Loops that halve their range each time (binary search) run about log₂ n times; loops that check every element run n times; comparing all pairs runs about n². Early exit (return when found) reduces the count in the best case but not the worst. The count can depend on the input, not just its size: searching for the first element takes 1 step; the last takes n. // Version A for (int i = 0; i < n; i++) { for (int j = 0; j < n; j++) { count++; // runs n * n times } } // Version B for (int i = 0; i < n; i++) { count++; // runs n times } for (int j = 0; j < n; j++) { count++; // runs n times } // B runs 2n times total. For n = 100: A = 10,000, B = 200. Read the loop headers, not the bodies. Nested with independent bounds → multiply. Nested with dependent bound → triangular sum (n(n+1)/2). Two loops in sequence → add. A question asking \"how many times is the statement executed when n = 5\" wants a number, so compute it. The exam's efficiency measure is statement execution count — how many times a given statement runs for an input of size n — not clock time and not formal Big-O (though the ideas align). Counting rules: a loop with k iterations runs its body k times. Nested independent loops: multiply. Nested dependent loops: sum the series. Sequential loops: add. An if inside a loop: the condition runs every iteration; the body runs only when true. Common growth patterns and what produces them: constant — no loop; linear (n) — one loop over n; quadratic (n²) — two nested loops over n; logarithmic (log n) — a loop that halves its range (binary search); n log n — merge sort. Best vs. worst case: a search with early exit takes 1 step if the target is first, n if it's last or absent. \"How many comparisons\" questions specify the input; if they say \"maximum\" or \"worst case,\" assume the target is last or missing. Comparing two implementations: the one with fewer statement executions for the same n is more efficient. The exam gives two versions and a specific n, and asks for the counts or which is better. Efficiency isn't everything: for small n, the simpler algorithm may be preferable. But the exam's questions are about the counts, not the judgment call. Counting loop headers as body executions. The condition check runs one more time than the body (the failing check). Assuming an if body runs every iteration. Only when the condition is true. If the question gives the data, count. Treating early-exit as always fast. Worst case is still n. How many times is sum++ executed when n = 6 ? for (int i = 0; i < n; i++) { for (int j = 0; j < n; j += 2) { sum++; } } 12 18 36 9 Outer: 6 iterations. Inner: j = 0, 2, 4 → 3 iterations. 6 × 3 = 18. Two methods search a list of n items for a value. Method X checks every item and returns the count of matches. Method Y returns true as soon as the first match is found. Which statement is true about their execution counts? Y always executes fewer statements than X. X always executes n comparisons; Y executes between 1 and n depending on where the match is. X and Y always execute the same number of statements. Y always executes exactly 1 comparison. X has no early exit. Y's count depends on the data — best case 1, worst case n. Statement execution count: how many times a statement runs; the exam's measure of efficiency Run-time analysis: estimating an algorithm's work as a function of input size",
  "ap-csa/unit-3.html": "Writing your own classes: instance variables, constructors, methods, static members, scope, and this. FRQ 2 (Class Design) asks you to write an entire class from a description — every topic here is a rubric point. A class bundles data (private instance variables) with the methods that operate on it; encapsulation hides the data behind public methods. Constructors initialize objects; methods take parameters, may return values, and can modify objects passed by reference. Static members belong to the class rather than any object; scope determines which variables are visible where. Program design choices have impact on people, including privacy and accessibility.",
  "ap-csa/3-1.html": "Before writing a class, decide what it represents and what it should be able to do. Abstraction is the discipline of hiding everything else. Abstraction means focusing on what something does and hiding how. Data abstraction : representing a concept as a class with attributes. Procedural abstraction : a method's name and header stand in for its implementation. Designing a class: identify the attributes (what it needs to remember — become instance variables) and the behaviors (what it needs to do — become methods). Encapsulation keeps instance variables private and exposes controlled access through public methods. Outside code can't corrupt the object's state. Breaking a program into classes and methods makes it easier to write, test, debug, and reuse — the same reasoning as for procedures. A class should represent one clear concept. Its public methods are its API to the rest of the program. Design happens before code: reading an FRQ 2 prompt, list attributes and behaviors first, then write. Prompt: \"Write a class representing a bank account with an owner, a balance, deposits, withdrawals that can't overdraw, and a way to get the balance.\" Attributes: owner (String), balance (double) → private instance variables. Behaviors: deposit(amount), withdraw(amount) returning whether it succeeded, getBalance() → public methods. Constructor: takes owner and starting balance. That outline is the design. The code follows in 3.3–3.5. On FRQ 2, underline every noun that's a piece of state (becomes an instance variable) and every verb (becomes a method). The prompt tells you the class's design; your job is to translate it faithfully, not invent extras. Data abstraction = modeling a real thing as a class: a Student with a name and GPA, rather than loose variables. Users of the class think \"a student\" not \"a String and a double.\" Procedural abstraction = a method's name and header standing for its body. Both hide detail; they hide different kinds. Designing a class from a description: nouns that describe state → instance variables. Verbs that describe what it does → methods. Adjectives describing the state at creation → constructor parameters. Every FRQ 2 prompt is written to be decomposed this way. Encapsulation has two halves: private instance variables (nobody outside can touch them directly) and public methods (the only way in). This guarantees the object's state can only change in ways the class allows — a BankAccount can enforce \"no negative balance\" because withdraw is the only way to reduce it. Why encapsulation matters for the exam: if a question shows a class with a public instance variable, the \"what's wrong with this design\" answer is that outside code can set invalid state. If it shows a private variable with no getter, the answer to \"how can other code read it\" is \"it can't — add an accessor.\" Good class design is cohesive (one clear concept) and has a minimal public interface (only the methods clients need). Extra public methods are extra ways to break things. Classes are also reuse : once Student exists, any program that needs students uses it. Libraries (1.7) are collections of well-designed classes. Adding features the prompt didn't ask for. FRQ 2 rubrics score exactly what's described. Extra methods earn nothing and can introduce errors. Making instance variables public. Breaks encapsulation and costs a rubric point. Mixing up the two abstractions. A list or class = data abstraction. A method = procedural abstraction. A programmer designs a Playlist class with a private list of songs and public methods addSong , removeSong , and getSongCount . Which of the following best describes this design choice? Encapsulation: the data is hidden and accessed only through public methods. Inheritance: the class extends another class. Recursion: the methods call themselves. Overloading: the methods share a name. Private data with public methods controlling access is encapsulation. Which of the following is an example of procedural abstraction? Storing a student's grades in a private array Calling Math.sqrt(x) without knowing the algorithm it uses Declaring an instance variable as private Creating two objects from the same class Using a method by its interface without knowing its implementation is procedural abstraction. Option A is data abstraction. Data abstraction: modeling a concept as a class with attributes Procedural abstraction: using a method without knowing its implementation Encapsulation: keeping data private and controlling access through public methods",
  "ap-csa/3-2.html": "The CED's ethics topic for this unit: design decisions affect people. Short, but it appears on the exam. Programs are used by people, and design decisions can help or harm them. Programmers have a responsibility to consider effects on all potential users. Privacy: deciding what data to store, how long, and who can access it is a design choice. Storing less is safer. Security: keeping data private (encapsulation) and validating inputs reduces the chance of misuse. Accessibility and inclusion: programs should work for people with disabilities and across different devices, languages, and contexts. Assuming every user is like the developer produces exclusion. Bias: a program's logic and data can embed assumptions that treat groups unfairly; testing with diverse inputs and users helps surface it. Legal and ethical obligations include respecting intellectual property (citing code you use) and not collecting data without consent. These considerations apply during design, not as an afterthought. A class UserProfile stores name, email, and — because it was convenient — full date of birth and home address, all with public getters. Reconsidered: date of birth is only needed to check age, so store a boolean isAdult instead; make the address private with no getter since nothing needs it. Same functionality, far less exposure if the data leaks. That's design impact. Questions here are scenario-based and the right answer usually involves collecting less data, validating before storing, considering users unlike the developer, or getting consent. Options that maximize data collection \"just in case\" are wrong. This is Unit 3's ethics topic, and it's specifically about design decisions — what a class stores, who can access it, what it assumes about users. Unit 4 has a parallel topic (4.1) about data collection . Together they're the CSA equivalent of CSP's Big Idea 5. Data minimization is the design principle: store only what the program needs. Every extra field is a privacy risk if the data leaks and a maintenance cost if it changes. A class that stores a full date of birth to check \"is 18+\" should store a boolean instead. Encapsulation is a security tool: private fields with validating setters mean bad data can't get in. A public field can be set to anything by anyone. This is why 3.1's design advice and 3.2's ethics overlap. Accessibility means designing for people with disabilities (screen readers, motor impairments, color blindness) and for varied contexts (slow devices, small screens, different languages). A program that only works for users like the developer excludes everyone else. The fix is investigating users during design and testing with diverse people. Bias in program logic : a class whose validation assumes names have no spaces, ages are under 100, or addresses have a US format encodes assumptions. When the assumption is wrong for a group, the program fails for that group. Testing with diverse inputs surfaces this. Consent and transparency: users should know what a program stores and does. Storing data silently, even if legal, is an ethical problem the CED wants you to recognize. Intellectual property applies to code you use: cite libraries and borrowed code. On the exam this is conceptual; in practice it's licenses and attribution. The CED frames all of this as the programmer's responsibility . The correct answer to \"who should consider these impacts\" is the developer, during design — not a legal team afterward. Choosing the \"collect everything\" option. Minimization is always the responsible choice. Treating accessibility as optional polish. The CED treats it as a design responsibility. Assuming legal = ethical. The exam distinguishes them. A design can be legal and still irresponsible. A developer is designing a class to store user information for a fitness app. Which of the following is the most responsible design decision regarding user data? Store every piece of data the user could possibly provide, in case it is useful later. Store only the data the app needs to function, and keep it private with controlled access. Make all instance variables public so other classes can access them easily. Share the data with third parties to improve the app. Minimizing collected data and encapsulating it protects privacy and security. Which of the following best describes why a programmer should test a program with a diverse group of users? To make the program run faster To discover assumptions in the design that exclude or disadvantage some users To reduce the number of classes To avoid needing documentation Diverse testing surfaces bias and accessibility problems that the developer's own perspective misses. Data minimization: collecting and storing only the data a program actually needs Accessibility: designing so people with disabilities and varied contexts can use the program",
  "ap-csa/3-3.html": "The parts of a class in the order the exam expects them, and the access modifiers that make encapsulation work. A class declaration: public class Name { ... } . Inside, in conventional order: instance variables , constructors , methods . Instance variables hold each object's state. Declare them private : private String name; Each object gets its own copy. private members are accessible only inside the class. public members are accessible from anywhere. The exam expects private instance variables and public methods/constructors. Instance variables have default values if not initialized: 0 for int, 0.0 for double, false for boolean, null for references. Local variables have no defaults. Accessing a private variable from outside the class ( obj.name ) is a compile-time error. Use an accessor method instead. The class's data type name is the class name; you can declare variables of that type: Student s; final on an instance variable means it's set once (in the declaration or constructor) and never changed. public class Student { // instance variables — private, one set per object private String name; private int grade; private double gpa; // constructor(s) — 3.4 public Student(String n, int g, double gp) { name = n; grade = g; gpa = gp; } // methods — 3.5 public String getName() { return name; } } FRQ 2 rubrics award a point for declaring instance variables private. Forgetting private costs it every year. Also, a class with instance variables but no constructor is legal — Java provides a default one — but on the FRQ you'll be asked to write one. Conventional order inside a class: instance variables → constructors → methods. Java doesn't require this order, but the exam uses it and FRQ graders expect it. Put private fields at the top. Instance variables exist once per object. Two Student objects have two separate name fields. Methods, by contrast, exist once per class and operate on whichever object they were called on. Default values apply only to instance (and static) variables: int → 0, double → 0.0, boolean → false, any reference → null. Local variables inside methods have no default and must be assigned before use. This asymmetry is a favorite compile-vs-runtime question. private means \"visible only inside this class.\" Not subclasses, not other classes in the same file, not anywhere else. Attempting obj.privateField from outside is a compile error — the exam's standard \"which line won't compile\" answer. public means visible everywhere. The exam expects private variables, public methods and constructors . That combination is encapsulation. A class's type name is its name: you can declare Student s; , pass a Student as a parameter, return one, store them in an array or ArrayList. Your class becomes a type as real as String. final instance variables are set in the declaration or the constructor and never again. Useful for things that shouldn't change after creation (an ID number). The class header : public class Name . The file must be named Name.java . One public class per file. Forgetting private on instance variables. A rubric point on every FRQ 2. Assuming a local variable defaults to 0. Only fields do. Accessing a private field from outside. Won't compile. Use the getter. Declaring instance variables inside a method. That makes them local. Fields go directly in the class body. Consider a class Box with private int width; . Which of the following statements outside the class will compile? Box b = new Box(); b.width = 5; Box b = new Box(); int w = b.width; Box b = new Box(); int w = b.getWidth(); where getWidth is a public method Box b = new Box(); System.out.println(b.width); Private variables can't be accessed outside the class. A public accessor method is the correct route. A class declares private int count; and has a constructor that does not assign to it. What is the value of count in a newly created object? A compile-time error occurs 0 null Undefined; it must be assigned before use Instance variables get default values; int defaults to 0. (Local variables are the ones that must be assigned first.) Instance variable: a variable declared in a class that each object has its own copy of private: accessible only within the class public: accessible from any class Default value: the automatic initial value of an instance variable: 0, 0.0, false, or null",
  "ap-csa/3-4.html": "A constructor runs when new is called and sets up the object's initial state. Get the name, the missing return type, and the parameter-to-variable assignments right. A constructor has the class's exact name and no return type (not even void). public Student(String n) { ... } Its job is to initialize instance variables, usually from parameters. Every instance variable should get a value here, even if it's a default like 0 or an empty list. Constructors can be overloaded : a no-argument constructor that sets defaults and a full constructor that takes values. If a class has no constructor, Java supplies a default no-argument one. If you write any constructor, that default disappears. Parameters that are references (like a String or an array) are copied as references — the object's variable points to the same object the caller passed. For mutable objects this can be a problem; for Strings it's safe because they're immutable. A common convention: parameter names differ from instance variable names ( n vs name ), or use this.name = name (3.9). public class Rectangle { private double width; private double height; public Rectangle() // no-arg: defaults { width = 1.0; height = 1.0; } public Rectangle(double w, double h) // full { width = w; height = h; } } Rectangle r1 = new Rectangle(); // 1.0 x 1.0 Rectangle r2 = new Rectangle(3, 4.5); // 3.0 x 4.5 Two mistakes cost FRQ points every year: writing public void Rectangle(...) (the void makes it a method, not a constructor) and assigning parameters to nothing ( w = width; backwards). Instance variable on the left, parameter on the right. A constructor's job: put every instance variable in a valid initial state . Usually from parameters, sometimes from defaults ( count = 0 , items = new ArrayList<>() ). A field left uninitialized gets Java's default, which may not be what you want — an uninitialized ArrayList field is null, and calling add on it crashes. Signature rules: name = class name exactly (case matters); no return type at all (not void, not anything); usually public. public Dog(String n) ✓. public void Dog(String n) ✗ (a method that happens to be named Dog). public dog(String n) ✗ (wrong case, won't compile as a constructor). Default constructor: if you write zero constructors, Java gives you public ClassName() {} for free. Write even one constructor and that freebie vanishes — new ClassName() then fails unless you wrote a no-arg constructor yourself. Overloaded constructors give callers options: no-arg with defaults, partial, full. Arguments select which runs. Within one class, they can't have the same parameter types. Parameter naming: either use different names ( n → name ) or use this.name = name (3.9). Using the same name without this assigns the parameter to itself and leaves the field at its default — a silent bug the exam loves. Reference parameters: if a constructor receives an object (say, an ArrayList) and stores the reference directly, the caller still has a reference to the same list and can modify it behind the object's back. Defensive code copies it. The exam mentions this in 3.6; FRQ 2 rarely requires the copy. Constructors can call methods of the class, and can do computation — a Rectangle constructor can compute and store the area. But they can't return a value. Adding void. Turns the constructor into a method. The class then has only the default constructor, and new Dog(\"Rex\") won't compile. Backwards assignment. n = name; — the field stays default. Field on the left. Same-name parameter without this. name = name; does nothing useful. Expecting the default constructor to survive. It's gone once you write any constructor. Which of the following is a correctly written constructor for a class named Timer with a private int seconds instance variable? public void Timer(int s) { seconds = s; } public Timer(int s) { seconds = s; } public Timer(int s) { s = seconds; } public int Timer(int s) { return s; } No return type, class name, instance variable assigned from the parameter. A has void (a method), C is backwards, D returns a value. A class Point defines only the constructor public Point(int x, int y) . Which of the following will not compile? Point p = new Point(1, 2); Point p = new Point(); Point p = new Point(0, 0); Point p = null; Once any constructor is defined, the default no-arg constructor no longer exists. Constructor: a method with the class's name and no return type that initializes a new object No-argument constructor: a constructor with no parameters that sets default values Default constructor: the no-arg constructor Java provides only when a class defines none",
  "ap-csa/3-5.html": "Writing methods that read and change an object's state. The exam expects accessors, mutators, and a toString that returns — not prints — a String. Method structure: public returnType name(parameters) { body } . A non-void method must return a value of the declared type on every path. Accessors return an instance variable's value: public int getCount() { return count; } . They never change state. Mutators change instance variables and are usually void : public void setCount(int c) { count = c; } . They may validate first. public String toString() returns a String description of the object. System.out.println(obj) calls it automatically. It returns ; it does not print. A method can call other methods of the same class by name, and can use instance variables directly. Reaching the end of a non-void method without a return is a compile-time error (\"missing return statement\"). A return inside an if isn't enough unless every branch returns. Parameters are local to the method; changing a primitive parameter doesn't affect the caller's variable. public class Counter { private int count; private String label; public Counter(String l) { label = l; count = 0; } public int getCount() { return count; } // accessor public void increment() { count++; } // mutator public void reset() { count = 0; } // mutator public boolean isPositive() { return count > 0; } // accessor public String toString() { return label + \": \" + count; // returns, not prints } } Counter c = new Counter(\"clicks\"); c.increment(); c.increment(); System.out.println(c); // clicks: 2 If the prompt says \"returns,\" write return ; if it says \"prints,\" write System.out.println . Mixing them up is a full rubric point. For toString, always return. For every non-void method, ask: does every possible path hit a return? Every path must return in a non-void method. The compiler checks this. if (x > 0) return 1; alone won't compile — what if x ≤ 0? Add an else, or a return after the if. Accessor naming: getName() , getBalance() , and for booleans isActive() , hasItems() . The exam follows these conventions, and FRQ 2 prompts often specify the exact names. Mutator with validation: a setter can reject bad values: public void setAge(int a) { if (a >= 0) age = a; } . This is why encapsulation matters — the class controls what's valid. Mutators that return a status: public boolean withdraw(double amt) that returns whether it succeeded. Common in FRQ 2. Read the prompt for what it should return. toString() must be public String toString() exactly — that signature is what println and string concatenation look for. It returns the description. If it prints instead, println(obj) prints the description and then prints the returned null or empty — wrong. Methods can call other methods of the same class by bare name ( total = computeSubtotal() + tax; ). This is how you avoid duplication inside a class. Local variables in methods are for intermediate work. They vanish when the method returns. If something needs to persist between calls, it's an instance variable. A method that modifies an instance variable and one that reads it can be in the same class without conflict — they're operating on the same field of the same object. Missing return on a path. Compile error. Every branch of a non-void method must return. Printing in toString. Return the String. Don't print it. Returning from a void method with a value. return x; in a void method — compile error. Bare return; is fine. Reading the FRQ verb wrong. \"Returns\" → return. \"Prints\" → println. They're different rubric points. Which of the following methods will cause a compile-time error? public int f(int x) { if (x > 0) { return 1; } return -1; } public int g(int x) { if (x > 0) { return 1; } } public void h(int x) { if (x > 0) { return; } } public boolean k(int x) { return x > 0; } If x ≤ 0, g reaches the end without returning an int — missing return statement. A class has the method public String toString() { return \"Item: \" + name; } . What does System.out.println(item); print, where item has name \"pen\"? Item: pen A memory address pen Nothing; toString must be called explicitly println calls toString on the object automatically. Accessor method: returns the value of an instance variable without changing it Mutator method: changes an instance variable; typically void toString: a method returning a String representation of the object, called automatically by println",
  "ap-csa/3-6.html": "When you pass an object to a method, you pass its address. The method can change the object — and the caller sees it. Primitives don't work that way. Java passes all arguments by value . For primitives, the value is the number. For objects, the value is the reference — so the parameter and the argument point to the same object . A method can modify the object a reference parameter points to (call its mutators, change array elements), and the caller sees the change. A method cannot make the caller's variable point to a different object. Reassigning the parameter ( p = new Thing() ) only changes the local copy of the reference. Changing a primitive parameter never affects the caller. Returning an object returns its reference. The caller can then modify that object through the returned reference. Returning a reference to a private instance variable that is a mutable object breaks encapsulation — outside code can change internal state. Strings are safe because they're immutable. public static void bump(Counter c, int n) { c.increment(); // affects the caller's object n++; // affects only the local n c = new Counter(\"other\"); // caller's variable still points to original c.increment(); // this increments the new local object only } Counter myC = new Counter(\"a\"); // count 0 int myN = 5; bump(myC, myN); System.out.println(myC.getCount()); // 1 System.out.println(myN); // 5 Draw the arrow. Parameter and argument arrows point to the same box; mutating the box is visible to both. Reassigning the parameter moves only the parameter's arrow. Primitives are just copied numbers. Java is always pass-by-value. The value of a reference variable is an address. So passing an object passes a copy of the address — the parameter points to the same object. The phrase \"pass by reference\" is misleading in Java; it's pass-by-value where the value is a reference. Consequence 1: a method can call mutators on a parameter object and the caller sees the changes. void bump(Counter c) { c.increment(); } — the caller's Counter is incremented. Consequence 2: a method cannot replace the caller's object. c = new Counter(); inside the method just repoints the local parameter. The caller's variable still points to the original. Arrays are objects , so passing an array lets the method modify its elements ( arr[0] = 5 is visible to the caller) but not replace the array ( arr = new int[10] is not). This is the basis for many Unit 4 questions. Strings are objects but immutable , so a method receiving a String can't change it — every String method makes a new String, and reassigning the parameter doesn't affect the caller. Strings behave like primitives for practical purposes. Returning a reference gives the caller access to the object. If a class returns a reference to its private mutable field (an ArrayList, say), the caller can modify the object's internals — an encapsulation leak. Returning a String or primitive is safe. FRQ 2 sometimes asks you to return a copy for this reason. Draw it. Boxes for objects, arrows for every variable and parameter. Mutation = change inside a box (visible everywhere). Reassignment = move one arrow (visible only there). Expecting reassignment to propagate. param = new Thing() changes only the parameter. Expecting a primitive parameter to change the caller. Never. Thinking a String parameter can be modified. Immutable. Reassigning it locally does nothing to the caller. Forgetting that array elements are shared. Modifying arr[i] in a method changes the caller's array. Consider the following method, where Score has a mutator add(int p) and accessor getPoints() . public static void update(Score s, int pts) { s.add(pts); pts = 0; } After Score sc = new Score(); int p = 10; update(sc, p); , what are sc.getPoints() and p ? 0 and 0 10 and 10 10 and 0 0 and 10 s and sc share the object, so add(10) is visible: 10 points. pts is a local copy; setting it to 0 doesn't change p. Consider the following. public static void reset(Counter c) { c = new Counter(\"new\"); } Counter x = new Counter(\"old\"); x.increment(); reset(x); System.out.println(x.getCount()); What is printed? 0 1 null A compile-time error reset only reassigns its local parameter. x still points to the original object with count 1. Pass by value: Java copies the argument's value; for objects that value is a reference Reference parameter: a parameter that holds the address of the caller's object",
  "ap-csa/3-7.html": "static members belong to the class, not to any single object. One copy, shared by everyone, accessible without creating an object. A class variable (static variable) is declared with static : private static int count; There is one copy shared by all objects of the class. Common uses: counting how many objects have been created, or constants: public static final double RATE = 0.05; A class method (static method) is declared with static and called on the class: ClassName.method() . It can be called without any object existing. Static methods cannot access instance variables or call instance methods directly — there's no object to refer to. They can access static variables. Instance methods can access static variables and methods. Static variables are initialized when the class is first loaded, before any object is created. Their default values are the same as instance variables. Access a static variable from an instance method or from outside (if public) via the class name: Student.count . public class Ticket { private static int nextId = 1; // shared by all tickets public static final double FEE = 2.50; // constant private int id; // each ticket has its own public Ticket() { id = nextId; nextId++; } public static int getNextId() { return nextId; } // OK: static uses static public int getId() { return id; } // public static int getId() { return id; } // ERROR: no object } Ticket a = new Ticket(); // id 1 Ticket b = new Ticket(); // id 2 System.out.println(Ticket.getNextId()); // 3 \"Which variable should be static?\" — anything that's the same for every object or tracks the class as a whole. \"Why won't this compile?\" — a static method touching an instance variable. Trace static variables as one shared cell, not one per object. static = belongs to the class. One copy for all objects, existing even if zero objects have been created. Non-static = belongs to each object. Uses for static variables: counters across all instances (how many Tickets sold), shared configuration, and constants ( public static final double TAX = 0.07; ). Constants are the most common on the exam. Static methods can't use this , can't access instance variables, and can't call instance methods without an object — because there may be no object. They can access static variables and call other static methods. main is static, which is why it needs to create objects to call instance methods. Instance methods can use static members freely. A getPrice() instance method can read a static TAX constant. The restriction is one-directional. Accessing static members: ClassName.member from anywhere it's visible. object.staticMember compiles but is misleading and the exam avoids it. Inside the class, bare name. Static initialization: static variables are set when the class loads, once. A static count = 0 then incremented in the constructor counts all objects ever created. Tracing static state: keep one row for the static variable, separate from any object's rows. Every object shares that row. Static method touching an instance variable. Compile error. The single most common static question. Thinking each object has its own copy of a static variable. One copy, shared. Using this in a static method. Compile error. Calling an instance method from main without an object. Compile error. Create an object first. A class Player has private static int numPlayers = 0; incremented in its constructor. After creating three Player objects, what is numPlayers ? 0 1 3 Each object has its own value One shared variable, incremented three times. Which of the following will not compile inside a class with private int size; and private static int total; ? public int getSize() { return size; } public static int getTotal() { return total; } public static int getSize() { return size; } public int getTotal() { return total; } A static method can't access an instance variable — there's no object. Class variable / static variable: a single variable shared by all objects of a class Class method / static method: a method called on the class that can't use instance variables Constant: a static final variable whose value never changes",
  "ap-csa/3-8.html": "Where a variable can be used depends on where it was declared. Local variables shadow instance variables, and blocks end scope. Instance variables are in scope throughout the class's methods and constructors. Local variables (declared inside a method, including parameters) exist only within the block where they're declared — from the declaration to the closing brace. A variable declared inside a loop body or if block is gone after that block. A for loop's header variable is gone after the loop. Shadowing: a local variable or parameter with the same name as an instance variable hides it. Inside that method, the name refers to the local. Use this.name (3.9) to reach the instance variable. Two local variables with the same name in the same scope is a compile error. In separate scopes (two different methods) it's fine. Access modifiers control visibility across classes: private = this class only, public = everywhere. A method's parameter is a local variable initialized by the argument. public class Demo { private int x = 10; // instance variable public void run(int x) // parameter shadows instance x { System.out.println(x); // prints the parameter int y = 5; if (x > 0) { int z = 1; // z only exists in this block y += z; } // System.out.println(z); // ERROR: z out of scope System.out.println(y); // 6 } } Find the closing brace of the block a variable was declared in — that's the end of its life. If a question uses a variable after that brace, it's a compile error. For shadowing, the closest declaration wins. Scope by declaration site: instance variable → the whole class. Method parameter → the whole method. Local variable → from its declaration to the end of the enclosing block { } . For-header variable → the loop only. Blocks nest scope: a variable declared in an if block is gone after the if. A variable declared in a loop body is recreated each iteration and gone after. Declaring a variable inside a loop and reading it after the loop is a compile error the exam uses often. Shadowing: when a local or parameter has the same name as an instance variable, the local wins inside its scope. The instance variable is hidden, not changed. this.name reaches it (3.9). No redeclaration in the same scope: int x = 1; int x = 2; in one method is a compile error. But two different methods can each have their own x , and a for loop can reuse i after a previous for loop ended. Access (public/private) is about visibility between classes . Scope is about visibility within code . Both determine whether a name can be used at a given point, but they're different rules. Lifetime follows scope: a local variable's memory is released when its block ends. Instance variables live as long as the object. Reading a question: for every variable use, find the declaration. If it's outside the current block or hasn't happened yet, compile error. Using a loop-body variable after the loop. Out of scope. Using a for-header variable after the loop. Out of scope. Assuming a shadowed instance variable was changed. The local was changed. The field wasn't. Redeclaring a variable in the same scope. Compile error. Which of the following will cause a compile-time error? for (int i = 0; i < 5; i++) { int sq = i * i; } int total = 0; for (int i = 0; i < 5; i++) { total += i; } System.out.println(total); for (int i = 0; i < 5; i++) { int sq = i * i; } System.out.println(sq); int n = 3; if (n > 0) { int m = n * 2; System.out.println(m); } sq is declared inside the loop body and is out of scope after it. A class has private int value = 8; and the method public int get(int value) { return value; } . What does obj.get(3) return? 8 3 11 A compile-time error The parameter shadows the instance variable, so value refers to the argument 3. Scope: the region of code in which a variable is accessible Local variable: a variable declared inside a method or block Shadowing: a local variable or parameter hiding an instance variable of the same name",
  "ap-csa/3-9.html": "this refers to the object the method was called on. It resolves shadowing and lets an object pass itself to other methods. Inside an instance method or constructor, this is a reference to the current object — the one the method was called on. this.name = name; assigns the parameter to the instance variable when they share a name. Without this , the line would assign the parameter to itself. this can be passed as an argument: other.compareTo(this) , or used to return the current object. this cannot be used in a static method — there's no current object. Calling a method with no object prefix inside a class is the same as this.method() . Using this is optional when there's no shadowing; it's only required to disambiguate. public class Book { private String title; private int pages; public Book(String title, int pages) { this.title = title; // instance ← parameter this.pages = pages; } public boolean isLongerThan(Book other) { return this.pages > other.pages; } } Without this. in the constructor, title = title; assigns the parameter to itself and the instance variable stays null. When a constructor's parameters share names with instance variables, look for this. on the left side. If it's missing, the instance variables are never set — a classic \"why does getTitle() return null\" question. this is an implicit reference variable available in every instance method and constructor. It points to the object the method was called on. a.getName() → inside getName, this is a . The main use: resolving shadowing in constructors and setters. this.name = name; assigns the parameter to the field. Without this , both sides refer to the parameter. Passing this: a method can hand the current object to another method: other.merge(this) , or list.add(this) . Useful when an object needs to register itself somewhere. Returning this enables chaining, though the exam doesn't test that pattern. Comparing with this: in an equals or comparison method, this.value == other.value compares the current object's field with the parameter object's field. Both are the same class, so the method can access other 's private fields directly. Static methods have no this — there's no object. Using it there is a compile error. Inside an instance method, a bare instance variable name is shorthand for this.name . The two are identical when there's no shadowing. Omitting this when the parameter shadows the field. Silent bug — the field stays default (null or 0). Using this in a static context. Compile error. Thinking this is a special variable you declare. It's provided automatically. Consider the constructor below. public Cat(String name) { name = name; } After Cat c = new Cat(\"Tom\"); , what does c.getName() return (where the getter returns the instance variable name )? \"Tom\" null \"\" A compile-time error The parameter shadows the instance variable; the assignment is parameter-to-parameter. The instance variable keeps its default, null. In which of the following can this not be used? An instance method A constructor A static method An accessor method Static methods have no current object, so this is meaningless there. this: a reference to the object on which the current method or constructor was invoked",
  "ap-csa/unit-4.html": "The biggest unit and the source of three of the four FRQs. Arrays, ArrayLists, 2D arrays, then searching, sorting, and recursion. If Unit 4 is solid, a 5 is realistic. Data sets are stored in arrays, ArrayLists, and 2D arrays, each with its own creation, access, and traversal rules. Standard algorithms — sum, max, count, filter, search, sort — are applied across all three structures. Text files are read with Scanner and File, and wrapper classes let numbers live in ArrayLists. Recursion is traced (not written), including recursive binary search and merge sort.",
  "ap-csa/4-1.html": "Before the technical content, the CED's second ethics topic: collecting data at scale has consequences. Programs today collect and process enormous amounts of data about people. Personal data can identify someone directly or in combination with other data. Privacy: people have a reasonable expectation about how their data is used. Collecting more than needed, keeping it longer than needed, or sharing it without consent violates that. Security: stored data can be breached; the more collected, the greater the harm from a breach. Bias in data: a dataset that under-represents some groups leads to programs that serve them worse. Analysis is only as fair as the data. Consent and transparency: users should know what is collected and why. Terms buried in fine print are legally compliant but ethically weak. Programmers are responsible for the systems they build — including anticipating misuse and designing to limit it. A school app records which students open which assignments and when. Useful for spotting who's struggling. But the same data could rank teachers, track students' late-night habits, or be sold to a tutoring company. Responsible design: collect only assignment-completion status, aggregate before sharing, tell students what's stored, and delete it at year end. Same rule as CSP's Big Idea 5: the correct answer minimizes collection, requires consent, considers who's harmed, and acknowledges bias. Absolute or one-sided options are distractors. This mirrors CSP's 5.5 and 5.6 but through a programmer's lens: you are writing the code that collects and processes the data, so the responsibility is yours. Personal data is anything that identifies a person alone or in combination. Names and IDs obviously; but ZIP + birthdate + gender identifies most people too. Combination is the trap. Privacy risks in code: storing more than needed, storing longer than needed, logging sensitive fields, sharing between systems without consent, weak access control (public fields, no validation). Each is a design decision, and each can be made better. Security risks: any stored data can be breached. The consequence scales with how much you stored. Minimization limits the damage. Bias in data sets: if a data set under-represents a group, statistics computed from it (averages, models) will be wrong for that group. Programs that make decisions from biased data make biased decisions — at scale, automatically. Checking representation before analyzing is part of responsible programming. Informed consent means people understand what's collected and why, and agreed. Buried terms of service technically count legally; the CED asks you to see that they don't count ethically. Transparency : users should be able to find out what data is held about them. Programs that make it impossible are designed badly. On the exam this is scenario questions: given a data-collection situation, which action is most responsible? Minimize, consent, anonymize, check for bias, secure it. Picking the maximal-collection option. \"Store everything in case it's useful\" is the wrong answer. Assuming anonymized data can't identify people. Combining fields re-identifies. The exam expects you to know this. Treating bias as a data-science-only problem. The program that uses the data inherits the bias. A program analyzes purchase histories to recommend products. Which of the following is the most significant ethical concern? The program may run slowly on large datasets. Users' personal data is being collected and used in ways they may not have agreed to or expect. The recommendations may use an ArrayList instead of an array. The program requires an import statement. Data use without informed consent is the core privacy concern. A dataset used to train a medical program includes very few records from older patients. Which outcome is most likely? The program will run faster for older patients. The program may perform less accurately for older patients due to bias in the data. The program will refuse to run. The program will automatically correct for the missing data. Under-representation in data produces worse performance for that group — bias. Personal data: information that identifies or could identify an individual Informed consent: users agreeing to data collection after understanding what and why",
  "ap-csa/4-2.html": "New in the revised course: a data set is a collection of related records, and programs process it as arrays, ArrayLists, or files. A data set is a collection of related data, often organized as records (one per item) with fields (attributes of each item) — like a spreadsheet's rows and columns. In Java, a data set is typically stored as an array or ArrayList of values, or of objects where each object is one record. Data may come from user input, a text file (4.6), or be built in code. Before analysis, data often needs cleaning : removing invalid or duplicate entries, standardizing formats, handling missing values. Common analyses: summary statistics (sum, average, max, min), filtering records that meet a condition, counting matches, sorting, and finding patterns. The structure of the data determines which collection to use: fixed-size known-length → array; growing/shrinking → ArrayList; grid → 2D array. // a data set of daily temperatures as an array double[] temps = {72.5, 68.0, 75.2, 71.8, 69.9}; double sum = 0; for (double t : temps) { sum += t; } double avg = sum / temps.length; // 71.48 // a data set of records as an ArrayList of objects ArrayList<Student> roster = new ArrayList<Student>(); roster.add(new Student(\"Ava\", 92)); roster.add(new Student(\"Ben\", 85)); Questions here are conceptual: which structure fits which data, and what step (cleaning, filtering, aggregating) a description refers to. The code for all of it comes in the following topics. A data set is structured: records (rows) with fields (columns). In Java, that's usually an array or ArrayList where each element is one record — either a primitive (one field per record) or an object (many fields per record). Choosing the structure: known fixed size → array. Size changes → ArrayList. Grid/table with rows and columns → 2D array. Records with multiple fields → array or ArrayList of objects of a class you define. Cleaning before analysis: consistent format (dates, capitalization, units), handle missing values (skip, default, or flag), remove duplicates, validate ranges. Without cleaning, an average with a stray −999 sentinel value is garbage. Transformation: computing new fields from existing ones — a BMI from height and weight, a total from unit price and quantity. Usually a loop that reads fields and writes a new value. Summary statistics (count, sum, mean, min, max, frequency) are the sum/count/max algorithms from Unit 2 applied to a data set. Filtering (records matching a condition) is a loop with an if. Sorting (4.15) enables binary search and ordered output. Data comes from user input (Scanner), files (4.6), or is hard-coded for testing. The processing code is the same regardless of source. The revised CED added this topic so that FRQ 3 (ArrayList data analysis) has a conceptual foundation: you're not just manipulating a list, you're analyzing a data set. Choosing an array when size is unknown. ArrayList. Analyzing before cleaning. Questions that show inconsistent data want cleaning first. Storing records as parallel arrays. An array of names and a separate array of scores is fragile. One object per record is the CED's model. A program must store the names of students who sign up for a club over the course of a semester; the number is not known in advance. Which structure is most appropriate? An int An array of fixed size An ArrayList A 2D array Unknown, changing size means ArrayList. A data set of survey responses contains some entries with a blank age field. Removing or replacing these entries before computing the average age is an example of which step? Sorting Data cleaning Encapsulation Recursion Handling invalid or missing values is data cleaning. Data set: a collection of related records, each with fields Record: one item in a data set, such as one row Data cleaning: fixing or removing invalid, duplicate, or missing data before analysis",
  "ap-csa/4-3.html": "Arrays are fixed-size, indexed from 0. Off-by-one and out-of-bounds are the whole story. Declare and create: int[] nums = new int[5]; — five ints, all initialized to 0 . double[] → 0.0, boolean[] → false, object arrays ( String[] ) → null . Initializer list: int[] nums = {4, 8, 15}; creates and fills in one step. Access: nums[i] . Indices run from 0 to nums.length - 1 . length is a field (no parentheses), unlike String's length() . Accessing index length or any negative index throws ArrayIndexOutOfBoundsException at run time. Array size is fixed once created. To \"grow\" you create a new, bigger array and copy. Arrays are objects: an array variable holds a reference. int[] b = a; makes b an alias of a — same array. An array of objects holds references; each element must be assigned an object before its methods are called, or you get a NullPointerException. int[] a = new int[4]; // {0, 0, 0, 0} a[0] = 7; a[3] = 2; a[a.length - 1] = 9; // last element → 9 System.out.println(a[2]); // 0 System.out.println(a.length);// 4 // a[4] = 1; // ArrayIndexOutOfBoundsException String[] words = new String[2]; // words[0].length(); // NullPointerException — element is null words[0] = \"hi\"; System.out.println(words[0].length()); // 2 Every array question: what's the first index (0), the last index (length − 1), and what's the default value? An index equal to length is the exam's favorite exception. And length without parentheses — with parentheses it won't compile. Two creation forms: new int[n] — n elements, all default; {a, b, c} — initializer, exactly those elements. You can't use an initializer after the declaration line ( arr = {1, 2}; won't compile); use arr = new int[]{1, 2}; . Defaults: 0, 0.0, false, null. An array of Strings or of your own class starts as all nulls; you must assign each element an object before calling methods on it. new Student[5] creates zero Students. length is a field: arr.length , no parentheses. (String's is a method: s.length() . ArrayList's is list.size() .) Mixing these is a compile-error question. Valid indices: 0 through length − 1. Index length is one past the end — the exam's favorite exception. Negative indices also throw. Arrays are objects. The variable holds a reference. int[] b = a; aliases; modifying b[0] modifies a[0] . To copy, make a new array and loop. Fixed size forever. To add an element you create a bigger array, copy everything, and switch the reference. This is what ArrayList does internally — which is why ArrayList exists. Array of arrays = 2D array (4.11). Each row is a 1D array object with its own length. Passing an array to a method passes the reference: the method can change elements, not replace the array (3.6). arr[arr.length]. Out of bounds. Last element is length − 1. arr.length(). Compile error. No parentheses for arrays. Calling a method on an element of a new object array. It's null. NullPointerException. Expecting int[] b = a; to copy. It aliases. Which of the following will cause a run-time exception? int[] arr = new int[6]; arr[0] = 5; arr[5] = 5; arr[6] = 5; arr[arr.length - 1] = 5; Valid indices are 0–5. Index 6 is out of bounds. What is printed by the following code? int[] x = {3, 6, 9}; int[] y = x; y[1] = 0; System.out.println(x[1]); 6 0 3 A compile-time error y is an alias of x; they're the same array. Changing y[1] changes x[1]. After double[] d = new double[3]; , what is the value of d[1] ? null 0.0 Undefined until assigned 1.0 Numeric arrays are initialized to zero. Array: a fixed-size, ordered collection of elements of one type Index: an element's position, from 0 to length - 1 length: the field giving the number of elements in an array ArrayIndexOutOfBoundsException: the run-time error from using an invalid index",
  "ap-csa/4-4.html": "Visiting every element. The standard for loop gives you the index; the enhanced for loop gives you the element. Know when each is appropriate. Standard traversal: for (int i = 0; i < arr.length; i++) with arr[i] . Use when you need the index — to modify elements, compare neighbors, or traverse backward or partially. Enhanced for (for-each): for (int x : arr) — x takes each element's value in order. Cleaner when you only read elements. In an enhanced for loop, assigning to the loop variable does not change the array — x is a copy of the element (for primitives). For objects, x is a copy of the reference, so calling mutators on it does affect the object in the array. Enhanced for can't skip elements, go backward, or access the index. Backward: for (int i = arr.length - 1; i >= 0; i--) . Loop bound i <= arr.length is the classic out-of-bounds bug. int[] nums = {2, 4, 6}; for (int n : nums) // enhanced: read only { n = n * 10; // does NOT change nums } // nums still {2, 4, 6} for (int i = 0; i < nums.length; i++) // standard: can modify { nums[i] = nums[i] * 10; } // nums now {20, 40, 60} If the question modifies the array through the loop variable of an enhanced for loop, the array is unchanged — that's the trick. If it calls a mutator method on an object element, the object is changed. Index needed? Standard loop. Standard for loop : index available, can go any direction, any step, can modify elements, can compare neighbors ( arr[i] and arr[i + 1] ), can stop early. It's the general tool. Enhanced for : for (type x : arr) — x takes each element's value in order. No index. Cleaner for read-only traversal. The type must match the element type (or a compatible one). The enhanced-for copy rule: for primitives, x is a copy — assigning to x doesn't touch the array. For objects, x is a copy of the reference — x.setValue(5) modifies the object in the array, but x = new Thing() doesn't replace the array element. Same as 3.6. Backward traversal must be a standard loop: for (int i = arr.length - 1; i >= 0; i--) . Note >= 0 to include index 0. Partial traversals: every other element ( i += 2 ), first half ( i < arr.length / 2 ), skipping the first ( i = 1 ). All need the standard loop. Bounds: i < arr.length is right. i <= arr.length throws on the last iteration. Comparing neighbors: i < arr.length - 1 . Nested traversal (comparing every pair): outer i over all, inner j from i + 1 to end. That's n(n−1)/2 comparisons. Modifying the enhanced-for variable and expecting the array to change. Primitives: no change. Objects: only via mutators. <= length. Exception on the last pass. Backward loop with > 0. Skips index 0. Using enhanced for when you need the index. Switch to standard. What is printed by the following code? int[] a = {1, 2, 3}; for (int v : a) { v += 10; } System.out.println(a[0] + a[1] + a[2]); 6 36 16 A compile-time error v is a copy; modifying it leaves the array unchanged. 1 + 2 + 3 = 6. Which of the following correctly prints the elements of arr in reverse order? for (int i = arr.length; i > 0; i--) System.out.println(arr[i]); for (int i = arr.length - 1; i >= 0; i--) System.out.println(arr[i]); for (int x : arr) System.out.println(x); for (int i = arr.length - 1; i > 0; i--) System.out.println(arr[i]); Start at length - 1, go down to and including 0. A starts out of bounds; D skips index 0. Traversal: visiting every element of an array Enhanced for loop: for (type x : arr) — iterates over elements without an index",
  "ap-csa/4-5.html": "The CED names the array algorithms you should be able to write. These are FRQ building blocks — practice until they're reflexive. Named algorithms: find min/max; compute sum/average; count elements meeting a condition; determine if any/all elements satisfy a condition; find the mode (most frequent); check for duplicates; reverse the array; shift or rotate elements; check whether an array is sorted; find consecutive pairs. Any/all: \"any\" loops and returns true at the first match, false after the loop. \"All\" returns false at the first failure, true after. Reverse in place: swap arr[i] with arr[arr.length - 1 - i] for i from 0 to length/2. Shift left by one: arr[i] = arr[i + 1] for i from 0 to length − 2, then handle the last element. Direction of the loop matters to avoid overwriting. Consecutive elements: compare arr[i] and arr[i + 1] with the loop ending at length - 1 (not length). Duplicates: nested loop comparing each pair (i, j with j > i). // are all elements positive? public static boolean allPositive(int[] a) { for (int x : a) { if (x <= 0) return false; } return true; } // count adjacent equal pairs int pairs = 0; for (int i = 0; i < a.length - 1; i++) { if (a[i] == a[i + 1]) pairs++; } // reverse in place for (int i = 0; i < a.length / 2; i++) { int tmp = a[i]; a[i] = a[a.length - 1 - i]; a[a.length - 1 - i] = tmp; } Any algorithm touching arr[i + 1] must stop at length - 1 ; touching arr[i - 1] must start at 1. For \"any\" vs \"all,\" the return inside the loop is the opposite of the return after it. The CED's named array algorithms: min/max, sum/average, count with property, any/all with property, mode (most frequent), duplicates present?, reverse, shift elements left/right, rotate, is-sorted?, consecutive pairs with property. Each has a canonical shape. Any: loop, return true at first match, return false after. All: loop, return false at first failure, return true after. The two returns are opposites of each other. \"None\" is !any . Mode: for each element, count how many times it appears (nested loop or a count array if values are in a known range); track the element with the highest count. Duplicates: nested loops i, j with j > i; if arr[i] == arr[j] , duplicate found. Or sort first and check neighbors. Reverse in place: swap positions i and length−1−i for i from 0 to length/2 (exclusive). Going to length would swap everything back. Shift left by one: for (i = 0; i < length - 1; i++) arr[i] = arr[i + 1]; then set the last element (to 0, or to the saved first element for a rotation). Shift right: loop backward from length−1 down to 1: arr[i] = arr[i - 1] . Direction matters: going forward while shifting right overwrites before copying. Is sorted: for i from 0 to length−2, if arr[i] > arr[i + 1] , return false. Return true after. Consecutive pairs: any comparison of arr[i] with arr[i + 1] loops i to length - 2 (i.e., i < length - 1 ). Building a new array: when the result size differs from the input, first count how many elements qualify, then create an array of that size, then fill it with a separate index. Any/all with an else-return inside the loop. Exits on the first element. Only one return in the loop. Reversing with a full-length loop. Swaps everything twice. Stop at length/2. Shifting right with a forward loop. Copies the first element into every position. Go backward. Neighbor comparison to length. arr[i + 1] throws on the last i. Stop at length − 1. What does the following method return for {3, 3, 5, 5, 5, 2} ? public static int count(int[] a) { int c = 0; for (int i = 1; i < a.length; i++) { if (a[i] == a[i - 1]) c++; } return c; } 2 3 4 5 Pairs (3,3), (5,5), (5,5) match → 3. The following method is intended to return true if any element of arr is negative. public static boolean hasNeg(int[] arr) { for (int x : arr) { if (x < 0) return true; else return false; } return false; } Which best describes the error? It will not compile. It only checks the first element, because the else returns immediately. It throws an exception on an empty array. It works correctly. The else-return exits on the first element regardless. Remove the else so the loop continues. In-place: modifying the array itself rather than building a new one Mode: the value that appears most often",
  "ap-csa/4-6.html": "New in the revised course. Reading a text file uses the same Scanner methods as keyboard input, plus a File object and one exception you must declare. Open a file for reading: Scanner in = new Scanner(new File(\"data.txt\")); . Both File (java.io) and Scanner (java.util) need imports. Creating a Scanner on a File can throw FileNotFoundException (a checked exception). The method must declare throws FileNotFoundException in its header. Read line by line: while (in.hasNextLine()) { String line = in.nextLine(); ... } . Read tokens: while (in.hasNext()) with next() , or hasNextInt() with nextInt() . Close the file when done: in.close(); Lines of text often need parsing: Integer.parseInt(str) and Double.parseDouble(str) convert String tokens to numbers. The same nextInt/nextLine buffer trap from 1.4 applies to files. A common pattern: read each line into an ArrayList so the data can be processed after the file is closed. import java.io.File; import java.io.FileNotFoundException; import java.util.Scanner; import java.util.ArrayList; public static ArrayList<Integer> readScores(String filename) throws FileNotFoundException { Scanner in = new Scanner(new File(filename)); ArrayList<Integer> scores = new ArrayList<Integer>(); while (in.hasNextInt()) { scores.add(in.nextInt()); } in.close(); return scores; } Three things the exam checks: the throws FileNotFoundException clause, the hasNext… / next… pair matching (hasNextLine with nextLine, hasNextInt with nextInt), and closing the Scanner. If a method opens a file and has no throws clause, it won't compile. The pattern: Scanner in = new Scanner(new File(\"name.txt\")); . File represents the path; Scanner reads from it using the exact same methods as keyboard input. Both need imports: java.io.File and java.util.Scanner . FileNotFoundException is a checked exception — the compiler requires you to acknowledge it. Since you won't write try/catch on the exam, the acknowledgment is throws FileNotFoundException in the method header (import java.io.FileNotFoundException ). Forget it → compile error. Reading loops: pair the check with the read. hasNextLine() ↔ nextLine() ; hasNext() ↔ next() ; hasNextInt() ↔ nextInt() . Mismatching (hasNext with nextLine) can miss data or throw on trailing whitespace. Parsing: file data is text. Integer.parseInt(token) and Double.parseDouble(token) convert. If a line has several values separated by spaces, next() reads them one at a time. The nextInt/nextLine trap applies to files exactly as to keyboard input (1.4). Close the Scanner when done: in.close() . Releases the file. The exam expects it. Typical structure: open → loop reading into an ArrayList → close → process the list. Separating reading from processing keeps the file open briefly and makes the processing code testable. This topic is new in the 2025–26 CED. Expect it to be tested — it's a signal the College Board wanted students to handle real data. No throws clause. Compile error. Every method that opens a file needs it (or the method that calls it does). Mismatched has/next. while (hasNext()) with nextLine() can behave unexpectedly on blank lines. Reading a number with nextLine and not parsing. You get a String. Parse it. Forgetting the imports. Three of them: File, FileNotFoundException, Scanner. A method creates new Scanner(new File(\"in.txt\")) . Which of the following must be true for the code to compile? The file must exist at compile time. The method must declare throws FileNotFoundException . The Scanner must be static. The file must contain only integers. FileNotFoundException is a checked exception; the method header must declare it. Which loop correctly reads every line of a text file using a Scanner sc ? while (sc.hasNext()) { String s = sc.nextLine(); } while (sc.hasNextLine()) { String s = sc.nextLine(); } while (sc.nextLine() != null) { } for (int i = 0; i < sc.length(); i++) { String s = sc.nextLine(); } hasNextLine pairs with nextLine. Option A can throw on a trailing blank line; C throws at end of file; D doesn't compile. File: a class representing a file on disk, passed to a Scanner to read it FileNotFoundException: a checked exception thrown when a file can't be opened; must be declared hasNextLine(): returns true if another line remains to be read",
  "ap-csa/4-7.html": "ArrayLists can't hold primitives, so Java wraps them in objects. Autoboxing makes this mostly invisible — except in the spots the exam tests. Integer and Double are wrapper classes : objects that hold one int or double. Needed because ArrayList<int> is illegal; you write ArrayList<Integer> . Autoboxing: Java automatically converts int → Integer when needed ( list.add(5) ). Unboxing: Integer → int ( int x = list.get(0) ). Integer.MAX_VALUE and Integer.MIN_VALUE — the int range limits, on the reference sheet. Useful as initial values for min/max algorithms. Integer.parseInt(String) and Double.parseDouble(String) convert text to numbers — essential for file input. Comparing two Integer objects with == compares references and is unreliable; use .equals() or unbox to ints first. Unboxing a null Integer throws a NullPointerException. Arithmetic on Integers unboxes automatically: Integer a = 5; int b = a + 3; works. ArrayList<Integer> nums = new ArrayList<Integer>(); nums.add(7); // autobox int → Integer int first = nums.get(0); // unbox Integer → int Integer big = Integer.MAX_VALUE; int n = Integer.parseInt(\"42\"); // 42 double d = Double.parseDouble(\"3.5\"); int min = Integer.MAX_VALUE; // start high for a min search for (int x : nums) { if (x < min) min = x; } You can't write ArrayList<int> — that's a guaranteed compile-error question. Everywhere else, treat Integer like int and let autoboxing work, except: never compare Integers with == , and remember a null Integer crashes when unboxed. Why wrappers exist: generics ( ArrayList<T> ) only work with reference types. int is a primitive. Integer is a class whose objects hold one int. So ArrayList<Integer> is the way to store ints. Autoboxing (int → Integer) happens automatically on add , on assignment to an Integer variable, on passing an int where an Integer is expected. Unboxing (Integer → int) happens on assignment to an int, in arithmetic, in comparisons with < > . You rarely write the conversion. The == trap: Integer a = 1000, b = 1000; a == b is false (different objects). For small values (−128 to 127) Java caches, so it's true — which makes the bug intermittent. Use .equals or unbox to int. The exam avoids relying on caching. Null unboxing: Integer x = null; int y = x; throws NullPointerException. An ArrayList slot that was never set is null; unboxing it crashes. Integer.MAX_VALUE / MIN_VALUE : the int range. Standard initial values for min/max searches — guaranteed to be replaced by any real value. Integer.parseInt(\"42\") → 42. Double.parseDouble(\"3.5\") → 3.5. Non-numeric input throws NumberFormatException . These are how file and keyboard text become numbers. Arithmetic on Integers auto-unboxes: Integer a = 5; a + 3 is 8 (an int). list.get(0) + list.get(1) adds two unboxed ints. ArrayList<int>. Compile error. Guaranteed question. == on Integer objects. Unreliable. Unbox or use equals. Unboxing null. NPE. Starting a min search at 0. Use Integer.MAX_VALUE or the first element. Which of the following declarations compiles? ArrayList<int> a = new ArrayList<int>(); ArrayList<Integer> a = new ArrayList<Integer>(); ArrayList<double> a = new ArrayList<double>(); ArrayList a = new ArrayList<int>(); Generic type parameters must be classes; Integer is the wrapper for int. A method finds the largest value in a list of integers by starting with int max = 0; . For which input does this produce a wrong answer, and what initial value fixes it? Lists containing zero; use 1 Lists of all negative values; use Integer.MIN_VALUE Lists of all positive values; use Integer.MAX_VALUE It is always correct If every value is negative, none exceed 0. Starting at Integer.MIN_VALUE (or the first element) fixes it. Wrapper class: an object type (Integer, Double) that holds a primitive value Autoboxing: automatic conversion from a primitive to its wrapper Unboxing: automatic conversion from a wrapper to its primitive Integer.parseInt: converts a String to an int",
  "ap-csa/4-8.html": "A resizable list. Six methods on the reference sheet, and the index-shifting behavior of add and remove is where questions come from. Create: ArrayList<String> names = new ArrayList<String>(); (empty, size 0). Import java.util.ArrayList . size() — number of elements (a method, with parentheses — unlike array length ). add(obj) — appends to the end, returns true. add(index, obj) — inserts at index, shifting later elements right ; size grows by 1. get(index) — returns the element. set(index, obj) — replaces the element at index, returns the old value; size unchanged. remove(index) — removes and returns the element at index, shifting later elements left ; size shrinks by 1. Valid indices are 0 to size() - 1 . get(size()) throws IndexOutOfBoundsException . add(size(), obj) is legal (appends). ArrayList holds objects only — use wrapper types for numbers (4.7). ArrayList<String> a = new ArrayList<String>(); a.add(\"x\"); // [x] a.add(\"y\"); // [x, y] a.add(1, \"m\"); // [x, m, y] inserted, y shifted right a.set(0, \"z\"); // [z, m, y] replaced String r = a.remove(1); // [z, y] r = \"m\" System.out.println(a.size()); // 2 System.out.println(a.get(1)); // y // a.get(2); // IndexOutOfBoundsException Rewrite the list in brackets after every call. set doesn't change size; add(i, x) and remove(i) do, and they shift everything after i. remove and set both return the element that was there — questions sometimes use that return value. Declaration: ArrayList<Type> name = new ArrayList<Type>(); . The type parameter is required for the exam. new ArrayList<>() (diamond) is also fine. Type must be a class: String, Integer, Double, your own class. The six methods on the reference sheet: int size() ; boolean add(E obj) (appends); void add(int index, E obj) (inserts, shifts right); E get(int index) ; E set(int index, E obj) (replaces, returns old); E remove(int index) (removes, returns removed, shifts left). Index rules: get/set/remove need 0 ≤ index < size. add(index, obj) allows 0 ≤ index ≤ size — index == size appends. get(size()) throws IndexOutOfBoundsException . Return values matter: remove and set return the element that was there. Questions use this: String s = list.remove(0); or if (list.set(1, \"x\").equals(\"y\")) . Size changes: add (either form) +1, remove −1, set 0, get 0. Track size when tracing. Printing an ArrayList shows [a, b, c] — brackets, commas, spaces. Printing an array shows a memory address. The exam prints ArrayLists directly. ArrayList vs array: resizable vs fixed; size() vs length ; get(i) vs [i] ; objects only vs any type. Same 0-based indexing. Not rewriting the list after add/remove. Indices shift. Write it out. get(size()). One past the end. Exception. Using [] on an ArrayList. Compile error. get and set. Ignoring set's/remove's return value when the question uses it. What is printed by the following code? ArrayList<Integer> list = new ArrayList<Integer>(); list.add(10); list.add(20); list.add(30); list.add(1, 15); list.remove(2); System.out.println(list); [10, 15, 30] [10, 15, 20] [10, 20, 30] [15, 20, 30] After add(1, 15): [10, 15, 20, 30]. remove(2) removes 20: [10, 15, 30]. Given ArrayList<String> w with 4 elements, which of the following throws an exception? w.add(4, \"e\") w.set(3, \"e\") w.get(4) w.remove(3) Valid get indices are 0–3. add(4, …) is legal because 4 == size (appends). What does list.set(2, \"new\") return, where list is [a, b, c, d] ? \"new\" \"c\" 2 true set returns the element that was replaced. ArrayList: a resizable list of objects size(): the number of elements in an ArrayList add(index, obj): inserts, shifting later elements right remove(index): removes and returns, shifting later elements left set(index, obj): replaces and returns the old element; size unchanged",
  "ap-csa/4-9.html": "Same two loop styles as arrays, with one extra hazard: modifying the list's size while looping over it. Standard: for (int i = 0; i < list.size(); i++) with list.get(i) . Enhanced: for (String s : list) . Removing during a forward loop skips elements. After remove(i) , the next element shifts into index i, then i++ jumps past it. Fixes: after removing, do i-- (or don't increment); or traverse backward with for (int i = list.size() - 1; i >= 0; i--) , where removals don't affect unvisited indices. Never add or remove inside an enhanced for loop — it throws ConcurrentModificationException . Enhanced for over an ArrayList of objects lets you call mutators on each element (the reference is shared); over ArrayList<Integer> , assigning to the loop variable does nothing to the list. The loop condition i < list.size() is re-evaluated each iteration, so a shrinking list shortens the loop. ArrayList<Integer> a = new ArrayList<Integer>(); // a = [4, 4, 7, 4] // BUGGY: remove all 4s for (int i = 0; i < a.size(); i++) { if (a.get(i) == 4) a.remove(i); } // result: [4, 7] — second 4 was skipped // CORRECT: backward for (int i = a.size() - 1; i >= 0; i--) { if (a.get(i) == 4) a.remove(i); } // result: [7] If a question removes elements in a forward loop, trace it literally — the answer usually has \"leftover\" elements that were skipped. If asked for the correct version, pick the backward loop or the one with i-- after remove. The removal bug, step by step: list [2, 2, 5] , remove all 2s with a forward loop. i=0: get(0)=2, remove → [2, 5] . i=1: get(1)=5, no. Done. Result [2, 5] — the second 2 shifted into index 0 and was never examined. Fix A — backward: for (int i = list.size() - 1; i >= 0; i--) . Removing at i shifts elements after i, which you've already visited. Unvisited elements (before i) keep their indices. Fix B — don't advance after removing: use a while loop with manual increment, or i-- right after remove(i) in a for loop. Either way, the shifted-in element is re-examined. Enhanced for + structural change = ConcurrentModificationException. Adding or removing inside for (T x : list) throws at run time (not compile time). Calling mutators on the elements is fine; changing the list's size is not. Adding in a forward loop: inserting at i pushes the current element to i+1, so i++ lands on it again — potential infinite loop if you insert every time. Insert, then i++ extra to skip. size() is re-evaluated each iteration. A shrinking list ends the loop sooner; a growing list extends it. This is different from a for loop over an array, where length is fixed. Object elements: for (Student s : roster) s.setGrade(…) changes the objects in the list — the reference is shared. for (Integer n : nums) n = 0; changes nothing — n is a copy. Forward-loop removal. Skips the element after each removal. The exam asks for the buggy output; trace it literally. Removing inside an enhanced for. Run-time exception. Caching size() before the loop. If the list shrinks, you go out of bounds. What is the contents of list after the following code, where list starts as [1, 2, 2, 3, 2] ? for (int i = 0; i < list.size(); i++) { if (list.get(i) == 2) { list.remove(i); } } [1, 3] [1, 2, 3] [1, 2, 3, 2] [1, 3, 2] i=1 removes the first 2 → [1, 2, 3, 2]; i=2 is 3 (the second 2 shifted to index 1 and was skipped); i=3 removes the last 2 → [1, 2, 3]. Which of the following correctly removes every element equal to target from an ArrayList<Integer> nums ? for (Integer n : nums) { if (n == target) nums.remove(n); } for (int i = nums.size() - 1; i >= 0; i--) { if (nums.get(i) == target) nums.remove(i); } for (int i = 0; i < nums.size(); i++) { if (nums.get(i) == target) nums.remove(i); } for (int i = 0; i <= nums.size(); i++) { if (nums.get(i) == target) nums.remove(i); } Backward traversal handles removals safely. A throws ConcurrentModificationException; C skips adjacent matches; D goes out of bounds. ConcurrentModificationException: the error from modifying an ArrayList inside an enhanced for loop Backward traversal: looping from size - 1 down to 0, safe for removals",
  "ap-csa/4-10.html": "The same algorithms as arrays, in ArrayList syntax, plus the ones that only make sense with a resizable list: insert in order, remove matches, build a filtered list. All the array algorithms (min/max, sum, count, any/all, duplicates, reverse) apply — swap arr[i] for list.get(i) , length for size() . Filter into a new list: create an empty ArrayList, traverse the original, add matches. The original is unchanged. Insert in sorted position: traverse until you find an element bigger than the new one, add(i, value) there; if none, add(value) at the end. Remove all matches: backward traversal with remove(i) (4.9). Traversing a list of objects: call accessors on each element ( list.get(i).getName() ). This is FRQ 3's shape: a class is given, and you process an ArrayList of its objects. Returning a new list vs. modifying the parameter: read the prompt. \"Returns a list of…\" → build and return a new one. \"Removes from the list…\" → mutate the parameter. // FRQ 3-style: return names of students with gpa above a threshold public static ArrayList<String> honorRoll(ArrayList<Student> roster, double min) { ArrayList<String> result = new ArrayList<String>(); for (Student s : roster) { if (s.getGpa() >= min) { result.add(s.getName()); } } return result; } // insert keeping ascending order public static void insertSorted(ArrayList<Integer> list, int v) { for (int i = 0; i < list.size(); i++) { if (v < list.get(i)) { list.add(i, v); return; } } list.add(v); } FRQ 3 rubrics typically award: correct loop over the list, correct accessor call on each element, correct condition, correct add/remove/return. Write the enhanced for loop first, then the if, then what happens inside. Don't forget to return the new list. Translation from arrays: arr.length → list.size() ; arr[i] → list.get(i) ; arr[i] = x → list.set(i, x) . Everything from 4.5 carries over. New with ArrayLists: insert in sorted position ( add(i, x) at the first i where x < get(i) , else append), remove all matches (backward loop), build a filtered result list (new ArrayList, add matches), merge two lists. FRQ 3 shape: you're given a class (say Song with getTitle() , getDuration() ) and asked to write methods over an ArrayList<Song> : find the longest, count those over a threshold, return a list of titles, remove those matching a condition. Every one is a loop + accessor calls + accumulator or result list. Return a new list vs. modify in place: read the verb. \"Returns a list of…\" → build and return new. \"Removes from…\" or \"updates…\" → modify the parameter, return void. Doing the wrong one earns zero on that part. Accessor calls on elements: list.get(i).getTitle() — get the element, then call its method. With an enhanced for, s.getTitle() . Chaining is normal. Comparing elements for max/min: the comparison uses a field: if (s.getDuration() > longest.getDuration()) longest = s; . Initialize longest to list.get(0) . Rubric reality: points typically go to the loop over all elements, the correct accessor call, the correct condition, the correct action (add/remove/update), and the correct return. Each independently. A structurally correct answer with one wrong comparison still scores most points. Modifying when asked to return new (or vice versa). Read the prompt's verb. Not returning the result list. Built it, forgot return result; . Comparing objects with == or <. Compare their fields via accessors. Forward-loop removal (again). Backward. What does the following method return for the list [5, 2, 8, 2, 9] ? public static int f(ArrayList<Integer> list) { int r = list.get(0); for (int i = 1; i < list.size(); i++) { if (list.get(i) < r) r = list.get(i); } return r; } 9 5 2 26 This is the min pattern. The smallest value is 2. A method should return a new ArrayList containing only the even values from nums , leaving nums unchanged. Which approach is correct? Traverse nums backward, removing odd values, and return nums . Create an empty ArrayList, add each even value from nums to it, and return the new list. Use set to replace odd values with 0 and return nums . Sort nums and return it. \"New list\" and \"unchanged original\" mean build and return a separate list. Filter: building a new list from elements that meet a condition Insert in order: adding an element at the position that keeps the list sorted",
  "ap-csa/4-11.html": "A 2D array is an array of arrays. Row first, then column — and each row is itself a 1D array you can pass around. Declare and create: int[][] grid = new int[3][4]; — 3 rows, 4 columns, all 0. grid.length is the number of rows ; grid[0].length is the number of columns . Initializer: int[][] g = {{1, 2, 3}, {4, 5, 6}}; — 2 rows, 3 columns. Access: grid[row][col] . grid[1][2] is row 1, column 2 (both from 0). grid[r] by itself is a 1D array — row r. You can pass it to a method that takes int[] . On the AP exam, 2D arrays are rectangular — every row has the same length. (Java allows ragged arrays, but they aren't tested.) Default values follow 1D rules: 0, 0.0, false, null. Out of bounds in either dimension throws ArrayIndexOutOfBoundsException. int[][] m = {{1, 2, 3}, {4, 5, 6}}; System.out.println(m.length); // 2 (rows) System.out.println(m[0].length); // 3 (columns) System.out.println(m[1][2]); // 6 m[0][1] = 9; // row 0 becomes {1, 9, 3} int[] secondRow = m[1]; // {4, 5, 6} // m[2][0] // exception: no row 2 Read m[a][b] as \"row a, column b\" every time — never reverse it. length counts rows; [0].length counts columns. A question that flips these is the standard distractor. int[][] g = new int[R][C] creates R row-arrays each of length C. g is an array of references to those rows. g.length = R. g[r].length = C for every r (rectangular). Initializer: {{1, 2}, {3, 4}, {5, 6}} — outer braces list rows, inner braces list each row's values. Count outer groups for rows, inner elements for columns. Access g[r][c] : first bracket row, second column. Always. The exam never varies this convention. g[r] is a 1D array — a real object you can pass to a method taking int[] , iterate with enhanced for, or alias with int[] row = g[r]; (changes to row[i] affect g[r][i] ). Defaults as for 1D. A String[][] is all null. Ragged arrays (rows of different lengths) are possible in Java but excluded from the CED. Assume rectangular; use g[0].length for column count. Out of bounds in either dimension throws. g[R][0] — no row R. g[0][C] — no column C. Mental model: a table. Rows go down, columns go across. Row index first because you pick the row, then the position within it. Swapping row and column. g[c][r] when you mean g[r][c] . Read as row-then-column every time. g.length for columns. It's rows. Columns is g[0].length . Counting inner elements as rows. In the initializer, outer braces = rows. After double[][] t = new double[4][6]; , what are t.length and t[0].length ? 4 and 6 6 and 4 24 and 1 4 and 4 First dimension is rows (4), second is columns (6). What is printed by the following code? int[][] a = {{2, 4}, {6, 8}, {10, 12}}; System.out.println(a[2][0] + a[0][1]); 14 12 16 8 a[2][0] is 10 (row 2, col 0); a[0][1] is 4. 10 + 4 = 14. 2D array: an array whose elements are arrays; rows of columns Row-major: the convention that the first index is the row Rectangular array: a 2D array where every row has the same length",
  "ap-csa/4-12.html": "Nested loops visit every cell. Which loop is outer determines the order — and the exam asks about that order. Row-major traversal: outer loop over rows, inner over columns. Visits row 0 left to right, then row 1, etc. This is the default. Column-major traversal: outer loop over columns, inner over rows. Visits column 0 top to bottom, then column 1. Nested enhanced for: for (int[] row : grid) { for (int v : row) { ... } } — row-major, read only. Use grid.length for the row bound and grid[0].length (or grid[r].length ) for the column bound. Partial traversals: a single row ( grid[r][c] for c), a single column ( grid[r][c] for r), the diagonal ( grid[i][i] ), or a sub-region. Traversal order matters when output is printed in sequence or when elements are appended to a list. int[][] g = {{1, 2, 3}, {4, 5, 6}}; // row-major: 1 2 3 4 5 6 for (int r = 0; r < g.length; r++) { for (int c = 0; c < g[0].length; c++) { System.out.print(g[r][c] + \" \"); } } // column-major: 1 4 2 5 3 6 for (int c = 0; c < g[0].length; c++) { for (int r = 0; r < g.length; r++) { System.out.print(g[r][c] + \" \"); } } Look at which index the outer loop controls. Outer over r → row-major. Outer over c → column-major. When asked for the printed output, write out the grid and read it in that order. Row-major (outer r, inner c) visits row 0 fully, then row 1, etc. Reading order for English text. Column-major (outer c, inner r) visits column 0 top to bottom, then column 1. The loop nesting order determines which. Nested enhanced for: for (int[] row : g) for (int v : row) — row-major only, read-only. To modify cells you need indices. Single row: for (int c = 0; c < g[r].length; c++) g[r][c] . Single column: for (int r = 0; r < g.length; r++) g[r][c] . There's no g[][c] — a column has to be assembled by looping rows. Diagonals: main diagonal g[i][i] ; anti-diagonal g[i][n - 1 - i] for a square grid. Bounds: row bound is g.length , column bound g[0].length (or g[r].length ). Swapping them works only for square grids and fails otherwise — the exam uses non-square grids to catch it. Output order questions: write the grid, then read it in the traversal order (across rows or down columns). Trust the loop nesting, not the variable names. Traversal with a running index: when copying between a 1D array and a 2D array, keep a separate counter k that increments once per cell. Assuming the outer loop is always rows. Check which variable it controls. Swapping bounds on a non-square grid. Exception or missed cells. Trying to grab a column as an array. No such thing. Loop rows. What is printed by the following code? int[][] m = {{1, 2}, {3, 4}, {5, 6}}; for (int c = 0; c < m[0].length; c++) { for (int r = 0; r < m.length; r++) { System.out.print(m[r][c]); } } 123456 135246 246135 654321 Column-major: column 0 (1, 3, 5) then column 1 (2, 4, 6). Which of the following computes the sum of the elements on the main diagonal of a square 2D array sq ? for (int i = 0; i < sq.length; i++) sum += sq[i][i]; for (int i = 0; i < sq.length; i++) sum += sq[i][0]; for (int i = 0; i < sq.length; i++) sum += sq[0][i]; for (int[] row : sq) sum += row[0]; The main diagonal is where row == column: sq[i][i]. Row-major traversal: outer loop over rows, inner over columns Column-major traversal: outer loop over columns, inner over rows",
  "ap-csa/4-13.html": "FRQ 4 is always a 2D array question. The algorithms are the 1D ones applied across rows, columns, or the whole grid. Named algorithms: sum/max/min of the whole grid, of a row, or of a column; count cells meeting a condition; find a value's position; check whether every element in a row/column meets a condition; find rows/columns with a property. Row operations: a method that takes int[] can be called with grid[r] . Reusing a 1D helper is common in FRQ 4. Column operations: there's no direct column array; loop over r with fixed c. Building a 2D array from a 1D array or list: fill in row-major (or column-major, if specified) order using a running index into the source. Searching: nested loops; return as soon as found (a return exits both loops). Modifying in place: nested loops with standard indices, assigning to grid[r][c] . // FRQ 4-style: return the index of the row with the largest sum public static int maxRow(int[][] g) { int best = 0; int bestSum = rowSum(g[0]); for (int r = 1; r < g.length; r++) { int s = rowSum(g[r]); if (s > bestSum) { bestSum = s; best = r; } } return best; } public static int rowSum(int[] row) { int sum = 0; for (int v : row) sum += v; return sum; } // fill a 2D array from a 1D array in row-major order int k = 0; for (int r = 0; r < g.length; r++) { for (int c = 0; c < g[0].length; c++) { g[r][c] = source[k]; k++; } } FRQ 4 part (a) is usually a 1D-style helper on one row or column; part (b) uses it across the grid. Write the nested loops with r and c named clearly and keep row bound = length , column bound = [0].length . If a return inside nested loops is needed, remember it exits everything. The CED's named 2D algorithms: sum/max/min/count over the whole grid, over a row, or over a column; find a value's location; determine whether a row or column has a property; find the row/column with the largest sum; count occurrences; transform every element. Reuse 1D helpers: if you have rowSum(int[] row) , then the grid's max-row is a loop over r calling rowSum(g[r]) . FRQ 4 part (a) is often the helper; part (b) uses it across the grid. Using your part (a) in part (b) is expected — and if you got (a) wrong, (b) is still graded assuming (a) works. Column operations have no helper shortcut — write the loop over r with fixed c inside a loop over c. Search with early return: nested loops; return when found exits both. Returning a position needs both indices — FRQ 4 may give you a Location class or ask for a formatted String. Filling from a 1D source: running index k; assign g[r][c] = src[k]; k++; in row-major order (or column-major if specified). k goes 0 to R×C−1. In-place transformation: nested standard loops with assignment. Enhanced for can't do this for primitives. Neighbor checks (is a cell surrounded by…): guard each neighbor's indices with bounds checks. Corners have 3 neighbors, edges 5, interior 8. Rubric reality for FRQ 4: points for correct nested loops with correct bounds, correct access g[r][c] , correct condition/accumulation, correct return. Get the loop structure right and you've earned most of it. Not reusing part (a). Rewriting it wastes time and invites errors. Treating a column like a row. Fixed c, loop r. Resetting the running index inside the outer loop. k continues across rows. Neighbor access without bounds checks. Exception at the edges. What does the following method return for {{1, 5}, {3, 2}, {4, 4}} ? public static int count(int[][] g) { int c = 0; for (int r = 0; r < g.length; r++) { if (g[r][0] < g[r][1]) c++; } return c; } 0 1 2 3 Rows where the first is less than the second: {1,5} yes; {3,2} no; {4,4} no. Count = 1. Which of the following correctly computes the sum of column c of a 2D array m ? for (int i = 0; i < m[0].length; i++) sum += m[c][i]; for (int i = 0; i < m.length; i++) sum += m[i][c]; for (int v : m[c]) sum += v; for (int i = 0; i < m.length; i++) sum += m[c][i]; A column means fixed c and varying row index i from 0 to m.length - 1. Options A, C, D all treat c as a row. Helper method: a method that handles one piece (like a single row) and is called from a larger algorithm",
  "ap-csa/4-14.html": "Two searches: linear works on anything, binary needs sorted data and is much faster. The exam counts comparisons. Linear (sequential) search: check each element from the start until found or the end is reached. Returns the index or −1. Works on any array or ArrayList. Linear search worst case: n comparisons (element absent or last). Binary search: requires a sorted array. Compare the target to the middle; if equal, done; if smaller, search the left half; if larger, the right half. Repeat until found or the range is empty. Binary search implementation uses low , high , and mid = (low + high) / 2 ; adjust high = mid - 1 or low = mid + 1 ; loop while low <= high . Binary search worst case: about log₂ n comparisons — for 1,000 elements, about 10. The exam asks \"how many elements are examined\" for a given target: trace the mid values. Binary search on an unsorted array gives wrong answers, not an error. Both searches can be written for ArrayLists using get and size . public static int binarySearch(int[] a, int target) { int low = 0; int high = a.length - 1; while (low <= high) { int mid = (low + high) / 2; if (a[mid] == target) return mid; else if (a[mid] < target) low = mid + 1; else high = mid - 1; } return -1; } // a = {2, 5, 8, 12, 16, 23, 38}, target 23 // mid=3 (12) < 23 → low=4; mid=5 (23) → found. 2 comparisons. For \"how many times is the target compared,\" trace mid: write low, high, mid, and the element at mid on each pass. For \"which search should be used,\" unsorted → linear; sorted and large → binary. And binary search's mid uses integer division — truncation matters. Linear search works on anything, needs no preparation, returns the index of the first match or −1 (or a boolean). Worst case n comparisons. It's the right choice for one-off searches on unsorted data. Binary search needs sorted data. low / high bracket the remaining range; mid = (low + high) / 2 ; compare, then discard half by moving low or high past mid. Loop while low <= high . If they cross, not found. Trace protocol: a table with columns low, high, mid, a[mid], action. One row per comparison. \"How many elements are examined\" = number of rows. mid uses integer division , so for an even-size range it picks the lower middle. With low = 0, high = 7 , mid = 3, not 3.5 or 4. Adjust past mid: low = mid + 1 or high = mid - 1 . Setting low = mid can loop forever when low and high differ by 1. Comparisons: up to ⌊log₂ n⌋ + 1. For n = 1000, 10. For a million, 20. Linear would be up to n. On an unsorted array , binary search doesn't throw — it just returns wrong answers (may miss a present element). The exam asks \"why did this return −1 even though the element is there\" → not sorted. On ArrayLists : same algorithm with get(mid) and size() . Setting low = mid or high = mid. Off by one; possible infinite loop. Rounding mid up. Integer division rounds down. Miscounting comparisons. One per loop iteration. The found step counts. Using binary search on unsorted data. Wrong answers, not errors. A binary search is performed on the sorted array {1, 4, 7, 10, 13, 16, 19, 22} for the value 4. Which elements are compared to the target, in order? 10, 4 10, 7, 4 13, 7, 4 1, 4 low=0, high=7, mid=3 → 10. 4 < 10 → high=2. mid=1 → 4. Found. Two comparisons: 10, then 4. Which of the following is true about binary search? It works on any array regardless of order. It requires the array to be sorted and eliminates about half the remaining elements with each comparison. It always examines every element. It is slower than linear search for large arrays. Sorted input and halving are the two defining properties. Linear search: checking elements one by one until the target is found Binary search: repeatedly halving a sorted range to locate a target",
  "ap-csa/4-15.html": "Two sorts to know by mechanism, not memorized code: what each pass does and what the array looks like after k passes. Selection sort: for each position i from 0, find the smallest element in the unsorted part (i to end) and swap it into position i. After k passes, the first k elements are the k smallest, in order. Selection sort always does the same number of comparisons regardless of input order: about n²/2. It does at most n − 1 swaps. Insertion sort: for each element from index 1 onward, shift it left past larger elements until it's in place among the already-sorted prefix. After k passes, the first k + 1 elements are sorted relative to each other (but may not be the k + 1 smallest overall). Insertion sort is fast on nearly-sorted data (few shifts) and slow on reverse-sorted data (many shifts). The exam shows an array after some number of passes and asks which algorithm produced it, or asks for the array state after a given pass. Both are O(n²) in the worst case; merge sort (4.17) is faster for large inputs. Sort {5, 2, 8, 1, 9} : Pass Selection sort Insertion sort 1 {1, 2, 8, 5, 9} — 1 swapped to front {2, 5, 8, 1, 9} — 2 inserted before 5 2 {1, 2, 8, 5, 9} — 2 already in place {2, 5, 8, 1, 9} — 8 already in place 3 {1, 2, 5, 8, 9} — 5 swapped in {1, 2, 5, 8, 9} — 1 shifted to front 4 {1, 2, 5, 8, 9} {1, 2, 5, 8, 9} Tell-tale: after pass k, selection has the k smallest at the front; insertion has the first k + 1 in order but not necessarily the smallest. Given a mid-sort snapshot: if the first few elements are the globally smallest values in order, it's selection sort. If the first few are in order but a smaller value still sits later in the array, it's insertion sort. Selection sort, pass i: scan positions i through end, find the index of the minimum, swap it into position i. After pass i, positions 0..i hold the i+1 smallest values in order. Exactly one swap per pass (or zero if the minimum is already there). Insertion sort, pass i: take element i, move it left past larger elements until it's in place among 0..i. After pass i, positions 0..i are sorted relative to each other — but a smaller value may still exist later in the array. Identifying from a snapshot: if the sorted prefix consists of the globally smallest values → selection. If the prefix is sorted but some later element is smaller than something in the prefix → insertion. Work: selection always does ~n²/2 comparisons regardless of input, and at most n−1 swaps. Insertion does few shifts on nearly-sorted input and ~n²/2 on reverse-sorted. So insertion is faster on nearly-sorted data; selection does fewer writes. Both are stable in the sense the exam cares about: they sort in place, need no extra array, and are O(n²). Merge sort (4.17) is O(n log n) but needs extra space. Sorting Strings or objects uses compareTo or an accessor in the comparison — the algorithm doesn't change. Descending order: flip the comparison. Everything else identical. You don't write these on the exam; you trace them and identify them. But knowing the mechanics well enough to reproduce a pass is what tracing requires. Confusing the two from a mid-sort array. Check whether the prefix has the smallest values overall. Thinking selection sort swaps every pass. Only if the min isn't already in place. Assuming insertion sort's prefix has the smallest values. It's sorted, not minimal. The array {6, 3, 9, 1, 7} is being sorted in ascending order. After two passes it is {1, 3, 9, 6, 7} . Which algorithm is being used? Selection sort Insertion sort Binary search Merge sort The two smallest values (1, 3) are in the first two positions — selection sort. Insertion sort after two passes would be {3, 6, 9, 1, 7}. Which of the following is true of insertion sort? It always performs the same number of comparisons regardless of input. It performs fewer shifts when the input is already nearly sorted. It requires the array to be sorted before it begins. It cannot be used on arrays of Strings. Insertion sort's work depends on how far each element must shift; nearly-sorted input needs few shifts. Selection sort: repeatedly select the smallest remaining element and swap it into place Insertion sort: insert each element into its correct position within the sorted prefix Pass: one full iteration of the outer loop of a sort",
  "ap-csa/4-16.html": "A method that calls itself. On the revised exam you trace recursion — you don't write it. Know the base case, count the calls, and follow the returns back up. A recursive method calls itself. It needs a base case (a condition where it returns without recursing) and a recursive call that moves toward the base case. Without a reachable base case, recursion never ends — a run-time stack overflow. Each call has its own copies of parameters and local variables. Changes in one call don't affect the others. Tracing: write each call with its argument, stop at the base case, then substitute return values back up the chain. Or draw the calls as a stack. Number of calls is a common question: count every invocation including the original. Any recursive method can be rewritten with a loop, and vice versa; recursion often mirrors the structure of the problem (factorial, sum of a list, string reversal). The revised CED says you will trace recursive code, not write it — FRQs won't require recursion. public static int f(int n) { if (n <= 1) return 1; // base case return n * f(n - 1); // recursive call } // f(4) = 4 * f(3) // = 4 * (3 * f(2)) // = 4 * (3 * (2 * f(1))) // = 4 * (3 * (2 * 1)) = 24 // Calls made: f(4), f(3), f(2), f(1) → 4 calls public static void printDown(int n) { if (n == 0) return; System.out.print(n + \" \"); printDown(n - 1); } // printDown(3) prints: 3 2 1 Write the calls in a column, arguments decreasing, until the base case returns a concrete value. Then fill in upward. For methods that print, note whether the print happens before or after the recursive call — that reverses the order of output. Structure: base case(s) first (return without recursing), then recursive case(s) (call with a smaller/closer argument, combine the result). If the argument doesn't get closer to a base case, infinite recursion → StackOverflowError. Each call has its own frame: its own parameters and locals. When f(4) calls f(3), f(4)'s n is still 4; f(3)'s n is 3. Changes in the inner call don't affect the outer. Two tracing techniques. Substitution: expand f(4) = 4 * f(3) = 4 * 3 * f(2) … until base, then multiply back. Stack: list the calls in a column as they happen; when the base returns, cross off from the bottom up, writing each return value. Order of side effects: a print before the recursive call happens on the way down (n, n−1, …). A print after happens on the way up (…, n−1, n). Same code, reversed output. Counting calls: include the original. f(n) with f(n−1) makes n calls to reach f(1)… or n+1 to reach f(0). Halving (f(n/2)) makes about log₂ n + 1 calls. Base case correctness: the recursion must hit it exactly. if (n == 0) with f(n - 2) from an odd n skips 0 and never stops. if (n <= 0) is the safe form. Recursion on Strings and arrays: process the first character, recurse on the rest ( s.substring(1) ); or use an index parameter that increments. Base case: empty string / index == length. Any loop can be recursion and vice versa. The exam may show a recursive method and ask which loop is equivalent. The 2025–26 CED says recursion is traced , not written . FRQs won't require it. Multiple choice will trace it. Not tracking each frame's own variables. Inner calls don't change outer variables. Printing in the wrong order. Before the call = descending. After = ascending. Miscounting calls. Include the first call and the base-case call. Missing a skipped base case. Check whether the argument sequence actually hits it. What is returned by g(5) ? public static int g(int n) { if (n == 0) return 0; return n + g(n - 2); } 9 15 5 The method never terminates g(5) calls g(3), which calls g(1), which calls g(-1). The base case checks n == 0 exactly, and n goes 5, 3, 1, -1, -3, … skipping 0 entirely. The base case is never reached, so the recursion never stops. (A base case of n p(3) ? public static void p(int n) { if (n > 0) { p(n - 1); System.out.print(n + \" \"); } } 3 2 1 1 2 3 3 2 1 0 0 1 2 3 The print happens after the recursive call, so the deepest call prints first: 1, then 2, then 3. How many times is the method h called (including the initial call) when h(8) is executed? public static int h(int n) { if (n < 1) return 0; return 1 + h(n / 2); } 3 4 5 8 h(8) → h(4) → h(2) → h(1) → h(0). Five calls. Recursion: a method calling itself Base case: the condition under which a recursive method returns without recursing Recursive call: the call to the same method with an argument closer to the base case",
  "ap-csa/4-17.html": "Binary search written recursively, and merge sort — the divide-and-conquer sort that's faster than selection and insertion. Trace, don't write. Recursive binary search: the same halving as the loop version, but each half-search is a recursive call with updated low / high . Base case: low > high (not found) or the middle matches. Merge sort: split the array in half, recursively sort each half, then merge the two sorted halves into one sorted array. The merge step walks through both halves with two indices, copying the smaller front element each time, then copying any leftovers. Merge sort's base case is a sub-array of one element (already sorted). Merge sort is faster than selection or insertion sort for large inputs — roughly n log n operations instead of n². It needs extra memory for the temporary merged array. The exam may show the array at some stage of merge sort (halves sorted, not yet merged) and ask which stage or which algorithm. Both algorithms illustrate divide and conquer : split, solve pieces recursively, combine. public static int bs(int[] a, int t, int low, int high) { if (low > high) return -1; // base: not found int mid = (low + high) / 2; if (a[mid] == t) return mid; // base: found if (a[mid] < t) return bs(a, t, mid + 1, high); return bs(a, t, low, mid - 1); } // merge sort on {8, 3, 5, 1}: // split → {8, 3} and {5, 1} // sort each → {3, 8} and {1, 5} // merge → compare 3 vs 1 → 1; 3 vs 5 → 3; 8 vs 5 → 5; leftover 8 // result {1, 3, 5, 8} For merge sort traces, the snapshot after the recursive sorts but before the final merge shows two independently sorted halves — that's the give-away. For recursive binary search, count the calls the same way as 4.14 counts comparisons. Recursive binary search takes the range as parameters: bs(a, target, low, high) . Base cases: low > high → −1; a[mid] == target → mid. Otherwise recurse on one half. Same comparisons as the iterative version; the exam may show either. Merge sort: if the range has ≤ 1 element, done. Otherwise split at the middle, sort each half recursively, merge. The recursion goes all the way down to single elements before any merging happens. Merge step: two indices, one per half, walking forward. Compare the elements at the indices, copy the smaller into the output, advance that index. When one half runs out, copy the rest of the other. This produces a sorted combination of two sorted inputs. Snapshots: after the recursive sorts and before the final merge, each half is sorted independently — that's the identifying state. During the merge, the output fills left to right with the globally smallest remaining. Efficiency: log₂ n levels of splitting, each level does n work merging → n log n total. For n = 1,000,000: ~20 million operations vs. ~500 billion for selection sort. Cost: merge sort needs a temporary array of size n. Selection/insertion sort in place. Space vs. time trade-off. Divide and conquer is the general name: split the problem, solve pieces recursively, combine. Binary search (discard half), merge sort (sort halves and merge), and many others share the pattern. You trace these; you don't write them. Know what each stage looks like and how many operations each takes. Thinking merge sort merges before both halves are sorted. Recursion completes first. Picking a snapshot where halves aren't independently sorted as \"before the merge.\" Both halves must be sorted. Saying merge sort uses less memory. More. It's faster, not smaller. During a merge sort of {7, 2, 9, 4, 6, 1} , which of the following could be the state of the array just before the final merge? {2, 7, 9, 1, 4, 6} {1, 2, 4, 6, 7, 9} {2, 7, 9, 4, 6, 1} {1, 2, 4, 7, 9, 6} Before the final merge, each half is sorted independently: {2, 7, 9} and {1, 4, 6}. Which of the following best describes why merge sort is preferred over selection sort for very large arrays? Merge sort uses less memory. Merge sort performs roughly n log n operations rather than n², so it is much faster as n grows. Merge sort does not require recursion. Selection sort only works on sorted arrays. The n log n vs n² gap is the reason. Merge sort actually uses more memory. Merge sort: recursively sort halves, then merge them Merge: combining two sorted sequences into one sorted sequence Divide and conquer: split a problem into smaller instances, solve recursively, combine",
};
